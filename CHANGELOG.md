## 0.72.2 — 2026-09-17

> 界面**卡死之后能自己爬起来**：壳按秒守着界面的心跳，心跳停了先探活，真停了就留证据、
> 重载界面——回到最后落盘的那一版、原来那一章。**真机演练（往页面里注入一个死循环）还没跑**，
> 这一版先把机制与单测立住。

### 新增

- **守护之心**（`app/src/watchdog.rs` 状态机 + `app/src/guardian.rs` 壳侧）：
  - 心跳走既有那条路——界面每 3 秒的写后读回校验（`body_fingerprint`），壳在内存里打一个
    时间戳，**不写库**；
  - 沉默 15 秒**不判死**：先注入一次探活（`ui-probe` 事件），界面回一声（`ui_alive`）就撤回误报
    ——误报要付的代价（丢掉内存里那几个字）远大于多等 3 秒；
  - 探活 3 秒还不回话才判死：先把现场（第几次、卡在哪一章、最后一版的指纹）写进会话标记与
    op-log，再重载 WebView——界面重新 mount 后按会话标记回到原来那一章；
  - **绝不无限重载**：退避 30 / 60 / 120 秒，一次会话最多 3 次；用尽、或连重载都调不动，就停手，
    把结论留给界面下次起来时说；
  - 探活回话**不经过数据库**：核心或库卡住时不会被误判成界面卡死（那种情况重载也救不了）；
  - **从"界面报到就绪"那一刻才开始判**（复用关窗闸门那面旗）：冷启动那一段（磁盘慢、首帧慢）
    可能十几秒没有心跳，那是在启动、不是在卡死——不这么分，慢机器上第一次开窗就会被判死重载。
- 启动交代多一句「界面刚才卡住了，已自动重载（第 N 次）」，并**压过**"上次没有正常退出"——
  重载期间会话标记仍是"未正常退出"，不区分就会每次复活都蹦一句假警报。

### 变更

- 会话标记多一格"卡死现场"：正常退出时清掉（那一次重载已经告诉过作者了）。

### 验收

- 状态机单测 **10 例**（假时钟）：超时先探活 / 回话撤回 / 不回话才判死 / 退避与上限 /
  停手只报一次 / 连重载都调不动就直接停手 / 一直很忙的界面不会被误判；
- 核心 **3 例**：卡死现场进会话标记与 op-log（同一事务）/ 正常退出把它清掉 /
  被杀之后它还在，且正文一个字不少；
- 前端 **5 例**：启动交代的四种情形（干净 / 崩溃 / 已复活 / 复活失败）与"不许蹦假警报"。

### 还没做的

- **真机演练**：往页面里注入一个死循环，看判定 → 探活 → 重载 → 回到原章 → 提示这一整条；
  以及"整个 WebView 浏览器进程没了"那一档 `reload` 能不能救回来（救不回来就如实告知作者，
  不做整进程重启）。
## 0.72.1 — 2026-09-17

> 写作流程（SOP）这件事的**底座**落下来了：作者"先干什么、后干什么"本身成了一格数据——
> 跟人（不跟作品）、可按作品临时覆盖、改动留痕且能回滚。**界面还没接**（那是下一步），
> 这一版先让核心说得清"有哪些步、什么顺序、勾没勾"。

### 新增

- **`store/sop.rs` + `store/sop_store.rs`**：一条 SOP 是**有序步骤列表**（id / 名字 / 说明 /
  参考时长 / 检查项 / 绑定动作 / 跳过标记），存在 `settings` 表里——键是 `sop`（我平时的写法）与
  `work.{id}.sop`（这一篇临时用一份），与外观偏好同一套命名空间，**不建表、不迁移**。
  （形状与"规矩 + 存取"分开两件写：前者只有类型、常量与默认模板，后者是校验与三层读写——
  与 `card` / `card_move`、`node` / `node_edit` 同一种分法。）
  - 三层来源：**书的覆盖 → 全局 → 内置那套起手流程**（定核心 → 搭大纲 → 写初稿 → 回头改）；
    核心只给 id 与顺序，**一个字的文案都没有**——名字与说明是用户数据，没改过的由界面
    按语言去字典里取（`sop.step.<id>`），所以默认文案将来改了，旧库跟着变，不必写迁移；
  - 两层之间是**不覆盖 / 覆盖**（"选已有 / 创建临时"就是这两种状态），层**内**只存改过的项；
  - **改动留痕且可回滚**：每次写与它**同一个事务**记一条 op-log（payload 是整份 SOP），
    `sop_history` 看"我改过什么"，`restore_sop` 回滚到某一次——而回滚本身又是一次新改动，
    **历史不涂改**（与快照同一条思路）；
  - 写错了当场报（id 重复或不合规、文字过长、参考时长越界、认不出的绑定动作），读回来那条路宽
    （坏 JSON 当没设过、坏项丢坏项）——不拿一格坏数据卡住面板。
  - 铁律落到数据上：**没有"下一步"门禁**（任何一步都能勾、能跳、能回退），**没有打卡与连续天数**
    （库里不存那类字段，有一条白名单守卫盯着），参考时长只是参考值、不进任何判据。
- 八条面向界面的新错误码（`sop.*`）连同字典文案一起进闸门。

### 变更

- `skipped` 与检查项的 `done` 是 `false` 时不进库——"只存改过的项"这条对布尔一样成立。

### 验收

- `crates/yanmo-core/tests/sop.rs` **9 例全过**：默认那套只给 id 与顺序 / 稀疏落库与"清空即删键" /
  书覆盖是整份覆盖（全局那几步一步都不混进来）/ 坏记录与坏项都不卡面板 / 八种错输入各报各的码 /
  每次改动留整份副本且新的在前 / 回滚能还原、且历史变成三条 / 别的层的 seq 不给回滚 /
  **白名单守卫**（SOP 里冒出"连续天数""达成日期"这类字段当场红）；
- 内置步骤 id 有守卫：它们要拼成界面字典键，点号与空格会把字典查崩，而那种崩是**静默**的。

### 还没做的

- **界面与命令面**：右侧第二栏第三片页签「流程」、`app/src/commands/sop.rs` + `api/sop.ts`、
  默认步骤的中文文案进界面字典；机械检查项与「计划 vs 实际」那一层的接线。

## 0.72.0 — 2026-09-17

> 「磁盘上一直有一份 `.md`」这条承诺，现在**能从外面验了**：多一个不开窗口的启动入口，
> 把"稿库 → 磁盘 `.md`"的对账跑几轮、把结论写成文件。它走的是界面里**同一条代码路径**
> （同一个对账函数），所以你从外面验到的就是平时跑的那一套——不是另写一份给测试用的实现。

### 新增

- **`--mirror-sync <目录> [--rounds N] [--out <文件>]`**：不开窗口把磁盘 `.md` 镜像对账跑 N 轮
  （默认 2 轮），结论写成 JSON——账上照看多少份、有几份等你定夺、镜像目录里有没有没账的文件、
  有没有没对上的书。
  - 报告里的 `rounds` 记着每一轮**有没有真往盘上写**：一轮该写、紧接着的第二轮什么都不该动，
    **幂等有据可查**，不是靠保证；
  - **退出时把会话关干净**（报告里的 `session_closed`）：这一趟只对账、没碰正文，
    所以走"只标干净、不写快照"那条路——不然你下次打开会看到一句"上次不是正常退出"的假警报；
  - 退出码：`0` 账对上了 / `1` 有书没对上或出过错 / `2` 连库都打不开（**没跑起来**，
    与"跑完发现不对"分开说）。
- 它与另外三条自检入口（数据基准 / 界面冷启动 / 库体检）同族：**只由启动参数进入、
  不开任何网络通道、数据目录由参数给**（默认系统临时目录，不碰你正在用的那份稿库）。
  `--version` 报的能力档位从 `acceptance=1` 升到 `2`——**只增不改**，按 `>= 1` 判断的脚本不受影响。

### 变更

- **一次对账"该怎么做"从"数据句柄从哪儿来"里拆了出来**：界面的后台线程与这个无窗口入口
  从此调**同一个函数**（同一份计划、同一份账、同一条"外面的改动一个字不碰"），
  不会各长一套、也不会哪天两边的判断悄悄分叉。
- 安全文档补一节「自带验收」：四条入口各自做什么、默认什么状态、你怎么自己验。
- 顺手拆了两件（代码健康巡检的棘轮盯着的）：**启动参数面**搬进 `acceptance_args.rs`
  （"从命令行怎么进来"与"进来之后做什么"是两件事），**无窗口驱动**单独成
  `mirror_headless.rs`（镜像是节拍 / 一轮怎么走 / 碰磁盘 / 从外面驱动的形状，四件各管一档）。
  `acceptance.rs` 因此从 695 行降到 607 行。

### 验收

- 壳层新增 3 例真写临时目录的用例：① 一次对账把整本书写上盘、**第二轮一个字都不再动**（幂等）；
  ② 从外面改过的那一份**一个字都不碰**、只如实报成待定夺；
  ③ **收尾之后会话是干净的**（下一次打开不会看到"上次不是正常退出"的假警报）；
- 壳层用例全过（新增后 58 例）、边界守卫照旧全过（零出网 / 核心无界面依赖 /
  开发档命令不进发行版）；
- QA 门全过：静态（版本四处一致 0.72.0、公开内容脱敏 0 命中、代码健康巡检 0 新增上帝）
  + Rust 全仓 609 项 + 前端 351 项 + 生产构建 + 规模基准。

## 0.71.0 — 2026-09-17

> 0.68.1 里那句「为什么一直没人发现：`.vue` 的 `<script setup>` 段没有类型检查」，
> 在这一版补上了：前端类型检查换成**认单文件组件的检查器**，组件内部的 script 段、
> 模板里的表达式、跨组件传参都在闸门里。这道闸门一开，当场抓出两处**作者碰得到的真错**——
> 设置里「排版」那三个旋钮从 0.50.0 起就没接到正文上，以及创作流里"这条是在这一章写的"
> 那个小标记从来不出现。

### 新增

- **前端类型检查扩到 `.vue`**：`npm run typecheck` 从纯 `tsc` 换成 `vue-tsc`——同一份
  `tsconfig.app.json`，不额外多一道工序，检查的是同一批文件，只是这次**进得去单文件组件**。
  原先那份只为"让纯 `tsc` 认得 `.vue`"的声明文件随之删掉：它在，等于给这个盲区贴了张
  "已经查过了"的条子——而它其实一个字都没查。

### 修复

- **设置 →「排版」的三个旋钮（字号 / 行距 / 字距）现在真的作用到正文**。0.50.0 引入这一栏时，
  值算成了 CSS 变量却**从没绑到编辑区那个元素上**，样式表里读到的是兜底值（`var(--ym-body-size)`
  后面那个默认数），所以拖旋钮只有面板里的示例字在变、正文一动不动。现在绑上，拖动即时生效。
- **创作流：「这条是在当前这一章写的」标记会出现**。面板内部算出了"当前这一章"，
  但**没有把它交出去**，界面那一头拿到的是空值，于是那个小标记永远不显示。现在交出去了。

### 变更

- **第三方声明**补登 TypeScript（Apache-2.0）与 vue-tsc（MIT，均为开发期依赖）。

### 验收

- `vue-tsc -p tsconfig.app.json --noEmit` **无输出**：存量 7 处全清（两处是真错，五处是历次
  重构留下的死绑定，一并摘掉）；
- 前端 351 项、Rust 全仓 606 项、生产构建与规模基准全过——QA 门给的是「通过」
  （版本四处一致 0.71.0、公开内容脱敏 0 命中）。规模基准顺带复量了这一版：1000 章 / 约 315 万字，
  写入 971ms、全树 61ms、「计划 vs 实际」全对一遍 250ms；
- **闸门强度实测过**（不是只查了组件自己那一亩三分地）：故意在跨组件调用处传一个错类型
  （`<ShelfDialog :session="123" />`），当场报 `TS2322`；证实后已改回。

## 0.70.0 — 2026-09-17

> 大纲不再只是"写之前填一次"：写完之后点「大纲 → 计划 vs 实际」，研墨把每一章的**计划**
> 与**正文里字面认得到的东西**对一遍——谁出现在正文里、哪条伏笔像被点到了——一条条摆出差异，
> 改不改由你。它**只报不改**，唯一能写的是"把正文里冒出来的人补进计划"（只补不删，改之前留底）。

> 发布说明只取本文件最新一节，而这一版与上一版（0.69.0「磁盘即 .md」）都还没发布过：
> 真要发这一版时，请把那一节一并带上（或先合并成一份汇总稿）。

### 新增

- **大纲 →「计划 vs 实际」**（第三片页签）：逐章三档——**未写**（计划填了、正文还空着）、
  **已写**（计划里的人在正文里都认到了）、**偏离**（计划里的人没认到 / 正文里冒出计划外的人 /
  正文写了而计划一字没填）。默认只列"未写"与"偏离"的，展开看细节。
  - **认人是字面的**：设定卡的名字与别称，在正文里按"最长优先"匹配——两张卡分别叫"小明"与"明"时，
    短的那个不会被"小明"顶成出现过；
  - **伏笔按相近认**：还埋着的伏笔，它的正文里有两个以上三字片段在正文里出现，就算"像被点到了"。
    这一类**单独列、标成候选**（字面像不等于真收了），不跟认人混在一起；
  - **四格与「一句话」判不了**：那要读懂正文，研墨不猜——它们不进这一屏比。
- **人工确认的"对齐"**：展开一条偏离，把"正文里出现、计划里没有"的人一键补进这一章的出场人物。
  **只补不删**：计划里挂了、正文里没认到的，只提示，绝不自动撤。
- **大纲自己也有留底**：每次对齐之前先把这一章的大纲（一句话 / 四格 / 出场人物）存一份，
  随时能「撤销上一次对齐」把它放回去（每章滚动留最近 10 份）。
- 库结构升到 v16（新增一张大纲留底表；纯加法，老库打开即升）。

### 验收

- 核心 10 例：认人认准（计划里挂了、正文没认到 → **只报不删**）、正文冒出计划外的人 → 可补、
  别称算出现、**短名字被长名字盖住时不算出现**、未写/已写/偏离三档、两样都空的章不报、
  伏笔候选只报还埋着的、字面不像就不报、**对齐前留下底且撤销能把补进来的人撤掉**；
- 前端 351（新增这一屏的纯逻辑 4 例）；`tsc -p tsconfig.app.json --noEmit` 无输出；
- 规模基准顺带量了这一屏：1000 章 / 约 315 万字、100 张人物卡 / 20 条伏笔，
  **全书对一遍约 0.26 秒**（这一族最重的一步：要把全部正文读一遍做字面匹配）。

### 说明

- **认不出不等于你没写**：它只是"研墨没认出来"。要让认人认得准，先把设定卡建起来（或补上别称）。
- 这一屏**只读正文、不改正文**；唯一的写是把人补进计划，而且改之前留底。

## 0.69.0 — 2026-09-17

> 「稿子永远是你能用记事本打开的 `.md`」不再只是一句承诺：库每落一次盘，同一份内容就在
> 数据目录的 `mirror/` 里同步出一份纯文本。写入链路（结构 / 改名 / 幂等）与**外面的改动
> 一个字不碰**都在这一版；在研墨之外被改过的那一份，启动后会列出来请你逐条定夺
> （收进研墨 / 用研墨的版本盖回去）。

### 新增

- **磁盘 `.md` 强镜像（默认开启）**：每个承载正文的节点在
  `mirror/<书名>-<作品号>/<卷>/<章>.md` 里各有一份，内容是第一行 `# 显示章名`、下面是正文
  （换行统一成 LF，末尾留一个换行）。
  - 目录结构、文件名里的三位序号、非法字符处理与「导出成文件」**同一套口径**（两边不会各长各的）；
  - 作品目录带作品号：两本同名书各有各的目录，不会互相踩；
  - **幂等**：同样的内容渲染出同样的字节；谁都没动过时一次对账零写盘（放进 git 没有噪声）。
- **落盘即写、异步**：正文落盘之后只投一个信号，后台线程自己合并、自己写——击键那条路一秒都不多等。
  另有每 2 秒一次的巡检兜底（结构变更、回收站清空这类动作也跟着走），长时间没人动时自动放慢。
- **改名 / 移动是 `rename`**：章改了名或挪了卷，磁盘上是同一个文件换了名字（版本工具认得出这是
  同一个文件），不留旧名字的孤儿；卷空了、整本书没了，空下来的目录一并收掉。
- **外面的改动一个字不碰**：文件在研墨之外被改过（记事本、别的编辑器、同步盘）时，研墨
  **不覆盖也不删**，只如实计数、在设置里报出来；作者把文件改回研墨这一版之后，冲突自己解除。
  想写的路径上本来就是别的东西时，同样不覆盖。
- **设置里多一块「磁盘上一直有一份 .md」**（在「稿子放在哪」那一栏）：开关、镜像文件夹路径、
  打开镜像文件夹、立即对一遍、已对上多少份 / 有几份被改过。**关掉不删任何已有文件**。
- **磁盘上那份被动过时，研墨会来问你**：启动后第一趟对账发现"磁盘上这一份不是研墨写的那份了"
  （记事本改过、别的工具动过、同步盘覆盖过），就把清单请出来，逐条给两条路：
  - **收进研墨**：磁盘上那份的字收进库里，**库里原来那一版先留成快照**（版本历史里看得到），
    两边都不丢；
  - **用研墨的版本盖回去**：删掉磁盘上那份，镜像随后按库里的字重写。
  - 处置之前**先把手上的字落盘**；收进研墨的如果正好是编辑器里开着的那一章，正文会立刻换成
    库里的版本（不换的话，下一次自动落盘会把刚收进来的字又盖回去）。
  - 镜像目录里**没账的 `.md`**（多半是自己把文件挪了名字）只列出来、**一个都不动**。
  - 这套清单在设置里也看得到（「看看这些文件」），不必非等启动那一下。

### 变更

- **作品目录名只有一处口径**：导出与镜像共用同一个函数（`<归一化书名>-<作品号>`），
  两处不再各写一遍——改一处漏一处的结果，是作者的同名作品有一边会互相踩。
- **拆件**（各成一件，变化理由不同）：导出 / 编译的落盘（写文件、清单、清残留）拆到
  `export_fs`，逃生导出的落点策略拆到 `escape`；运行期数据句柄因此瘦了 100 多行，
  镜像那边也分成了「决策」（纯逻辑）与「碰磁盘」两半。

### 验收

- 核心 19 例：结构稳定、两次渲染同字节、幂等对账、改名是 rename 而不是删了重写、外部改动只报
  不覆盖、别人的文件不覆盖、删章收文件、收残留不误删这一轮刚写的位置、日常对账信账而全量核对
  看盘、冲突自行解除、重名作品各自成目录、开关默认开；另有**取正文与写文件互为逆运算**、
  手改过的文件两种形状都读得懂、**收进研墨会把库里那一版留成快照且不污染今日码字**。
- 壳层 7 例（真的写进临时目录再回来看）：落盘内容、二次对账零动作、外部改动字节不变、改名后
  旧文件消失、删掉最后一章后空掉的卷目录一起收掉、改一章只重写那一个文件、扫目录只认 `.md`、
  **「盖回去」之后冲突真的消失**。
- 前端 347（新增镜像状态那一行与清单那两条的 7 例）；`tsc -p tsconfig.app.json --noEmit` 无输出。
- 库结构升到 v15（新增一张镜像账表：节点 → 相对路径 + 两把指纹 + 冲突标记；纯加法，老库打开即升）。

### 说明

- 镜像是**「不让你被格式困住」**，不是备份：它和稿库在同一个盘、同一个文件夹里，盘坏了会一起没。
- **收进研墨不做逐字合并**：作者改的那份整篇进库，库里原来那一版留成快照——两边都留着，
  由人来对照，机器不猜。
- 三处已知边界：① 日常对账**信账不看盘**（性能取舍），文件被删或长度变了由空闲巡检发现，
  **等长替换**要等启动或手动「立即对一遍」；② 章节改名时若旧路径那份被人改过，研墨不碰它，
  它会作为「没账的文件」列出来；③ 从备份恢复之后，与备份不同的那些章会显示成「被改过」，
  宁可不覆盖，等你自己定夺。

## 0.68.1 — 2026-09-16

> 真机复看 0.68.0 时抓到的两件事：**「对不上的地方」一直报错**（报的还是一句查不到的
> `error.ipc.unavailable`），以及「总纲」该在「大纲」里而不是藏在「资料」弹窗第三页。

### 修复

- **「对不上的地方」（大纲体检）自 0.63.0 起一直是坏的**：`OutlineCheck.vue` 把会话里的
  `workId`（一个 Vue 的 ref）**当值**递给了面板，面板里 `if (!work)` 判得过去、参数却是
  一个对象，于是每次扫描都在 IPC 那一层反序列化失败。面板改成与别的面板同一形状（收 `Ref`，
  内部取 `.value`），`watch` 也跟着改成盯 `props.workId.value`。
  - 为什么一直没人发现：`.vue` 的 `<script setup>` 段**没有类型检查**（纯 `tsc` 进不了 SFC），
    这种"忘了 `.value`"编译期一声不吭。
- **报错被包装成天书**：`asError()` 会把**已经整好的错误**再包一层——`CoreUnavailableError`
  的码（`ipc.unavailable`）被当成核心给的码去查字典，于是屏幕上只剩一个查不到的键，
  **真正的原因（底层那句英文）被这层包装吃掉了**。现在已经是界面自己的错误就原样交回，
  底层那句话照旧留在句子里。
- **新增一道机械守卫**：`call()` 参数里混进 ref 会**当场**报一句能照着改的话
  （`ipc.ref_arg`：哪个字段忘了 `.value`、哪条命令），不再让它烂到 IPC 那一层。

### 变更

- **「总纲」挪进「大纲」，并且是第一页**：顶栏「大纲」现在两片页签——**总纲（默认显示）**
  与**章纲**（原来那张大表）。作者点「大纲」先看到"这本书讲什么"，要逐章填再点第二页；
  Esc 分三层退（选人卡 → 回总纲 → 收起整屏）。
  - 「资料」弹窗回到四页（人物 / 设定 / 事件 / 伏笔）：总纲是"架子"，不是"资料"；
  - 大纲表表头那一行总纲摘要撤了（总纲已经在同一屏的第一页，不必再摆一份）；
  - 投稿包那一节「故事总纲」不变（它在「大纲」之前）。
- **拆件**（两页结构落地后顺手做的，也是巡检的要求）：`OutlineGrid.vue` 只管外壳
  （遮罩 / 标题 / 两片页签 / Esc），章纲那一页整块搬进 `OutlineTable.vue`，
  样式跟着分成 `outline-grid.css`（外壳）与 `outline-table.css`（表）。

### 验收

- Rust 569（不动）；前端 340（新增：已整好的错误原样交回、ref 参数守卫、默认落总纲那一页）；
- **拿真机那份库在命令行上核对过**：同一本书 `outline-scan` 返回 0 条、退出码 0——
  核心一直是好的，坏的是面板那一层（这也正是那条报错最该说清、却没说清的地方）。
- QA 门全过：静态 + Rust + 前端 + 生产构建。

## 0.68.0 — 2026-09-16

> 真机一问顶出来的缺口：「现在的大纲没有总纲吗」——确实没有。大纲表管的是"**这一章**写什么"，
> 而作者动笔前要想清楚的是"**这本书**讲什么"。这一版把那一层补上：一段自由文本，
> 位置在「资料」第一片页签「总纲」，投稿包里在大纲前面单独一节。

### 新增

- **故事总纲**（「资料 → 总纲」）：题材与风格 / 主线 / 卖点 / 主角想要什么 / 结局往哪儿走——
  一段自由文本，在框里直接写、**点别处就存下**（与大纲表逐格存同一条手感，一段长文没有"提交"那一刻）。
  - **与「作品简介」分开两格**：简介是给编辑看的（投稿 docx 里摆在书名下面），
    总纲是给自己看的（这本书的架子）——合成一格，作者每次落笔都要先想"这段是写给谁的"；
  - **大纲表表头多一行总纲摘要**（取第一句有内容的话，太长截断），点一下跳进那一页去写；
  - 投稿包 docx 在「大纲」前面多一节「**故事总纲**」（作者没写就整节不出现，绝不补占位句子）。
- **存储：迁移 v14** —— `works` 加一列 `storyline`。挂作品表上而不是另起一张表：
  一本书只有一份、与作品同生共死（软删 / 级联都跟着走），另起表只会多一个写入口与一次 JOIN。
  `ALTER TABLE ADD COLUMN` 走"先查列再加"（迁移铁律的幂等化），**旧库打开自动升级**，
  升级到一半断电再跑也不会撞车。

### 顺手整的

- 「资料」弹窗那两处过时注释（0.65.0 撤掉场景卡页签时留下的：还写着"五片页签 / 含场景卡"）改正。
- **两处拆件**（加总纲时两个文件越过 400 行，巡检当场拦下——按规矩拆，不是登记）：
  - 出场人物那张多选卡整块搬进 `OutlineCastPicker.vue`（表里唯一"点选"的一格，与打字 / 拖动
    两套手感都不搭）；大纲表组件 405 → 397 行；
  - 作品的**简介与总纲**搬进 `store/work_text.rs`（两段长自由文本共用一条规矩：存原话、
    留痕只记字数、回执取库里那份）——**拆的是变化理由，不是行数**；`store/work.rs` 443 → 377 行。

### 验收

- Rust 565 → **569**（新增：总纲原样存取与两格互不影响 1 例 + **老库长出新列且稿子一字不动** 1 例 +
  迁移重跑幂等 1 例 + 迁移 `prepare` 幂等 1 例）；投稿包那一节并进既有的 docx 验收断言
  （总纲在大纲前面、正文在总纲前面）；
- 前端 336 → **341**（摘要取值 / 空判 / 截断 3 例、总纲页只读总纲 1 例、切页与收起顺手存档 1 例）；
- QA 门全过：静态 + Rust + 前端 + 生产构建。

## 0.67.0 — 2026-09-16

> 接着 0.66.0 那张「大纲」大表做**1.5 期**：从别人的表格**整片粘进来**、把**出场人物**记在表上、
> 行还能**拖着换位置 / 换卷**。三件事都对着同一句真机诉求——"手上那份细纲表要能一次搬进来，
> 搬完还得看得见谁在第几章出场"。

### 新增

- **整片粘贴**（大纲表）：在任意可填的格子上 `Ctrl+V`，从 Excel / WPS / 记事本复制的一整片
  按 **Tab 分格、换行分行**落进来；锚点就是点下去的那一格，**往右往下铺**（与在电子表格里粘贴同一条直觉）。
  - 一片**一次事务**：里面有一行落在卷上（分组行）、或者表尾 / 表右边放不下，整片当场拒并说清是哪儿不对，
    **绝不留下"粘了一半"的表**；
  - **不是打字填的列原地跳过**（章名 / 伏笔 / 字数由核心算，出场人物是点选的）：按列号跳、
    别的列不错位——从 Excel 迁一整张表时那几列几乎一定在选中的区域里，为它们把整片退回去，
    等于"一键迁入"永远迁不进来；跳过了哪几列在回执里如实说；
  - 空格也照粘（迁进来的表要跟原表对得上）；粘完回执说清落了几格、几行；
  - 一片上限 2000 格（正常用碰不到，误把整张表贴进来会当场被拦住）。
- **「出场人物」列**：一章（/ 节 / 场景卡）挂了哪几张**人物卡**，点开是多选框，**点一下即落库**
  （整份覆盖；名字按名字排；表里只显示名字，不显示 id）。
  - 关联认 **id 不认名字**：作者把「陆文」改成「陆文昭」，表里下次读就是新名；
  - 删掉一张人物卡，它自动从每一章的人名里消失（软删的卡在读的时候就过滤掉了）；
  - 卷是分组行，没有"这一章有谁出场"这回事——那一格对它不摆。
- **表里拖行**：按住**章名那一格**拖动，可换位置、也能拖进别的卷（落点分上 / 中 / 下三段：
  排前面 / 放进这一卷 / 排后面）；拖进自己那一支（会成环）不给落点。

### 存储

- **迁移 v13**：新表 `node_cast`（`(node_id, entity_id)` 主键 + 反向索引）——出场人物是一张
  **关联表**，不是 `nodes` 上的一列（"这个人物在哪几章出场"要问得动；改名不用动关联；
  卡真被清掉时外键级联收尾）。**旧库打开时自动升级**，作者不用做任何事。

### 顺手修的

- **目录树拖动的"差一格"**：核心的移动是"先把自己摘出去、再插到第 index 位"，
  而目录树原来算下标时没减掉自己占的那一格——**把第 1 章往下拖一格，它会掉到第 3 章后面**。
  这一版把落点算法收成一处（`drop-plan.ts`，目录树与大纲表共用）并用例子钉住；
  上 / 中 / 下三段的其余行为不变。
- 大纲表那一屏多了两个驱动用的开发档命令（`cast-set` / `cast-list` / `outline-rows` / `paste-cells`），
  只存在于开发构建——界面点得动的事，自动化也要能反复跑。

### 验收

- Rust 555 → **564**（新增：出场人物 4 例 + 整片粘贴 4 例 + 迁移不变量 1 例）；
- 前端 321 → **336**（新增：拆格与落点 9 例、拖动落点 5 例、出场人物列 1 例）；
- 命令行多一例走查（挂人 → 表里看得见 → 整份覆盖 → 整片粘贴 → 一片里有一格落不了就整片拒）；
- QA 门全过：静态 + Rust + 前端 + 生产构建。

## 0.66.0 — 2026-09-16

> 真机反馈第三轮：「给成一个大表吧，现在这样分割一块块的很不好写」——照做，并且先按
> 要求**调研了市面写作软件的大纲录入形态**（结论落在 `docs/调研-大纲录入形态.md`）。
> 这一版把大纲录入改成**一整块主区的大表**：一章一行，要素就在格子里填。

### 调研结论（决定这一版形状的两条）

- **Scrivener 的 Outliner** 自己就把这叫做"给喜欢用电子表格的人"：稿件树是表的**第一列**，
  后面跟信息列（标签 / 状态 / 字数 / 自定义元数据），**列可自由开关、可拖宽**、摘要直接在表里改。
- **中文网文圈**最熟的是 Excel / 飞书那种**细纲表**：一章一行，列≈章节 / 剧情 / 出场人物 / 伏笔 / 字数。
  ⇒ 小表单把"顺着一列往下填"拆成了"开一次填一次"，这正是"很不好写"的来源。

### 新增

- **「大纲」表**（顶栏「大纲」/ 整块主区，Esc 关）：
  - **行**：卷（分组行，可收起）→ **章** → 章下的**场景卡**（缩进子行）；
  - **列**：章名（点一下跳正文）/ **一句话** / **视角 · 目标 · 冲突 · 结果** / 伏笔（埋几收几）/ 字数，
    **列可勾选开关**（默认露前六列），列宽按列固定；
  - **录入手感**：格子里直接打字，**Enter = 下一行同一列**（顺着一列往下填最省事）、
    Tab / Shift+Tab 左右走（浏览器原生）、**Shift+Enter 往回**、失焦即存（逐格存，
    回执是**库里真有的那一份**）；
  - 「**只看没补的**」筛选（没写一句话、或四格填了一半的），表尾「加一章」。
- **四格放宽到"凡承载正文的节点"**（章 / 节 / 单篇 / 场景卡）：中文网文的习惯就是一章一行，
  非要先建场景卡才填得了四格，等于给日更加一道工序。场景卡仍是"想把一场戏单独拆出来"的细粒度。
  - 编辑器里那张小表单也跟着对所有正文单位露面（不再只对场景卡）。
- **体检规则跟着调**：四格**只报"填了一半"**的——一个字都没填的不念（那是"还没打算填"，
  一本没规划过的书会在清单里刷出一百条，把真正该看的几条埋掉）；"哪些章还没补"归表里的筛选项。

### 顺手整的

- 五页签弹窗**缩回「资料」**（人物与设定 / 事件 / 伏笔）——**场景卡那一页撤了**（四格进表了，
  两处填同一份数据只会让人两头看）；顶栏那颗按钮改叫「资料」，另加一颗「大纲」。
- 树序走法**收到一处**（`Store::node_walk` 带缩进层级）：阅读顺序、体检数章、大纲表三条路
  共用同一次 DFS——分开写迟早会在"卷算不算一层、场景卡排哪儿"上分家。
- `NodeKind` 补上"按稳定码序列化"（它现在真的上线了：表里按类型分行），并纳入稳定码守卫。

### 验收

- 大纲表读数 2 例：树序 + 缩进层级（卷 0 / 章 1 / 场景卡 2）、列齐全（渲染后章名 / 一句话 /
  字数 / 有没有正文 / 四格）、伏笔账按"埋在哪章 / 收在哪章"数（只看埋着的算待收）；空书只有一卷。
- 四格验收 6 例：**章也有四格**（卷没有，如实拒）、没有那一行 = 四格全空、存了读回来一致、
  只列"填过的"、软删的不再列。
- 规则验收 12 例（含改过的 1 例）：只报"填了一半"的，填全与一字未填都不报。
- 前端 9 例（新）：默认列 / 卷不算一格 / 格值取值 / 空着的判定 / 要不要管一下 / 伏笔列怎么念 /
  折叠 / 只看没补的 / Enter 跳过卷那一行。
- 命令行端到端：记设定 → 填一格四格 → 体检三类都在 → 忽略幂等与回头路 → 软删后撞车消失 →
  填全后缺项消失；**章能填四格、卷不能**。
- QA 门：静态全过（版本四处一致 0.66.0）+ Rust 全仓 + 前端 321 项 + 生产构建。

## 0.65.0 — 2026-09-16

> 真机反馈第二轮：「设定」那一屏**没把大纲本体收全**——事件在创作流里、场景卡的
> 冲突/结果要打开那一章才填得到、而"设定"这一档还照抄了人物的字段（名字 / 别称）。
> 这一版把它升级成**「大纲」**：五页签一处收全，每一档用自己的字段。

### 新增 / 改变

- **「设定」→「大纲」**（顶栏那颗按钮与弹窗标题一起改）。里面五页签：
  | 页签 | 记什么 | 字段 |
  |---|---|---|
  | **人物** | 谁 | 名字 + 别称 + 设定（键值）+ 备注 |
  | **设定** | 地方 / 东西 / 一条规矩 | 名称 + 条目（键值）+ 内容说明（**不摆别称**） |
  | **事件** | 发生了什么 | 一句话 + 故事时间（第几天）+ 倒叙标记 |
  | **场景卡** | 这一场戏怎么搭 | 视角 / 目标 / 冲突 / 结果，**就地填** |
  | **伏笔** | 埋下的线头 | 埋在第几章 / 收了 / 不写了 |
- **事件页与「创作流 → 碎片池」是同一份数据**（同一个状态实例），所以两边永远一致；
  事件页把"全书的事件"按一处摆出来，还能直接跳回写它的那一章。
- **场景卡那页一眼看全书**：哪几张没填全排在前面，标着"还缺 N 格"；四格就地填、
  一键跳去那一场；也能**在你打开着的这一章下面新建一张**。
  （编辑器里那张四格小表单照旧保留——写的时候顺手填。）
- **体检那条问题点得动了**：核心在参数里带上卡的**类型**，所以"去大纲里改"会直接
  打开人物页或设定页，不必作者自己再找一遍。

### 顺手收掉的（同一处改动的连带）

- 页签本身就是筛子，原来那一排「全部 / 人物 / 设定」筛选项与类型徽标撤了
  （留着是两套筛法互相打架）；列表改成一行一张卡 + 一行计数。
- 每一页自己滚，弹窗高度封顶（不再把整页撑开）。

### 验收

- 前端：外壳 6 例（默认停在人物页 / 打开只读要读的那一页 / 人物与设定共用一份名单 /
  事件页读的是碎片池那份数据 / 伏笔页只读伏笔 / 换页与收起都放下表单）；
  实体那一份 7 例跟着改（筛选项撤了，筛选改由页签承担）。
- 核心：实体类问题现在带上卡的类型（规则测试跟着钉）。
- QA 门：静态全过（版本四处一致 0.65.0）+ Rust 553 项 + 前端 312 项 + 生产构建。

## 0.64.1 — 2026-09-16

> 真机反馈修一处**我自己漏的**：「设定」那个弹窗漏引了公共弹窗外壳的样式，
> 于是它根本没变成弹窗——掉到页面底下成了一坨裸文本（用户原话："太丑了，而且看不懂怎么用"）。

### 修复

- **「设定」弹窗找回它的壳**：`dialog.css`（遮罩、卡片、标题、按钮）漏引了，
  只有它自己的 `entity-dialog.css` 被加载——所以那些"弹窗零件"的样式全没生效。
  现在与书架、回收站那些弹窗一样：居中卡片 + 半透明遮罩 + 真按钮。
- **加一条守卫，让这一类漏引不再发生**：壳的边界测试里新增一条——
  凡是自己就是那个遮罩的组件（`class="… dialog"`），必须引 `dialog.css`；
  文件里那些 `dialog__box` 之类的**零件**不算（设置面板里的小页用它们，但自己不是遮罩）。
  现在扫到 12 个弹窗，漏一个就红。
- 顺手清掉界面文案里**会原样显示**的 markdown 强调（`**加粗**` 与反引号）：
  弹窗里是纯文本插值，屏幕上会真的出现一串星号。新增一条字典守卫钉住
  （已存的 5 条一并清掉）。

### 改好懂

- 顶栏那颗按钮叫「设定」，弹窗标题也统一成「设定」（页签里才是「人物与设定 / 伏笔」）。
- 两屏的开场话说清"**我该做什么、做完去哪儿看**"：
  记什么（名字 + 别称 + 设定）、记完不用管它（大纲体检会拿它比对、结果在「创作流」那一栏）。
- 空态不再只说"没记过"，而是指到具体那一步：点右上角「记一个新的」。
- 表单上加一行说明：只有名字必填、设定一行一项写成「发色 = 黑」；
  伏笔那屏也说清"不选章节也能记，只是判不了埋了多久"。

### 验收

- 壳边界测试 11 项（新增"弹窗必须引公共壳"1 项，扫 12 个弹窗）。
- 字典守卫新增 1 项（不许 markdown 标记），既有 5 条文案一并清掉。
- QA 门：静态全过（版本四处一致 0.64.1）+ Rust 全仓 + 前端 + 生产构建。

## 0.64.0 — 2026-09-16

> 大纲体检补齐**另外两条判得动的线**：**伏笔**（埋了太久没收）与**时间线**
> （后一章的事件，故事时间反而更早）。到这儿，"大纲冲突检测"这一版要的六条规则齐了；
> 与正文事实对不上仍然不做（那要实体抽取，硬猜出来的假警报比不报更糟）。

### 新增

- **伏笔账本**（顶栏「设定」→「伏笔」页签）：一条线头 = 一句话 + 埋在第几章 +
  状态（**埋着 / 收了 / 不写了**）+ 收在第几章 + 一句备注。它能跳到埋的那一章。
  - **「不写了」是正经结局**，不是失败——检测只念还埋着的（语气层的一贯立场）；
  - 收了 / 又埋着都能来回改（收错了、改主意了都不该被堵死）；
  - 收的时候默认把"收在哪一章"记成**你现在打开的这一章**（作者多半就站在收线那一章上）。
- **事件的故事时间**（碎片池里的事件：点「改这条」）：一句自由文本（`承平三年·春`，
  给人看）+ 一个排序值（"故事开始后第几天"，给排序用）+ 一个「倒叙」勾。
  - **填得少就报得少**：两边都填了排序值、且都不是倒叙，才判得动；
  - 倒叙是作者自己的写法（后写的章讲更早的事本来就对），勾上就不参与顺序检查；
  - 只有事件有这三栏——灵感 / 口述带上它会**当场被拒**，不静默丢掉作者写的东西。
- **两条新规则**（体检里报，与那四条同样只报告）：
  - **伏笔未回收**：埋着的那条，距全书末尾已经**超过 20 章**（阈值写在核心一处，说清理由）；
  - **时间线倒置**：按章的顺序扫，后一章的事件故事时间反而更早——一次说清它比谁早
    （带两边的正文与时间）。没记埋点 / 没记章锚点的**一律不猜**。
- **一行稳住一类静默故障**（这一版顺带修的）：跨界枚举的 JSON 形状与稳定码（`as_str()`）
  必须**一模一样**，新增守卫测试 `wire_codes.rs` 逐类跑一遍。它当场抓到几处曾经
  "不报错但对不上"的地方——设定卡的类型会序列化成 `Person`（界面上一句"人物"都认不出来、
  按类型筛选点不动）、问题卡的状态会变成 `Pending`。现在这些枚举**直接按 `as_str()` 序列化**
  （不再靠"把变体名改成蛇形"——`NoNumber` 的码是 `none`，靠改名本来就靠不住）。
- **开发档命令**：`foreshadow-new/list/update/move/delete`、`fragment-edit`（故事时间）。

### 设计取舍（写下来，免得下次重猜）

- **故事时间怎么表达**：作者不用算日号——**自由文本给眼睛、数字给排序**，两个都可选。
  只有"两边都有数字且都不是倒叙"才判，所以宁可少报也不误报。
- **伏笔另起一张表**：它有两个章节锚点与一条小生命周期，碎片统一表装不下这个形状。
- **锚点在核心先校验**：埋点 / 收点必须是这本书里还在的节点——外键也会拦，
  但它报的是"数据库约束失败"，**那句话不该让作者看见**。
- **与付费模块的边界**：更漂亮的地图 / 时间轴 / 伏笔账本界面归模块（模块只读核心数据）；
  核心这里只给**数据**与"你自己打开体检时看到的那份静态清单"——不主动提醒、不弹条、不催办。

### 验收

- 伏笔验收 7 例：记 / 三态互通与回头路 / 非法边当场拒且不写 / 空正文拒 /
  按状态筛而埋着的排前 / 软删行还在 / **脏锚点说人话**（不存在 / 已删 / 别的书）。
- 故事时间验收（并在碎片那一份里）：事件三样存得下读得回、灵感卡带它当场拒、
  正文与它一次给全、空正文与不存在的卡都拒。
- 规则验收 12 例（含新增 3 例）：伏笔只看 `planted` 且没记埋点的不猜 /
  时间倒置要两边都有数字且不是倒叙、顺序正常与相等都不报。
- 跨界枚举稳定码守卫 1 例（逐枚举跑，形状与码必须一致）。
- 命令行端到端 1 例：埋久了报 → 收了不报 → 倒置报 → 标成倒叙不报 →
  非法边与脏锚点都说人话。
- 前端 15 例：伏笔的状态称呼 / 筛选项 / 章的名分（查不到就不编号）/
  故事时间的文本↔数字 / 事件小标 / 只有事件有故事时间；「设定」外壳（打开才读、
  点哪条开哪页、收起时放下表单）；时间标签空着时用排序值顶一句。
- QA 门：静态全过（版本四处一致 0.64.0）+ Rust 552 项 + 前端 309 项 + 生产构建。

## 0.63.0 — 2026-09-16

> **大纲体检**：把你自己记下的设定（人物 / 设定卡）与场景卡四格对一遍，
> 指出**对不上的地方**——只看不说，改不改由你。
> 这是"大纲冲突检测"的第一刀：先建承载，再做判得动的规则（判不动的绝不硬猜）。

### 新增

- **设定卡（人物 / 设定）**：记下"谁 / 什么地方 / 什么东西"——名字、别称（字、号、绰号）、
  **键值设定**（发色 = 黑、佩剑 = 青霜……键名由你自己定）、一句备注。
  入口在顶栏「设定」，跟书架、回收站一样是一个面板。
  - 跟人不跟书这点同理：**设定卡按书分**（一本书一套名单）；
  - 名字是身份（空着不让存）；别称与整条空的设定在入口就丢掉，**只有键没值的那条留着**
    （那是"还没填"，不是手滑）；删是软删（库里还留着，只是不列出来）。
- **场景卡四格**：打开一张场景卡（章下面那种），正文上方多一小张表单——
  **视角 / 目标 / 冲突 / 结果**。它是低频手填字段，与"章纲一句话"并排：单独存、
  不参与落稿的防抖与指纹校验，**正文一个字都不动**。
- **大纲体检**（在「创作流」那一栏里）：四条规则，全部只依赖已经记下来的结构化数据——
  | 报什么 | 判据 |
  |---|---|
  | 称呼撞车 | 两张卡共用一个名字或别称（喊一声两个人回头） |
  | 同一张卡里重复登记 | 正名与别称是同一个字，或别称写了两遍 |
  | 属性自相矛盾 | 同一张卡里同一个键给了两个不同的值（同一个值写两遍不算） |
  | 场景卡缺项 | 四格缺了哪几格（**只问填没填，不问写得对不对**） |
  - 每条能**点开定位**：设定卡那条去设定面板，场景卡那条直接跳到那一场；
  - 「这一处我知道了」能**记住**（忽略的是"这条问题的身份"，不是这一次的结果——
    你填了一格，原来那条不会又冒出来；同一张卡的另一个属性冲突照样会报）；
    忽略了能列出来、能捡回来、能「全部重新看」。
- **开发档命令**：`entity-new / entity-list / entity-update / entity-delete`、
  `scene-field`、`outline-scan / outline-dismiss / outline-undismiss / outline-clear-dismissed`。

### 设计取舍（写下来，免得下次重猜）

- **先建承载，再做检测**：这一版开工前的家底核对发现，「大纲冲突检测」列的五项里
  **只有场景卡有承载**（还只是"名字 + 正文"的节点），人物卡 / 伏笔 / 事件的故事时间一概没有。
  硬写检测器就是**架空**。所以这一刀只做**地基的一半**：设定卡 + 场景卡四格，
  检三条规则；**伏笔（埋 / 收）与事件的故事时间留第二刀**。
- **与正文对不上暂不判**：那要实体抽取（人物卡说左手剑、正文写右手），
  硬做只会变成一堆猜出来的问题——**假警报比不报更糟**（看两次就再也不看了）。
- **设定卡另起一张表，不塞进碎片统一表**：碎片是**素材**（会被"用掉"、会随时间冷却降权），
  设定卡是**实体**（有身份、有键值属性，不该因为"用过一次"就降权）。
  混在一张表里，素材那套引力语义就会污染设定（同表就同元数据的理由不适用于此）。
- **场景卡四格不进 `nodes`**：目录树那张表是所有节点共用的，读树时不该拖着只有场景卡才有的列；
  卫星表还顺手给了个好语义——**没有那一行 = 这张卡还没填过**。
- **忽略认的是"身份"，不是"状态"**：指纹 = 规则 + 位置 + 主体。

### 验收

- 设定卡验收 7 例：建 / 整卡覆盖 / 名字空拒且不落半行 / 空项丢而"只有键"的留 /
  按书按类型列 / 软删行还在 / 坏 JSON 如实报错（不静默当空）。
- 场景卡四格验收 4 例：不是场景卡如实拒（与"节点不在"分开报）/ 没有那一行=四格全空 /
  存了读回来一致且整行覆盖 / 按书列出时四格跟着、顺序按树。
- 规则验收 9 例（**纯逻辑，不碰库**）：撞车给两边锚点与名字 / 同卡重复单算一条 /
  两种说法才算冲突而重复不算、空值不算说法 / 缺哪几格说清楚、填全就闭嘴 /
  两次扫描结果一模一样且"填了一格身份不变" / 库那一层的软删与忽略幂等、坏记录当没忽略。
- 命令行 1 例端到端：记卡 → 体检三类都在 → 忽略幂等与回头路 → 软删那张卡后撞车消失 →
  场景卡填全后缺项消失 → 章没有四格如实拒。
- 前端 16 例：别称与设定的解析与回写（逗号/顿号/空格/换行、等号/中文冒号）、
  表单 ↔ 卡片、筛选项与列表那一行；发现的句子渲染、缺格称呼、忽略分堆、锚点解析；
  四格状态机（不是场景卡不发写请求 / 回执用库里那份 / 存失败只报一句）。
- QA 门：静态全过（版本四处一致 0.63.0）+ Rust 536 项 + 前端 293 项 + 生产构建。

## 0.62.1 — 2026-09-16

> 第二栏**记住上次**：你上次在「叩问」还是「创作流」，下次打开就在那一块。
> 纯偏好，跟人不跟书——换一块只是换个地方看，稿子一个字节都不动。

### 新增

- **第二栏记住上次露哪一块**：换页签时把选择写进偏好（全局那一层，沿用「外观与写作行为」
  那套稀疏设置，**不加迁移**）；下次打开先露它。没设过 / 读不出来时露**叩问**
  （作者最常待的地方），不猜"上次大概是创作流"。
- 取值只有一处知道（`SideTab`：`flow` / `creator`）：写进来不认识的值当场拒
  （`value.unknown_aside_tab`），库里已有的坏码当没设过——与口径 / 引号 / 语气同一条规矩。

### 验收

- 核心偏好验收 12 例（+1）：默认叩问、换过就记住、`"auto"` = 清掉这一层、
  认不出的码当场拒且不落库、库里坏码当没设过。
- 前端验收：`aside.test.ts` 4 例（偏好还没读回来时用默认 / 读回来就跟着走 /
  认不出的值不带歪界面 / 换一块先动界面再写回偏好）+ 偏好接线 1 例（只写全局那一层）。
- QA 门：静态全过（版本四处一致 0.62.1）+ Rust 全仓 + 前端 + 生产构建。

## 0.62.0 — 2026-09-16

> 右侧那一栏不再只有叩问：新增**创作流**——你自己记下的灵感、事件、口述段落都住在
> 那一块里。它跟问题卡、答案是**同一张表**里的不同种类（从 v1 起就留好的位置），
> 所以"怎么记下的 / 多新鲜 / 用没用过 / 关联着哪一章"这套元数据天生就有。
> 研墨只替你归位，**不替你写**。

### 新增

- **创作流面板**（右侧那一栏顶上的页签：叩问 / 创作流）：
  - **记一条**：写一句、选「灵感」或「事件」，按下去就记下了——不打断手上这件事。
    开着某一章时，它会顺手把"是在这一章写的"记成锚点（`chapter:<章 id>`），
    列表里那条会带一个「本章」的小标。
  - **碎片池**：最近记下的在最上面，每条带着种类、关联、来源（口述 / 口述后改过）
    与「今天 / 昨天 / N 天前」；筛选项只摆**真有的那几档**，数字由核心按同一种类算
    （不是数当前这一屏，所以"全部"与各档永远对得上）。
  - **删是软删**：删掉一条，旁边当场给「撤销」——捞回就是把时间戳抹掉，不是把行找回来。
- **碎片统一存储模型补齐**：`frag_kind` 这个闭集从"问题 / 答案 / 灵感"扩成
  **问题 / 答案 / 灵感 / 事件 / 口述段落**，一次写到一处（`FragmentKind`）——
  问题卡、答案、灵感卡原来的种类码都改成由它派生，不再各写一个字符串。
  口述那一档**存储先到位**（写入者由口述那条链路带，这一版只有键盘能记）。
- **命令行（开发档）**：`fragment-add` / `fragment-board` / `fragment-delete` /
  `fragment-restore`——界面上"删了能捞回""坏种类会被拒"这类事得能反复跑。

### 设计取舍（写下来，免得下次重猜）

- **面板只管"作者自己记的"**：问题卡与答案是叩问那条线产的（它们有自己的状态机、
  处置四件套与回头路），从创作流面板凭空建只会长出一张没人处置得了的孤儿卡——
  核心**当场拒**（`fragment.kind_not_jotted`），不是靠界面自觉。
- **不另起第二张表**：同表就同元数据，将来按"优先用新鲜碎片"爆灵感要用的四样
  （新鲜度 / 用过几次 / 关联 / 重要度）不必再补列。
- **口述不是键盘能"记"出来的**：所以"记成"里只有灵感与事件两档；
  口述那一种照样能读、能显示、能删（界面不认得的值会照原样露出来，不静默显示成别的）。
- **锚点由界面给、核心只存不懂它**：`chapter:12` 这个写法与叩问那边一致，
  将来按章 / 卷把它找回来也认这一条。

### 验收

- 存储验收 11 例（`crates/yanmo-core/tests/fragment.rs`）：可记的每一种都真的到得了
  （没有"定义了却产不出来"的死值）；新的在前、按种类计数与列表同一口径；
  问题与答案从这儿建**当场被拒且一个字节不写**；空白正文与认不出的输入方式都拒；
  锚点与来源存得下读得回（锚点那一列被手改成坏 JSON 时按空数组读）；
  删是软删（行还在）、捞回真的回来、重复捞回不报错也不留多余痕；
  删两次 / 删不存在的都如实报「不存在」；跨书不混；往回收站里的书写会被拒；
  库里认不出的种类如实报错不猜。
- 前端单测 7 例：筛选项（全部 + 只摆真有的档 + 不把问题/答案算进来）、筛选、
  种类称呼走字典（字典里没有的照原样露）、来源小标只标非手打、是不是"这一章写的"。
- 命令行 4 条：`fragment-add` / `fragment-board` / `fragment-delete` / `fragment-restore`。
- 守卫：声明的命令 ↔ `main.rs` 注册 ↔ 前端网关白名单一一对应（漏一个就红）；
  错误码 ↔ 界面字典穷举对表（新增 4 个码都进了字典）。
- QA 门：静态全过（版本四处一致 0.62.0）+ Rust 全仓 + 前端 + 生产构建。

## 0.61.0 — 2026-09-16

> 叩问学会**怎么问**，也学会**什么时候别问**：语气三档（温柔 / 中性 / 直接），
> 以及「主动开口」的护栏——一天最多几次、两次之间隔多久，全由作者定；
> 面板（你自己打开的那种）**永远不受配额管**。

### 新增

- **语气三档**：温柔（默认）/ 中性 / 直接。每条问题在字典里三版，说的是同一件事
  （槽位一模一样，有守卫钉住），只是换说法。**「中性」就是关**——不加语气，
  与最早的写法一样；不必再记一个总开关。语气只改"怎么问"，不说教、不评价剧情。
- **主动问一句（推）的护栏**：开着写作时，软件最多少量地凑上来问一句——
  默认**一天 3 次**、两次之间**至少隔 60 分钟**，都能改（**设成 0 就是永远别问**）。
  三个时机：**开新章**（到了一章还没写字的地方）、**卡住一会儿**（连续 15 分钟没有动静，
  而这一章已经写了点东西）、**刚写完一章**（切走时那一章有正文）。
  - 提示出现的地方就是**那一条安静小条**（跟备份提醒共用一个出口，一次只显示一条）：
    「想问你一句：……」+「答一句」（右侧面板直接把这张卡打开）/「先不用」。
  - **显示即算问过**：新颖度从那一刻就消耗掉了——那是实话，你看见过。
  - **配额只管推**：用完了，右侧叩问栏照样能开、问题照样能翻——那是你自己要看的不限。
  - **错过一次推是静默的**：没得问、次数用完、冷却没到，都不报错、不打扰写作。

### 验收

- 存储验收 4 例：配额与冷却真的挡得住（含跨天重置）、0 次 = 一次都不问、
  推出去的卡走到「已问」且留痕写着是哪条时机（`push:new_chapter` 这类）、
  池子空了就说"没得问"而不是硬造一条；推走的那条不在池子里，面板照常。
- 偏好验收 1 例：默认（温柔 / 3 次 / 60 分钟）、**0 是正经取值**（不打扰，不是"清掉"）、
  负数才是清掉这一层、认不出的语气码当场拒。
- 命令行 1 条：两次问得出去、第三次被配额挡住、跨天恢复、0 次不打扰。
- 前端：语气三版挑得对 + 认不出的语气码当「中性」+ 本地那一天怎么算，单测 3 例；
  模板↔字典守卫扩成**三档齐全、占位符一致**（少写一版、或换个问法改动了槽位，测试都红）。
- QA 门：静态全过（版本四处一致 0.61.0）+ Rust 492 项 + 前端 265 项 + 生产构建，全绿。

## 0.60.0 — 2026-09-16

> 答案有了**三个去处**：正文段落、**章纲**（这一章的那句话）、**场景卡**（这一章下面新建一张）。
> 开篇那两问（谁在看 / 第一场戏在哪儿）从此默认落章纲——它们答的本来就是这一章的纲。

### 新增

- **落点三档**：作答时（或一轮问完在托盘里）每条答案自己选落到哪儿——
  **正文段落**（默认）/ **章纲** / **场景卡**；场景卡还能当场起个名字（留空也行，回头在树上改）。
- **默认落点按要素类型分**：问的是"章纲那一类"（这一章发生了什么 / 谁在看 / 第一场戏在哪儿）
  就默认落**章纲**；其余默认落正文。这只是默认——逐条能改（不设任何"只能"）。
- **章纲怎么合**：那一句话可能已经有作者**手写**的内容，所以是**合并**而不是覆盖——
  原有内容留着，新答案接在后面，中间用「；」（它本身是单行输入框，投稿包大纲也把它拼成一行）。
  同一句落两回不会在章纲里出现两遍。
- **落地之后界面更新的是库里的真值**：核心把"章纲最后成了什么""新建了哪些场景卡"回给界面——
  "一句话"那一栏换成核心给的那一行，目录树重拉一次（新场景卡在树上看得见）。界面不自己猜。
- 命令行（开发构建）：`question-land --target body|outline|scene [--title …]`；
  `question-round` 的 JSON 里每条带 `target` / `title`。

### 整理

- 核心把"作答"（把答案记下来）与"落章"（送到哪儿去）拆成两个文件：
  `question_answer.rs` 174 行 + `question_land.rs` 311 行——两件事的变化理由不一样
  （一个跟着答案池长，一个跟着落点长）。两条落法（答一条落一条 / 一轮一次落）共用同一个实现。

### 验收

- 存储验收 3 例：三档各落各的（章纲合并、场景卡真的建成了节点且挂在那一章下面、正文核心一字不写）、
  同一句落两回章纲不重复、认不出的落点整轮拒且章纲与场景卡一个都不动。
- 命令行 1 条：三档从命令行走一遍（含"这一章正文一个字节没动"与坏落点被拒）。
- 前端：默认落点规则 + 落点称呼 + "只有落正文的那几条进正文"单测 3 例。
- QA 门：静态全过（版本四处一致 0.60.0）+ Rust 482 项 + 前端 262 项 + 生产构建，全绿。

## 0.59.0 — 2026-09-16

> 补上**开篇两问**：一本书刚开、开头还没落笔的时候，「这一章从哪儿开始」太空——
> 真正要定的是这两件：**谁在看**（视角）与**第一场戏在哪儿**。

### 新增

- **开篇两问**（只在开头还没落笔时出现）：`{第一章}是开头——故事从谁的眼里看出去？`
  / `{第一章}这第一场戏，放在哪儿、什么时候？`
  为什么只在这一刻问：视角与第一场戏是**落笔之前**要定的事；写下第一句之后这两问就不再出现
  （再问就成了马后炮）。锚点都落在**阅读顺序里第一个承载正文的节点**上（卷不算）。
- **模板对表守卫**（新增一条自动检查）：核心每加一条问题模板，界面字典里必须有一句对得上，
  而且占位符要正好填得上模板声明的槽位。少一句的后果不是"少一句文案"——是机制新问了一句话，
  而作者在面板上看到 `question.template.plan.opening_pov`，一个字都读不到。

### 诚实交代

- 这两问的回答**先落进正文**（与别的答案同一条路），不是落到"章的一句话"上：
  落点身份那一维还没做（章纲 / 场景卡下次再说）。作者可以把它们当开篇备忘，写完真稿再改。
- **不为"一堆空章"另外造机制**：真实的写法是写完一章再建下一章（不会先囤十来章空着），
  一屏只看得到一条同类问题已经够用——所以没做「这一卷还有 N 章没动笔」这类问法。

### 验收

- 生成规则 2 例：开头空着时三问都在（从哪儿开始 / 谁在看 / 第一场戏在哪儿），且都锚在第一章；
  开头写过了，开篇两问不再出现。
- 命令行 1 条改成新口径：草稿三条都对得上，且"问过的那一条不再生成"。
- 界面：模板 ↔ 字典对表 1 例（新增守卫：缺一句、或占位符与槽位对不上就红）。
- QA 门：静态全过（版本四处一致 0.59.0）+ Rust 477 项 + 前端 259 项 + 生产构建，全绿。

## 0.58.0 — 2026-09-16

> 把「为什么先问它」从一串小数改成**人话**，并治一件事：书里状态重复时（比如十来个空章），
> 候选会变成一屏只差章名的同一句话——看着像机制只会问这一句。

### 改进

- **「为什么先问它」讲人话**：原来那一栏是 `时机紧迫度 0.50 / 要素重要度 0.50 / 新颖度 1.00 /
  引力 0.25`——数字对作者没有意义。现在每一行说清"这意味着什么"：
  「时机：正是该问的时候」「这件事的分量：一般要紧」「问过几回：还没问过」；
  真在起作用的乘数也用话说（「你说过这类问题好，往上抬一点」「你延后过它，先压一压」），
  不抬高也不压低的那几项干脆不提。**面板上不再出现裸数字**（档位阈值只有一处，有测试钉住边界）。
- **同类不再一屏扎堆**：一个状态在书里重复出现时（十来章都空着，"这一章从哪儿开始"就会产出
  十来条只差章名的问题），一屏里同一类**先只摆一条**，其余排到别的类后面——**一条都不丢**，
  答完一条下一条就浮上来。同时面板会如实说「池子里还有 9 条在排着」，免得作者以为能问的就这一条。

### 验收

- 选题验收 1 例：五条同类 + 两条别类，取 3 条时同类只出现 1 条、别类不被挤掉；
  取 10 条时七条都在（一条都不丢），池子总数不受一屏上限影响。
- 前端：引力拆解单测改成"**一个裸数字都不许出现**" + 档位边界 1 例。
- QA 门：静态全过（版本四处一致 0.58.0）+ Rust 475 项 + 前端 259 项 + 生产构建，全绿。

## 0.57.0 — 2026-09-16

> 叩问的第二种问法：**先问后排版**——开新章之前把这一章一次问完，答案先在旁边攒着，
> 问够了再一起落进正文。上一版的「跟着这一章走」是边写边问，这一版是**先想清楚再动笔**。

### 新增

- **先问后排版**（面板顶上第二个开关）：同样跟着当前章出题、答完自动出下一张，但答案
  **先攒在托盘里**——能排队（↑↓ 调顺序）、能删掉某一段、能顺手改字。问够了点「一轮问够了」，
  面板把"将要把这 N 段依次落进当前章的末尾 / 光标处"摆出来给你看一眼，**你点了才动稿**。
  - 落到哪儿仍由作者定：**光标处**或**本章末尾**（与上一版同一套落点）。
  - 落的时候**一次插进编辑会话**——所以它就是一次正常编辑：自动落盘、字数、码字账本、
    版本快照全都照常走同一条路。
  - 在托盘里改过的字会**回写答案池**，并留下"改了"这一笔（改前改后各有几个字）——
    答完发现串行了，不必重新答一遍。
  - 一轮里只要有一条不对（没答过、不是这本书的卡、这一段放不了），**整轮都不写**：
    半轮落下去比整轮不落更难查。

### 整理

- 面板的两半落法各成一件事：`use-landing.ts`（答一条落一条）与 `use-round.ts`（一轮一次落），
  面板那一层只管编排三种走法；三种走法共用一个开关位（同时只有一种）。

### 验收

- 存储验收 2 例：一轮按给的顺序落 + 改过的字回写并留痕（`create → amend → land`）、正文一个
  字节不动；坏输入（空轮 / 空答案 / 没答过 / 别的书 / 非正文节点）**整轮拒绝**且一条都不写。
- 命令行 1 条：一轮两条（倒序 + 改字）→ 状态与文字都对、坏输入被拒、稿子指纹不变。
- 前端：托盘的纯逻辑单测 4 例（同一张卡不重复攒、边界挪不动、删除与改字、拼段丢掉空条目）。
- QA 门：静态全过（版本四处一致 0.57.0）+ Rust 474 项 + 前端 258 项 + 生产构建，全绿。

## 0.56.0 — 2026-09-15

> 叩问的两种问法里先落一种：**跟着这一章走**——边想边写的那种，写第几章就先问第几章的事，
> 答案可以直接落进这一章的正文。落进正文的永远是**作者自己写的字**：机制一个字都不生成。

### 新增

- **跟着这一章走**（面板顶上那个开关）：打开后，与当前章有关的问题排到最前（其余照旧按引力
  跟在后面，一条都不少），答完自动出下一张，还有「跳过这张」。作者换到哪一章，那一章的问题
  就自然浮上来——**不猜"下一章是哪一章"**：机制不知道你接下来写哪儿，只知道你现在写哪儿。
- **答案落进正文**：作答时可以勾「记下之后落进本章正文」，落点自己选——**光标处**或**本章末尾**。
  没在正文里点过光标时一律落在章末（照字面"落在光标处"会把答案甩到全章最前面）。答完当时没落，
  回执里还能补落一次。
  - 落章分两半走：**界面把字插进编辑会话**——于是它就是一次正常编辑，自动落盘、字数、码字账本、
    版本快照全都照常；**核心只记下"这一条用掉了、落到哪一章"**（留痕一条不多不少）。
  - 落下去的那一段就是普通正文：看不顺眼直接改，或撤销。留痕记的是"落过"，不是"正文里现在还
    有"——这两件事本来就不同。

### 整理

- 叩问面板的**状态与命令编排**整个搬进 `flow-panel.ts`：`.vue` 那一份只管"长什么样"，
  一个 API 都不直接调。面板这几版一直在长，再混在一起就要变成谁都改不动的那种文件了。
- 「正在问」那一屏单独成件（`AskingPane`）。

### 验收

- 存储验收 4 例：落章只留痕不动稿（含 op-log 里记下落点）、反复落每次都有据可查、
  落点不是正文节点 / 不是这本书的章 / 节点已经不在了分别被拒且一个字节不写、没答案就落要如实报。
- 选题验收 1 例：跟着这一章走时这一章的问题排最前、别的一条不少；没有专属问题时顺序原样不动。
- 命令行 1 条：落章后答案状态可查、稿子指纹不变、坏落点被拒、`--node` 的出题顺序对。
- 前端：落点称呼 + "这条是不是问这一章的"（含 `chapter:70` 不许被 `chapter:7` 认下来）单测 2 例。
- QA 门：静态全过（版本四处一致 0.56.0）+ Rust 471 项 + 前端 254 项 + 生产构建，全绿。

## 0.55.0 — 2026-09-15

> 叩问补上**作答**：前面几版把"问什么、怎么处置"做完了，但答案没处放——这一版作者终于能把
> 心里那一句写下来。答案落进**答案池**（与问题卡、灵感速记同一张碎片表），并带上是**怎么打出来的**
> （键盘／口述／混用）。作答**一个字都不写正文**：答案要不要落到章里，是下一步两条落点模式的事。

### 新增

- **作答**：面板「正在问这一张」里多了一个「作答」。写下的答案进答案池，问题卡走到「已答」终态
  （不再回队列）。**没点开就直接答也算答**——会先补一条「问出」，新颖度照常消耗，
  不留"从没问过却已答"这种把账做平的假记录。
- **答案带输入方式**：答案文本与"怎么打出来的"**解耦**——`source` 记「键盘打的／口述转的／
  口述加手改的」三档之一，它是创作留痕的原始素材，以后按它算口径才算得准。
  认不出的取值**当场拒绝**（留痕里的一列写歪了，比少一条记录坏得多），不用一个"像那么回事"的值兜底。
  这一版只有键盘这一条通道：口述那条链路接上时，写入者换个取值，命令与存储的形状都不用动。
- 命令行（只在开发构建）补 `question-answer` / `question-answers`，与界面走同一条正路：
  一样的校验、一样的留痕、一样的状态机。

### 整理

- 叩问面板把**回头路**那半边拆成单独一件：冷却库（捞回）与两张已静音清单（解除）。
  它与"挑问题、处置、作答"的变化理由不一样——那边在长交互，这边只是在长"撤销入口"；
  拆开之后面板本体回到四百行以下（那一条是给"上帝文件"设的线，到了就得看一眼）。
- 开发档命令行里叩问那一族的**重复分支**清掉了：同一族命令在两个文件里各存了一份，
  靠前面那份抢先命中，后面那份其实是死代码（两处逐字节相同，只有后来新加的一条命令落在后面那份里，
  于是它成了唯一走得到的）。现在这一族只有一个真相源，新加命令不会再漏进另一个文件。
- 作答的回执**从核心读回来**再显示（而不是把界面刚传进去的那份原样回显）：界面上写的就是
  库里真有的那一份——修剪后的原文、核心认下的输入方式。自己回显自己等于没核对。

### 验收

- 存储验收 9 例：答案与状态同一个事务、未问先答补一条「问出」、输入方式三档都收得下、
  认不出的当场拒且一个字节不写、空答案不入库、已答是终态（再答被状态机拒且原答案不被改）、
  舍弃过的不可答、答案与灵感同表却互不串味、没答如实报「还没有答案」。
- 命令行 1 条：未问先答 → 读回答案（原文／输入方式／溯源）→ 事件链 `create / ask / answer`
  → 再答被拒 → 坏输入方式被拒 → **作答前后正文指纹一致**。
- 前端：输入方式称呼单测补 1 例（三档人话 + 认不出的原样露出来，不静默吞掉）。
- QA 门：静态全过（版本四处一致 0.55.0）+ Rust 466 项 + 前端 252 项 + 生产构建，全绿。

## 0.54.1 — 2026-09-15

> 补两处「出不去」：上一版面板里有两个**只进不出的开关**——按下去了，界面上没地方松开。

### 修复

- **「这类别再问」现在能撤销了**。上一版的解除入口只在核心与命令行里，界面没接上。
  现在面板多了一块「已静音的类别」，点 ✕ 就让这一类回来（解除之后，这一类的问题
  重新回到候选池）。
- **延后现在能取消了**。上一版面板只把"在等条件的"列出来，没有回头路——「我自己想起来再问」
  那一条尤其明显：等作者真想起来，却只能在命令行里捞。现在每一条旁边有「别等了」，
  点一下当场回候选池，那条等待记录也会标掉（作者当时写的那句话留着，翻历史还看得到）。
- 命令行也补了 `question-undefer`，与界面走同一条正路。

### 验收

- 存储验收 ＋1 例（两处撤销的回头路：解除类别之后同类问题回来、取消延后之后当场回池且记录标掉、
  不在延后态的卡去取消要被状态机拒掉）＋命令行断言补两条。
- QA 门：静态全过（版本四处一致 0.54.1）+ Rust 455 项 + 前端 251 项 + 生产构建，全绿。

## 0.54.0 — 2026-09-15

> 叩问的**处置**齐了，并第一次有了能用的界面：右侧那一栏从"占位"变成真面板——
> 机制挑出来的问题摆到面前，作者只做处置（说好／延后／舍弃／静音／记灵感），**它一个字都不写**。
> 顺带抓掉两个真问题：派生链深在"记灵感"这一环会断（防套娃形同虚设）、命令行无值开关静默失效。

### 新增

- **叩问面板**（原来那一栏的占位换成真面板）：候选问题按引力排序，**点开一张才算"问出"**
  （那一刻才消耗新颖度；只浏览不算已问）。点开后是处置四件套——说「这个问题好」／
  延后（五档「什么时候再问我」，还能自己写一句）／舍弃（进冷却库）／静音（这一类或这个来源）／
  记灵感。面板顶上写着这条产品的红线：**只问不写**。这一版**没有"作答"入口**——答案输入是
  下一步的事，宁可没有也不放按不动的按钮。
- **冷却库**：舍弃的卡进这里（最近放下的在前），随时能捞回；同时它作为负样本教系统少问这类。
  「两档冷却」不另造机制：**短冷却＝延后（带条件）**、**长冷却＝舍弃**。
- **按来源静音**：某个模块太吵时**只让它闭嘴**，不是把整个叩问关掉；静音可解除。
- **记灵感（派生 + 溯源）**：被一个问题勾起念头时一句话记下来，落成一张带溯源的灵感卡——
  它带着溯源指回问题卡，**问题的状态一个字节都不动**（记灵感与处置正交、可叠加）。
- **壳侧时机**：到条件的延后会在**打开作品／打开一章／建完一章**这三个低频时机顺手放回候选池；
  面板自己也每几分钟扫一次并刷新（绝不挂在击键那条路上）。
- 命令行（只在开发构建）补上处置四件套的驱动口：冷却库／捞回／按来源静音／记灵感／看灵感。

### 修复

- **派生链深在灵感卡这一环会断**：链深只沿"问题卡"这一类的父级往回走，而
  「问题 → 灵感 → 由该灵感生成的问题」这条链中间是灵感卡——于是防自激的深度上限形同虚设。
  现在沿任意碎片的溯源链走，两跳到此为止（作者手动再问不受限）。
- **命令行无值开关静默失效**：`--off`、`--auto-derived` 这类开关走的是"取值型"选项的读取路径，
  而那条路会把空串当成"没给"——于是开关永远读不到，还悄没声地按默认走。开关改走"只看给没给"。

### 验收

- 存储验收 4 例（冷却库与捞回、按来源静音与坏记录、记灵感不动状态、**派生链穿过灵感卡的回归**）
  ＋命令行 1 条（舍弃 → 捞回 → 静音 → 记灵感全链，含无值开关）。
- 前端：面板的纯逻辑（句子渲染、引力拆解、等多久、来源称呼）单测 5 例。
- QA 门：静态全过（版本四处一致 0.54.0）+ Rust 454 项 + 前端 250 项 + 生产构建，全绿。

### 一处诚实交代

设计里"记灵感支持口述一句"要等语音那条链路（外部引擎）；这一版只有打字。

## 0.53.0 — 2026-09-15

> 把「延后」做成**带条件的延后**：不是"过会儿再问"，而是说清"什么时候再问我"——
> 到点了、写到某一章／某一卷了、或者"我自己想起来再问"。条件满足才回候选池（不硬插队）；
> 同一问题延后多了会自动降权，但**不会消失**。

### 新增

- **延后条件**（核心零文案）：三类能真判的条件——`time`（到某个时刻）、
  `written`（锚点子树里出现正文："这一章写完了"与"进到某一卷了"是同一条判据，
  差别只在锚点是章还是卷）、`manual`（只有作者，不自动重出）。条件的字段**私有**、
  只能经三个构造器建出来——"写到某处却没锚点"这类半截条件**根本构造不出来**。
- **五档预置**（给界面"选一个"）：一天后 / 三天后 / 一周后 / 写完这一章再问 /
  我自己想起来再问。核心只给**键**，话在界面字典里；「写完这一章再问」自己从卡上的
  章节锚点找出"哪一章"，作者不必再被问一遍。
- **重出入口**：条件满足的延后回「待问」+ 标掉那条队列记录 + 留一条事件
  （`trigger` 里带上**是哪条条件**到了）。它由壳在合适的时机显式喊一声（开新章 / 保存 / 定时）——
  **选题本身仍然只读**：翻看问题不该顺手改库。
- **防死循环**：同一问题延后到第三次起引力对折、每多一次再对折，下限 0.1——**不清零**：
  延后过的卡排得后面一点，但作者主动翻照样看得见、答得了。
- **锚点被删 = 翻篇**：那一章／那一卷已经不在了，条件当作满足——让删掉的东西永远堵着
  一条问题，比"多问一次"更糟。库被人手改坏导致条件字段缺失时同样按翻篇处理：
  **宁可多问一次，不留死路**。
- **结构迁移 v10**：新增延后队列表（一次延后一行：条件是什么、作者当时填了什么话、
  什么时候重出的，全留着）。

### 验收

- 存储验收 8 例：延后离开候选池、时间没到不回来／到点回来并留痕、写到锚点（章与卷各一条）
  才回来、`manual` 永不自动回来、锚点被删翻篇、过去时刻与缺锚点当场报错、
  累计延后降权但不清零、队列与"作者那句话"读得回来、迁移重跑不丢队列。
- 命令行（只在开发构建）新增三条驱动命令（带条件延后 / 到条件重出 / 看队列），
  配一条命令行验收把整条链走一遍。
- QA 门：静态全过（版本四处一致 0.53.0）+ Rust 449 项 + 前端 245 项 + 生产构建，全绿。

## 0.52.0 — 2026-09-15

> 叩问又往前走一步：问题不再靠人硬想——**模板池**照着书里已有的东西生成候选，
> **加权选题**决定先问哪个，「不是永远那几个问题」由**新颖度冷却**天然实现；
> 再加上一层**偏好学习**（回答 / 说好 / 舍弃 / 静音 → 同类模板的权重）与**防自激护栏**。
> 这一版仍然没有界面：它先把机制与证据做到可机械验收，界面按计划随后落地。

### 新增

- **问题模板池**（核心零文案）：模板只给键与槽位名，句子在界面字典里按语言渲染；
  起步池五条，只用**已经有的数据**（章节树、每章一句话、字数）就能问出来——
  还没动笔的章 / 写完却没留一句话的章 / 连续几章字数雷同 / 某一章明显过长 / 刚写完那一章的承接。
- **候选生成 + 同锚点去重**：同一件事（同模板 + 同锚点）问过就不再生成；锚点随卡存下来，
  冷却库里的卡可以捞回，但不会在下次生成时又冒出一张重复的。
- **加权选题引擎**：`引力 = 时机紧迫度 × 要素重要度 × 新颖度 × 派生折扣 × 模板权重`，
  并给出**逐项拆解**（将来界面能回答"为什么先问这个"）；排序稳定（同分按卡号）。
  选题**只读**：作者主动翻看不受任何配额限制，看多少都不算"已问"、不消耗新颖度。
- **新颖度冷却**：问出时记下**次数与时刻**——问过越多次越沉、时间过去慢慢回升，
  但从没问过的永远最高；按"问过几次 + 重要度"分档决定冷却期（短期 2 天 / 中期 21 天 /
  长期不自动回池子）。这条曲线的数学与档位判定取自一处 MIT 许可的复用内核
  （只取纯逻辑机制、接口对齐、语义自己长），并接上它的**跨语言测试向量**逐例核对（1e-9）：
  "复制得对不对"不靠读代码印象。许可方向是宽松 → 严格，允许。
- **偏好学习闭环**：作答＝中性、说好＝加权、舍弃＝降权、静音＝**该类整体停用**；
  信号由动作码**自动折算**（处置即教学，界面不可能漏喂），"说好"另给一个**不动状态**的入口
  （评价与处置正交、可叠加）。静音**可撤销**：撤销后权重回到中性、历史计数留着。
- **防自激护栏**：系统自动派生的问题引力打对折（只影响排序，不禁用），
  自动派生链深最多两层——**作者手动顺着灵感再问不受此限**。
- **结构迁移 v9**：新增模板权重表，并给碎片表补「上次问出的时刻」与「是不是自动派生」两列；
  只增不改、可重跑。

### 验收

- 生成规则验收 5 例（纯逻辑）：什么时候该问、槽位填没填满、雷同与过长各自触发。
- 存储验收 9 例：草稿照书生成与去重、选题只读且"从没问过的最前 / 问过越久越靠前"、
  问出记时刻并出池、说好不动状态、处置自动教学、静音整类消失且可撤销、
  派生折扣与链深上限（手动不受限）、迁移重跑不丢学习。
- 对齐验收 2 例：18 条跨语言向量逐例命中（1e-9）；默认参数与来源一致。
- 命令行（只在开发构建）新增五条驱动命令（生成草稿 / 选题 / 看学习账本 / 说好 / 解除静音），
  配一条命令行验收把「生成 → 建卡 → 选题 → 说好 → 静音 → 解除」走一遍。
- QA 门：静态全过（版本四处一致 0.52.0）+ Rust 433 项 + 前端 245 项 + 生产构建，全绿。

## 0.51.0 — 2026-09-15

> 把「叩问」的地基铺好：问题卡有自己的模型与**完整生命周期状态机**，卡住进与事件 / 灵感速记 /
> 口述段落**同一张碎片表**（不是第二套存储）。这一版还没有界面——它先把「六个态都到得了、
> 都出得去、每一步都留得下证据」做成**可机械验收**的东西。

### 新增

- **问题卡模型**：卡住进碎片统一表（`frag_kind = question`），复用碎片那一套元数据
  （来源 / 创建时间 / 被问过几次 / 关联 / 重要度 / 派生来源），另加一个「这条卡是哪条问题模板
  问出来的」键，给后续的偏好学习留钩子。新建时三重校验：正文不许空、重要度要落在 0~1、
  派生来源必须是**同一本书**里的一张卡（跨书溯源等于把两本书的因果混在一起）。
- **六态生命周期**：待问 → 已问 → 已答 ／ 延后 ／ 舍弃 ／ 静音。迁移表是唯一真相源：
  - 每个态都**到得了**，每个态都**出得去**（舍弃能捞回、静音能解除、延后能重出）；
  - 只有「已答」是终态（答完的卡不回池子；想再问是作者自己写新卡）；
  - 非法边（跳态、从终态出去）**当场拒绝**，一个字节都不写。
- **每次迁移都有可核对的证据**：同一个事务里改状态字段（并盖更新时刻）、写一条变更日志
  （动作码 + 从哪到哪 + 谁触发）。「问出」这一步还把「被问过几次」加一——那是新颖度冷却的账；
  舍弃 / 静音不动这笔账。
- **结构迁移 v8**：只给碎片统一表补一列（哪条模板）与一条按状态的查询索引，只增不改、可重跑。

### 验收

- 新增存储验收 9 例：建卡落表与溯源、六态可达性（**机械遍历迁移表**：进得来 / 出得去 /
  终态声明与出边互补）、**每条迁移都被真实调用走一遍并核对两处证据**（状态字段 + 事件里的
  `from`/`to`/`trigger`）、非法边与不存在 / 已删的卡一律报码且不写库、
  「结构迁移重跑不丢卡」。
- 命令行（**只在开发构建**）新增四条驱动命令（建卡 / 迁移 / 列卡 / 看迁移史）：状态机因此
  能从外部被驱动并独立核对；配一条命令行验收，把六个态从头走一遍、把事件历史对一遍。
- QA：静态通过（版本四处一致 0.51.0）+ Rust 全仓测试全绿 + 前端测试与生产构建全绿。

## 0.50.3 — 2026-09-15

> 只动发布链路的两处，让「构建指纹」里那句诚实说明在云端也成立。

### 修复（发布链路）

- **云端构建的指纹不再恒写「工作区有未提交改动」**。Windows 跑手的 git 默认
  `core.autocrlf=true`：检出的工作区是 CRLF、索引里是 LF，于是 `git status --porcelain`
  把 **272 个文件**全报成"已改"——而指纹里那句话本来是给人核对"这一包出自哪份源码"用的，
  恒真就等于噪声（v0.50.1、v0.50.2 的 Release 都带着它）。现在判据只看**已跟踪文件的内容差异**
  （比较前先把换行归一化），未跟踪文件另起一行列出来给人看：本机构建也能把"构建产物"与
  "真的没提交"分清楚。

- **仓库钉住 LF 换行**（新增 `.gitattributes`：`* text=auto eol=lf`）。这是上面那件事的根——
  检出到哪个平台都是同一份字节，指纹里的提交号才真的可核对。另一处一直被这份差异咬的
  "跨行选择器的守卫在云端找不到那段 CSS（本地绿、CI 红）"也随之消失。

### 验收

- 在**故意做成 CRLF 工作区**的克隆里实测新旧判据：旧判据报 272 个文件、新判据报 0 个；
  再对一个已跟踪文件改一个字节 → 立刻报"已改"（没有把真改动一起放过）。
- 本机整条出包流程重跑：指纹里不再有"工作区有未提交改动"，未跟踪文件单独一行列出。

## 0.50.2 — 2026-09-15

> 第二次按同一份评审员提示词做对抗式通读，这次专门挑"**静默失败 / 看起来像丢了**"这一类：
> 10 条中等（其中一条复核后定级为严重）+ 一批轻微项，逐条人工复核后全部为真，这一版全修了。
> 评审报告里另有结构性与规模类的中等问题（递归预算、全量扫、若干壳层例外）留待后续版本。
>
> 最重的一条是：**一次永不返回的落盘会把落盘状态机与关窗闸门一起锁死**——界面照常能打字，
> 退出时永远卡在"正在保存"，连那句"存不下去"的对话框都弹不出来。

### 修复（静默失败与"看起来像丢了"）

- **一次永不返回的落盘不再锁死状态机与关窗闸门**。"存不下去"有两种形态，以前只处理了"会报错"
  的那种：如果写库的那一次调用**永不返回**（通道异常、核心卡在锁上），在飞标记永不释放，
  读回校验与卡住抢救全部停摆，退出闸门永久停在"正在保存"。现在在飞的每一笔都有期限：
  到点就作废它（它回来时也不许再改状态）按"卡住"抢救；`flush()` 同样有限期，六道
  "先落盘再动手"的守卫与关窗闸门照常走到"拦住 + 弹对话框"那一步。

- **留痕与数据同生共死**。以前是"先提交、再追加变更日志"：日志那一条写不进去（磁盘满最典型）
  会把**已经落库的操作**报成失败——作者据此重试，导入会得到第二本书、改名会以为没改成。
  现在 30 处留痕全部放进与数据同一个事务（含"心跳"那条），只有一条语句的写入也一并收进事务。
  新增测试用触发器让日志表必定写不进去，断言改名 / 落盘 / 建节点三条路都是"报失败 ↔ 数据没变"。

- **备份账本写失败不再谎报"备份成功"**。包写好了但账本没写进去时，界面以前显示成功，
  而"缺了哪几天"还是旧值。现在账本成败随备份报告一起回给界面，失败了就明说。

- **读回指纹不再把"干净退出"标回"崩溃"**。纯读的一次核对以前会写心跳标记，把刚标好的
  "正常退出"重新标成"没退干净"，于是下次启动误报"上次没有正常退出"——崩溃提醒是作者唯一的
  崩溃线索，误报几次就没人信了。现在读只推进时间戳。

- **命令行与 `--check` 不再自称"只读"**。看稿那几条命令与 `--check` 都会打开库
  （可能升级结构、登记本机、给老库回填字数标记），`--check` 的报告默认还写在库旁边。
  它们改的从来不是稿件内容，但"不会改动稿库""不写任何东西"是假话——帮助文本、模块文档与
  `SECURITY.md` 的命令表都按事实改了，并加了一条守卫钉住"要体检坏库，先复制一份"。

- **逃生导出不再与故障同源**。"存不下去"的常见原因正是盘满 / 目录只读 / 介质写保护 /
  网络盘断开，而逃生导出以前只写"数据目录里的逃生文件夹"——最需要它的时候恰好也用不了。
  现在按"系统临时目录 → 作者主目录 → 数据目录"依次真写，哪个写成了就用哪个，
  并把**实际落点**告诉界面。

- **导出目录的旧文件清理只认自己写下的清单**，落点按作品分开：以前两本同名作品共用同一个
  导出目录，后导出的一本会把前一本上次导出的文件当成"残留"删掉。

### 修复（数据安全与结构）

- **换库失败不再让人跑在空书架上**。回滚失败以前被吞掉，壳接着去重开库、并在原位开出一个
  **空库**：界面看起来一切正常，其实作者面对的是空书架。现在两件事一起报出来
  （换库为什么失败 + 库不在原位，真库多半在「旧库留底」里），并且**绝不在原位建空库**。

- **恢复一章不再顺手复活"更早单独删掉"的子孙**（与"恢复整本书"同一口径：只捞同一次删除
  带下去的），同一处还修掉了"拿时间戳当批次凭据"在同一毫秒内连删两次会失效的飘。

- **命令行的看稿命令不再凭空建空库**。`--data` 写错一个字符时，以前会当场造出一个空库、
  再一本正经地回"没有作品"——作者看到的正是"稿子没了"。现在只认已经存在的库，
  并把"命令写错"与"没有稿库"分开报。

- **桌面壳的权限收到"具体命令"级，并给 WebView 设上内容安全策略**。原先授的是
  `core:image/menu/tray/resources/webview` 的 `:default`——其中 `core:image:default` 内含
  "按任意路径读文件"，与"界面不碰文件系统"的自述不符；`csp` 还是 `null`。现在只留界面真正
  用到的五条命令，`csp` 设成最小集。

### 修复（正确性与文案）

- 未闭合的编号宏只影响它自己——后面的合法宏照常渲染（以前表现为"后面的号不渲染"）；
- 从成稿导入改成**两遍法**：先把整批解析校验完，再统一写；"一份成稿读不进来就整批停下
  （不许救一半）"从此不是一句空话；
- "今天 / 昨天"按**本地日历日**算，不再拿毫秒差除 24 小时（跨零点前后的那几十分钟不再说错）；
- 备份暂存区名精确到毫秒：上一轮崩溃留下的同名目录不再让下一次备份"突然做不了"；
- 日期天数换算只留一份实现，两处与实现不符的注释（字数回写、落盘指纹各一处）改准。

### 验收

- 每条修复都配了一条"**修之前会红**"的测试（这一轮新增 12 条：前端 4、Rust 8）。
- 权限守卫从"查前缀黑名单"改成**反向枚举**：权限 → 展开后的命令 → 前端真实调用点，逐条对齐，
  多一条授权就必须写清"放开了什么、谁在用"；另加一条钉住 `csp` 不许再变回 `null`。
- 上帝级文件与文案欠账按棘轮的规矩登记（14 个 ≥400 行的文件都写清"为什么长、要拆先拆哪儿"；
  核心诊断串仍记在欠账里，"改成码 + 参数"那条任务待做）。
- Rust 全仓 42 个测试目标、前端 245 项、类型检查、生产构建与静态六项全绿。

## 0.50.1 — 2026-09-15

> 公开之后第一次修数据安全缺陷。一次**外部视角的对抗式代码评审**（一个专门找问题的 AI 角色，
> 按一份固定的评审员提示词只读通读源码，每条发现必须给文件行号与能自己复现的验证方法）
> 找出 8 条严重问题，逐条人工复核后 **8 条全部为真、没有误报**——这一版把 8 条全修了，
> 每条配一条"修之前会红"的测试。
>
> 最重的一条是：**出货的 0.50.0 里，验收模式会按参数删掉整个目录**，包括你的稿库。

### 修复（数据安全）

- **验收模式不再能删掉你的稿库**（本版最重的一条）。`--self-test-bench --dir <目录>` 原先会先把这个
  目录整个删掉、再重建成验收用的临时库，**对参数不做任何校验**；而验收脚本的 `%*` 又允许覆盖 `--dir`。
  实测（拿公开的 0.50.0 指向一个放着稿库文件与作者自己备份包的目录）：退出码是 `0`，
  看着像跑成功了，备份包消失、库被换成 22 MB 的验收库，**不可恢复**。
  现在清空之前必须先过一道门：目录要么落在系统临时目录之下，要么带验收模式自己写的记号文件；
  **只要目录里有稿库、又没有记号，一律拒绝**（哪怕它就在临时目录里）。拒绝时**一个字节都不动**、
  退出码非零、原因写进报告第一步；脚本里 `%*` 挪到 `--dir` 之前，并显式拒绝传 `--dir`。

- **"存不下去就不许走"这句话，以前是假的**。落盘有两处漏：① 已经有一笔写在飞时，
  "立刻落盘"只把那笔**旧的**等回来就返回——防抖窗口里落的第二笔没人管，随后切章会把定时器取消，
  那一段输入永远没入库；② 写失败时只把状态标成错误，却不把失败报给调用方。
  于是"先落盘再切章 / 删章 / 删书 / 回滚 / 换库 / 搬家"这**六道守卫全都拦不住**。
  现在 `flush()` 的契约是"**返回时手上这一版必须已经落进库里，落不下去就抛**"——
  六道守卫的调用点一行没改，就全部生效了。

- **双击标题栏的 X 不再直接退出**。第一次关窗请求之后界面才开始落盘，而第二次请求
  （双击本来就会产生两次）被当成"用户坚持"，直接退出：从上次防抖落盘到那一刻的击键无声消失，
  作者连"要丢字吗"都没被问过。现在第二次只是**再通知界面一遍**，期限不重置——
  所以"一直点"既不会关不掉，也不会挡住"界面已经死了"的兜底退出。

- **位置记录指向的文件夹里没有库时，不再凭空建一本空书**。文件夹被改名或挪走、库文件被清理工具
  删掉之后启动，以前会照旧建目录、开一个新库：作者看到的是**空书架**加一本自动冒出来的"未命名作品"。
  现在明确拒绝启动并把话说清——稿子多半还在别处，先把那个文件夹找回来或用备份恢复；
  确实想从头开始，就删掉位置记录文件再启动。

- **删掉正在写的那一章之后，编辑器不会进入"能打字、却永远不会落盘"的静默状态**。
  以前删章后会把落盘控制器摘成 `null`，万一换落点失败就永久为 `null`：界面照常能打字、
  状态栏停在上一章的"已保存"、退出时还直接放行。现在控制器留着并明确报出
  "这一章已被删除，新的字暂时存不进去——先复制一份备份，再切到别章"，退出闸门据此拦住你。

### 修复（结构与正确性）

- **收卷 / 撤卷 / 整本换写法改成"要么全成、要么全不成"**。这三件事原先由多步写拼起来、
  每一步各自提交：中途任何一步失败（或进程被杀）都会留下半成品——比如新卷建好了、一部分章
  还在外面，或者前半本中文数字、后半本阿拉伯数字，而作者拿不到撤销。现在各自是一个事务。
  实测：把旧版代码拿来跑"收卷中途失败"的场景，失败之后根层确实多了一个"建好一半的新卷"。

- **补零与偏移可以共存了**：`{$N:3+49}` 以前是**坏宏**——把编号档切成"补零"再执行整本换写法
  就会生成它，而解析不认，于是整本书的标题变成原样显示的乱码，且不可逆。
  现在两头都通，**已经被写坏的书也跟着好了**（它现在能被认出来、正常编号）。

- **极深的目录树不再让导出 / 备份爆栈**。备份对每一本书都调一次导出渲染，关窗自动备份也在其中——
  一次栈溢出整个进程就没了，连"干净退出"标记都来不及做。现在超过深度上限明确报错
  （`TREE_TOO_DEEP`），而且与读路径**同一把尺子**：写入口允许的深度一定导得出来，
  比它深的坏数据一定报错而不是崩。

### 验收

- 每条修复都配了一条"**修之前会红**"的测试（本次共新增 11 条）。其中"收卷中途失败"与
  "验收模式指向真稿库"两条，逐个用旧版实测过红——确认测试真的能抓住它。
- Rust 全仓 374 项、前端 241 项、生产构建，以及静态检查（版本一致 / 公开内容脱敏 /
  文档链接 / 文件卫生 / 依赖方向 / 规模）全绿。

## 0.50.0 — 2026-09-14

> 设置里多一栏「排版」：**字号 / 行距 / 字距**随你调——只改你看正文的观感，
> 不进导出、不动正文一个字。

### 新功能

- **正文排版：字号 / 行距 / 字距**（设置 → 排版）。三个旋钮，拖一下正文立刻变，
  旁边还有一段示例字就地看效果：

  - **字号** 12–30 px；**行距** 1.1–2.6 倍；**字距** 0–20%（按字号的比例算，5% = 每字之间加 0.05 字宽）；
  - 值存在外观偏好里（与别的偏好同一套：全局打底、每书可覆盖的机制已经在了，第一版先只暴露全局）；
  - **纯显示层**：不进导出、不动正文一个字节（有验收钉着），换书/重启都还在；
  - 越界与坏值一律**夹进可读范围**（手滑打 1000 号字不会把版面炸了，也不会报错挡人）。

- 档位表与换算只写在一处（`app/frontend/src/editor/typography.ts`）：组件里不写死 px 与倍数，
  范围与核心一致（核心夹一次防坏数据，界面再夹一次防手滑）。

### 修复（公开前的收尾，都在这一版里）

- **产物文件名改成 ASCII**：`yanmo-<版本>-setup.exe`（安装版）、`yanmo-<版本>-portable.exe`（便携版）、
  `yanmo-<版本>-portable.zip`（绿色版），构建指纹同样改成 `yanmo-<版本>-build-fingerprint.txt`。
  原因是实测出来的：**GitHub 的 Release 资产名存不了中文**——中文会被剥成 `-0.50.0-setup.exe`
  这种形态（连直接调 API 改回中文都会被退回 ASCII），于是页面上显示的名字与下载到的文件名对不上，
  而这又跟 README 里写的不同。现在两头都是 ASCII，一一对应。
  **包内不受影响**：绿色版里仍是 `研墨/研墨.exe`，装好后的程序名、窗口标题、界面文案仍是「研墨」。
- **SECURITY.md 补上「发现了安全问题，怎么告诉我们」**：优先走 GitHub 的**私密报告**通道
  （修好之前不公开），并写清三样别贴的东西——稿子正文、完整库文件、你自己的文件路径；
  同时说实话：个人项目不承诺响应时限。另外把两处公开文档里指向内部工具的指针改掉
  （它们不随公开仓一起发布，读者点不到）。
- 顺手修掉一处**会飘的测试**（跟产品无关，是测试自己写错了）：码字账本那条"两个时区分桶"的
  断言，拿 `days_ago(0)`（按 UTC 数）当查询窗口的上界，可其中一笔是按 +8 区写的——
  本地午夜前后的 8 小时里那笔落在窗口外，测试必红。现在窗口改用**账本里真实存在的日子**来定。

### 验收

- 核心 `tests/appearance.rs` 新增 1 例：三项分别能记、**只写传进来的那项**、每书覆盖各记各的、
  坏数据当没设过；极端值夹到范围；**调完正文与导出字节一个都不变**（这条是这项功能的底线）。
- 前端 `typography.test.ts` 5 例：默认档 / 三项各归各位 / 越界与坏值夹住 / 档位表与核心范围一致
  （改一处忘另一处会红）/ 行距显示成倍数；`appearance.test.ts` 补 1 例：只写这一项、0 = 清掉。

## 0.49.0 — 2026-09-14

> 章节编号多一个选择：**第2卷接着第11章数**（新的默认），也可以每卷从第1章重来。
> 下载页（Release 说明）也跟着补齐：三个产物哪个是哪个、怎么装、第一次打开看到什么。

### 新功能

- **章节编号：跨卷延续（默认）/ 每卷从头数**。设置 → 命名那一栏多了「章节编号」：

  - **跨卷延续**（默认）：全书按阅读顺序数下去——第1卷 1~10，第2卷就是 11~20；
  - **每卷从头数**：每一卷各自从 1 开始（原来那个行为，想回去选它就行）。

  它与命名写法共用同一个「作用范围」（所有作品的默认 / 只设某一本），所以也能一本书单独设。

- 号仍然是**位置的函数**：标题里存的还是写法（`第{$N}章`），这里换的只是"数的范围"，
  所以**改一下立刻全见效**，标题与正文一个字都不动；插入 / 删除 / 拖动 / 从回收站捞回
  照旧自动顺延。作者手写的 `{$N_RESET:101}` 仍然说话——那是显式指令，到哪儿都算数。
  （若以前是靠手写偏移给第二卷接号的，请改回「每卷从头数」或把偏移去掉——两样叠在一起会重复加。）

### 修复

- **发布说明自动带上「上手三步」**：以前 Release 页面上只有"这一版做了什么 + 怎么核对"，
  读者得回仓库首页才看得到"三个文件该下哪个"。现在生成发布说明时一并带上，版本号按本次发布填：
  安装版 / 便携版 / 绿色版的区别、怎么装、第一次打开会问什么。
  （内容与 README 的「上手三步」是同一份——两处都写，是为了让**下载页自己就够用**。）
- 顺带把版本号解析加了兜底：CHANGELOG 标题认不出来就写占位符，
  **绝不在最后一步报错**——发布说明挂掉等于前面的包白打（这条踩过一次，见 0.43.5）。
- **README / RELEASING / CONTRIBUTING 跟着这几轮改动对齐**：上手三步与「当前已经能用」补上
  「一卷大概多少章」；「能帮你什么」与常见问题补上收卷提示、编号默认跨卷延续两条
  （第二卷的号跟以前不一样了，这是最可能被问的一句）；一键构建由"四件事"改成实际的六步；
  校验和示例不再写死某个旧版本号；开发地图补上新的渲染模块。

### 验收

- 核心新增 `tests/chapter_numbering.rs` 6 例：默认跨卷延续 / 三条读路径（整树、单层懒加载、
  单节点）给出同一个号 / 每卷从头数（含每书覆盖、全局改后跟随、清掉覆盖）/ 显式重置指令
  仍然生效 / 导出跟着同一份号 / 认不出的代码明确拒绝、库里的坏记录当没设过。
- 前端 `appearance.test.ts` 补一例：写哪一层由「作用范围」决定；没打开作品时"只设这本书"是空操作。
- 在 CI 跑手那样的**西文代码页（1252）**下实跑了一遍发布说明生成步骤：输出文件是 UTF-8、
  中文完好、版本号已代入产物名；完整预演（干净检出 + 同一条构建命令 + 1252 下的发布说明）全绿。


## 0.48.0 — 2026-09-14

> 一卷写到差不多的时候，目录栏里会安静地问一句「要在这里收卷吗？」——
> **只提议、不动结构**；收好了留一个「撤销」，撤了正文一个字不动。

### 新功能

- **自动分卷**。长篇写到一卷的尾声时，目录栏里出现一行小字与两个按钮：

  - 「**在这里收卷**」：**卷内**按 = 这一卷到此为止——在它后面新建下一卷，收卷点之后的章一并挪进新卷
    （若后面还没写，就在新卷里起第一章，光标落过去接着写，新卷从「第1章」重新起号）；
    **根层**按（章还散在根上、没进过卷）= 把「上一个卷之后」这一段散章收成一个新卷，光标不动。
    **没有收卷点就绝不改结构**，正文一个字都不碰。
  - 「**再等等**」：这一次先不提；连着两次，这一卷就不再提（回答不白问）。
  - 收好之后留一句回执 + 一个「**撤销**」：把刚收的那一卷拆掉，里面的东西按原顺序还回去
    （并回它前面那个卷；前面没有卷就抬回原位），阅读顺序与正文都不变——那一卷进回收站，捞得回来。

- **阈值跟他自己比**：作者在「每卷 __ 章」里定的数先用；这本书一旦有**两个以上收好的卷**，
  就改用他实际收卷点的**中位数**——越写越像他自己的节奏。**80% 起就可以收**（提前收卷是正常决定），
  过了 **120% 话术加重但不停口**（若就此闭嘴，他反而彻底没有入口，只能自己算号建卷）。

- **建书页多问一句「一卷大概多少章」**（只有长篇显示，可以留空）：填了就在开写之前落好，
  之后随时能在目录栏那一栏改。它只影响那行小字与收卷提示，**不改书的结构**。

### 验收

- 核心 `tests/volume.rs` 8 例：零层级收拢 / 卷内分出新卷 / 提前收卷起新卷第一章 / 多卷继续成卷 /
  两种撤卷都还原 / 提议的起提线与档位 / 从历史学阈值 / 收不了卷的位置明确报错。
  成卷与撤卷一律断言**阅读顺序不变、同级序号密集、正文逐字节一致**。
- 前端 `volumes.test.ts` 7 例：只在收卷点上有提议、「再等等」连着两次闭嘴、换卷不串门、
  慢回来的旧答复不许盖掉新状态、没有提议时不碰结构、撤销、换作品清干净。
- 建书页那一条线加了「填了就落、没填就不写」（`shelf.test.ts`）。

## 0.47.1 — 2026-09-14

> 书架上那一排按钮换个次序（用户真机给的）：**编译 | 编辑 导出 删除 | 收起**。

### 修复

- **书架卡片上的按钮次序调整**：展开「更多」之后那一排从
  「编译 收起 导出 编辑 删除」改成 **「编译 编辑 导出 删除 收起」**——
  三个低频动作（编辑 / 导出 / 删除）紧跟在「编译」后面，开合开关「收起」挪到最右边。
  按钮一个没多、一个没少，只是位置换了。

### 验收

- 界面侧 `shelf` 那一场加一条次序判据（按 DOM 次序读这一排按钮的文字，
  不是看截图像不像）：展开后必须是 **编译 | 编辑 导出 删除 | 收起**。

## 0.47.0 — 2026-09-14

> 首启那条提示上多给一个入口：**「就在这一本里写」**——不用另建一本，
> 直接给自动建的那本空壳起个名就开始。

### 新功能

- **首启引导的第二个入口「就在这一本里写」**（用户真机反馈后排定的形态）。原来只有
  「建一本书」一条路：想马上动笔的人也得先走一遍建书表单。现在**两个入口并排**——
  另建一本，或者**直接接手那本空壳**：点第二个人开的还是那张作品表单（标题写「给这一本起个名」），
  填完名字落在**原来那一本**上，一本都不新建。
- 接手表单里**类型是可挑的**：空壳还没名字、也还没写一个字，这会儿换类型没有旧目录要保护
  （「编辑作品」里类型只读，是因为它决定已有目录长什么样）。同类型就地改名；选了长篇 / 短篇集
  就按那个类型**另建一本**，空壳照旧收进回收站并说一声——对作者来说结果都一样：
  书架上留下一本他起名的书，类型是他要的那种。

### 验收

- 界面侧新增第十九场 `first-run-here`（真程序 + CDP）：提示条上**两个按钮都在**；
  点「就在这一本里写」开出的是接手表单（类型可挑、书名空着）；填完提交后书架上**只有一本**、
  书名进库、**id 就是原来那本空壳**（没新建、也没进回收站）、提示条自己消失。
- 前端 223 例（新增 4 例：同类型就地改名、换类型另建并收空壳、空书名当场拦住、非首启态没有这条入口）。
- 首启 `first-run` 那一场（8/8）继续全绿——两个入口各走各的，谁都没挡住谁。

## 0.46.2 — 2026-09-14

> 导出的回执回来了（它从 v0.39.0 起就没再显示过）；第一次打开自动建的那本空壳，
> 在作者建了自己的书之后**顺手收进回收站**并说一声。

### 修复

- **「导出」点了像没反应**（用户真机报的）：导出**一直在写文件**，但屏幕上没有任何回执——
  v0.39.0 的建书页重构把「导出到哪儿了」那一行误删了。谁都没发现，因为功能本身不报错：
  这一类"功能在跑、反馈没了"的毛病，只有从真界面点一遍才抓得到。现在回执行补回来了
  （与回收站 / 版本那两个弹窗同一条样式与位置），里面写着**几个文件**和**落到哪儿**。

### 行为

- **建了自己的书之后，第一次打开时自动建的那本「未命名作品」顺手收进回收站**（可捞回）。
  它是首启的占位壳子；留着只会让书架多一条没意义的东西。只收**判据成立的那一本**
  （只有一本 + 没名字 + 一个字没写），并且**收完在书架上说一声**——悄悄动作者的书架是要不得的。

### 验收

- 界面侧新增第十八场 `shelf`（真程序 + CDP）5/5：建了自己的书之后书架上是它、
  **那本空壳在回收站里**（不是真删）、再打开书架看得见"已经收进回收站"那句；
  点「导出」**看得见回执**（含文件数与落点）、且**回执里那个落点真有文件**。
- 前端 219 例（新增 3 例：建书收空壳并说一声、有名字的书不许动、写过字的不许动）。

## 0.46.1 — 2026-09-14

> 「恢复默认」不再在**每一栏**都长一个——它只在「关于」那一栏。

### 修复

- **设置面板右列底部的「恢复默认…」原来每一栏都渲染一次**（那个 footer 写在分栏容器之外，
  所以切哪一栏它都在）。它放在右列底部本来是为了离「关闭」远一点、防手滑，但七个分栏
  各长一个，看着就像六七个按钮。现在**只在「关于」那一栏**出现（不可逆的全局动作
  仍然保留那一次确认层）。

### 验收

- 界面侧新增第十七场 `settings`（真程序 + CDP）4/4：第一栏（写作行为）里**没有**它、
  切到「关于」才有（含「只恢复偏好，不动稿子」那句）、切回别的栏又收起来。

## 0.46.0 — 2026-09-14

> 改名与简介搬进**同一张作品表单**（新建 / 编辑两用）；首启不再是一片空白——
> 跟「上次没正常退出」同一条位置给一句"第一次使用？"+「建一本书」。

### 新功能

- **作品表单统一**：新建与编辑用**同一张表单**。编辑时标题是「编辑作品」，只手填**书名 + 简介**；
  **作品类型只读展示**（它决定目录长什么样，建好之后不给改），**命名规则也不在编辑表单里改**
  （那有「整本换写法」的预览确认流程，两套改法迟早打架）。
  书架卡片上原来那两个内联小框（双击改名的单行输入、「简介」的多行框）**整块撤掉**，
  合并成一个「编辑」——空书名也不会再撞出核心那句红字「作品标题不能为空」了
  （表单在主按钮上就拦住了）。
- **首启引导**：给「第一次使用」做了一条**机械可判**的状态——只有一本、没起名字、一个字没写
  （核心首启会先建这么一本无名空壳，所以"书架上没有书"永远不成立，作者看到的是空白 + 未命名作品）。
  命中时在正文上方（与「上次没正常退出」同一条位置与样式）显示一句「第一次使用？现在就可以建一本
  自己的书」+ **「建一本书」按钮**，点了直接开建书表单；建完它自己消失（不用另记"看过引导没有"）。
- 顺带修的**顺序坑**：书架列表原来只在打开书架时才拉，而首启那本空壳是"打开当前章"那一步才建出来的——
  引导要拿它判断，早拉一步只会拿到空列表（真机上就是这么翻的车：提示永远不出现）。
  现在拉取点挪到"打开当前章之后"。

### 验收（真界面，走 CDP 那条通道）

- 界面侧新增第十六场 `first-run`（8/8 判据）：提示条在不在、写着什么、按钮文案；点它开出的是
  「新建作品」；填完真建出一本书（书名 + 简介都落库）；建完提示消失；书架卡片上的「编辑」开出的是
  「编辑作品」且书名已预填；改完存下后**按 id 核对**库里的书名与简介都变了。
- 界面侧五场 **38/38**（first-run / portable / trash-states / snapshot-states / restore-swap）；
  前端 216 例全绿（新增 3 例：首启判据、编辑动作与空书名拦截、表单开合的两种模式）。

## 0.45.0 — 2026-09-14

> 绿色版**转正**：稿子真的跟着程序目录走，而且改名 / 换盘符不会再把稿子"变没"；
> 发布产物补齐三件（安装包 / 便携 exe / 绿色版 zip），**许可证随包**。

### 新功能

- **便携模式（真·绿色版）落地**：程序目录里放一个 `yanmo-portable.txt`（由**发布形态**带进去——
  绿色包自带），稿子就放在程序旁的 `data\`：拷到别的盘、别的机器，稿子一起走；并且
  **不会**去认领那台机器的 `%APPDATA%`（那是"乱认亲"——别人机器上的稿子不该被当成自己的打开）。
- **便携模式下不再为"程序旁的 `data\`"写位置记录**。这是这次核对代码时挖出来的真坑：
  记录里存的是**绝对路径**，而打开时对它 `create_dir_all` + 绝不静默换地方，于是
  文件夹**改名 / 移动**会凭空建出一个空库（作者看到的是"稿子没了"），U 盘**换盘符**
  会直接报"数据目录不可用"——两条都正好打在"拔盘即走"上。现在便携形态的数据目录由
  规则（标记 + `data\`）确定，**不写、也不依赖**那条记录；作者**亲手换过位置**的照旧记
  （那是他的选择，"记录就是记录"在这条上仍然成立）。

### 发布产物（三件一起出，同一次构建）

- `tools/build_release.py` 现在一次出齐：**安装包**（NSIS）、**便携 exe**、**绿色版 zip**
  （`研墨-<版本>-绿色版.zip`；里面是一个**不带版本号**的 `研墨/` 文件夹：exe + 便携标记 +
  LICENSE + 一页怎么用）。绿色包的文件夹名**故意不带版本号**——带版本号的话，"下载新版解压"
  会解出一个新文件夹，而稿子在旧文件夹的 `data\` 里，那正是"看起来稿子没了"的经典现场；
  名字稳定，解压到同一个位置＝原地覆盖 exe＝真更新。
- **许可证随包**：`bundle.licenseFile` 指向仓库根的 LICENSE（安装包也带上），绿色包里也放一份——
  AGPL 分发二进制本来就该随包给许可证。
- `一页怎么用.txt` 是写给上手的人的：怎么开始 / 稿子在哪 / **怎么更新（解压到同一个文件夹、
  别动 `data\`）** / 备份与换电脑 / 需要系统 WebView2 / 许可证。

## 0.44.4 — 2026-09-14

> 版本列表里"认不出来的来源"不再被显示成「自动留的」；工具仓多一件静态审计：
> **界面认识的值必须有人产出**。

### 修复

- **`snapshots.reason_auto`（「自动留的」）是个死标签**：核心只有 5 档写入理由
  （`close` / `stuck` / `desync` / `keep` / `before_restore`），**没有任何地方会产出 `auto`**。
  更要紧的是它被当成"认不出来的 reason"的**兜底文案**——于是一个真正没见过的来源
  （新加的档没配文案、或库里那条记录来源异常）会显示成「自动留的」，看起来像个正常来源。
  现在认不出来的码**原样露出来**（与字典那条「查不到就返回键名，宁可露键也不要编」同一条纪律），
  死标签一并删掉，前端测试把这条期望钉住（`snapshotReason("没见过的码") === "没见过的码"`）。

### 工具链（在独立工具仓，不进本仓）

- 新增静态审计 `qa/audit_states.py`（**值可达性**：界面认识的值 ↔ 真能产出的值，两头对表；
  「死值」＝界面认识却没人产出，「没人认领」＝产出了但界面不认识），并挂进本仓 QA 门的静态层
  （与「脱轨审计」并列，同为启发式：只报警不拦）。第一次跑就抓到上面那个死标签。
- 顺带修了第十三场（并发）**自己的判据**：它原来写死了"另一章那一路必须写成功"，
  可并发下被明确拒（给码）本来就是允许的结果——判据与契约不一致会假红。
  现在改成"成了就必须完整、被拒了就不许被动"，两种结果都算过。
- 外部演练台新增第十四场 `session-states`：会话状态机的**可达性**——三个标记态
  （无标记 / 开着 / 干净）都到得了、**每个态都出得去**（异常退出之后必须能回到「上次退干净了」）、
  两条收尾出口的差异（`end` 留关窗快照、`abandon` 不留）、重复与交错收尾幂等，
  并钉住一个容易看走眼的语义：`begin` 的交代说的是**上一次会话**、`report` 说的是**此刻标记**。

## 0.44.3 — 2026-09-14

> 库体检的结论分成三态：**「没查成」不再被说成「坏了」**——库文件只读时，救援工具不会
> 再对着一本完好的书喊「库文件有问题，先别做别的操作，赶紧导出留底找开发者」。

### 修复

- **库只读时体检喊狼来了**（由外部演练台的组合故障那一场发现）：`PRAGMA quick_check`
  里的 FTS5 索引核对需要**写权限**，库文件被设成只读时 SQLite 回的原话是
  `unable to validate the inverted index for FTS5 table main.node_fts: attempt to write a readonly database`。
  原来的调用方拿「不是 ok」当「库有问题」，救援菜单于是照警告那一支念：
  「库文件有问题……先别做别的操作，用 [4] 把还能读出来的稿子导出成文件，再复制一份留底找开发者」——
  对一本**完好**的书喊这套话，作者会白做一堆抢救动作，而真正的原因只是文件带了只读属性。
  现在核心把结论分成**三态**：`clean` / `problem` / `not_checked`（SQLite 的原话仍留档），
  救援菜单照三态说三种话：「看起来没问题」/「库文件有问题」/「**这一次没能核对**（多半是库文件
  被设成了只读，核对索引那一步要写权限）——这不是说库有问题，稿子照常读得出来；想核对就先去掉
  只读再跑一次 [2]，只想拿稿子直接 [3] / [4] 导出」。
- `verify` 命令的 JSON 多给一格 `integrity_state`（`clean` / `problem` / `not_checked`），
  `integrity` 仍是 SQLite 的原话（日志与报告留档用）——调用方据此判断，不再拿字符串跟 `"ok"` 比。

### 验收

- 核心新增 `tests/integrity.rs` 2 例：健康库判 `clean`；`PRAGMA query_only`（可移植的"写不进去"
  造法，Windows 上不必依赖只读属性）下判 `not_checked`、原话留档、**正文照样读得出来**；
  `db` 模块另有分类单测钉住「同样带 unable to validate、但原因是索引真坏了」不许被说成"没查成"。
- 外部演练台新增第十三场 `chain-conflict`：只读库 + 备份目标不可写 + **杀掉正在写的进程**三样齐发，
  18/18 判据通过（不卡死、每个失败都留痕、备份目标里已有的那份一个字节没动、撤掉故障后一切照旧）。
  其中两条直接钉住本次修复：体检结论不许判 `problem`；**菜单里不许出现「库文件有问题」，
  而要给「没能核对」**。

## 0.44.2 — 2026-09-14

> 极深目录树：**写得进去的树，必须读得出来**——写入口补上深度守门，读路径不再静默少算。

### 修复

- **能造出「写得进、读不了」的树**（由外部演练台发现）：`create_node` 没有深度守门，
  而读路径有 512 层上限——连续往下建 600 层全都建进去了，正文写得进、读得回、导出也出得来，
  可是那一章在界面里**打不开**（「打开章节沿祖先链展开」当场报错，码还是文不对题的「疑似成环」）。
  现在**新建与移动都在写之前守门**：能写进去的树，永远在能读出来的深度以内；到上限给
  `tree.too_deep`（带 `max` 参数，话怎么说由界面自己定）。移动那一条还看**子树高度**——
  只算自己要落地的那一层是不够的，底下还挂着一串。
- **卷行字数会静默变少**：`subtree_rollup` 原来用 `depth < 512` 截断**且不报错**，
  比上一条更糟——"说 ok 但数字不对"正是本项目最不容忍的那一类。现在碰到越限的子树**明确报错**，
  绝不少算。祖先链与回收站父链用同一条口径（同样报 `tree.too_deep`，不再冒充「疑似成环」）。
- **深度上限从 512 收到 64**（同一次演练量出来的第二件事）：那个数当初是当"坏数据哨兵"定的，
  **比代码实际走得动的深度还大**——调试构建在 **384 层**导出成 json 就 `has overflowed its stack`
  （发布构建 512 层能过），也就是"合法数据"里含着能把进程搞崩的一档，而崩掉的正是作者手上没落盘的东西。
  写作软件的大纲（卷 → 章 → 节 → 场景卡）通常 2~4 层，512 从来没有产品上的理由；64 离崩点留
  ≥4 倍余量，也远宽于任何真实书。现在**读写共用这一个数**：新建 / 移动超过就拒绝，读路径碰到就报错。

### 验收

- 新增 `crates/yanmo-core/tests/deep_tree.rs` 3 例：一路建到上限（**拒绝要给码 + 深度预算够用**）、
  最深处祖先链/整树列出/字数汇总都对、把两层的小树挂到最深处会被拒**且原地不动**、
  **绕过写入口塞一个越限节点**（模拟坏数据）时读路径报错而不是少算。
- 外部演练台（独立工具仓，不进本仓、不链接核心）新增第十一场 `deep-tree`，判据全过：600 层试探→拒绝给码→
  最深处写读→整树列出→导出 json 带得走最深那一章（层数对得上）→分章 txt 文件数对得上、
  最深那一份读得回。顺带记下一个边界：**分章 txt 会把层数铺成目录**，那么深的现场路径
  超过 260 字符，普通 Windows 工具摸不到（演练台已能自己收拾干净）。

## 0.44.1 — 2026-09-14

> 「恢复默认」不再挨着「关闭」；发布前多了一道**拿上一版真实库跑一遍**的演练。

### 修复

- **「恢复默认」离「关闭」太近，容易手滑**：它是不可逆的**全局**动作，挨着关闭按钮迟早有人点错。
  现在它搬到右列底部（与关闭按钮分开），并且**点它还会再问一次**：
  「把偏好恢复到初始状态（打开最新章的行为、命名规则、字数口径、引号风格、每日目标）——
  稿子一个字都不动」。确认层单独一个组件（`SettingsResetConfirm.vue`），设置面板本身不跟着长胖。

### 发布流程

- **`RELEASING.md` 加「发布前的升级演练」**：备一份上一版的真实库样本（**连 `-wal`/`-shm` 一起复制**）
  → 用新版本打开副本跑常用动作（`verify` / `works` / `nodes` / `read` / `search` / `export`）
  → **对账**（两边各导出一份成稿 `diff -r`，外加规模只增不减、索引跟得上、快照与回收站还在、还能接着写）。
  为什么必须有这一步：自动测试用的是合成样本，而真机上的生僻字、深浅目录、场景卡、捞回的章是造不出来的。
- 同一次演练已**真跑**过：v0.43.5 写的真实库（2 本书 / 11 个节点 / 8 份快照）跑 0.44.0，
  `integrity: ok`，11 个原有节点**逐字一致**，导出 9 个文件，新写一章后索引 11 → 12 行。

## 0.44.0 — 2026-09-14

> 设置面板从"一长列"改成**左列分类 + 右列内容**：设置项一多，摊在一条竖线上就看不出层次了。

### 新增

- **设置面板分区**：左边一列四类——**写作行为 / 新建条目 / 稿子放在哪 / 关于**，
  右边只显示当前这一类的内容。面板也放宽到 720px（原来 520px），右列**自己滚**，
  头部那排按钮（恢复默认 / 关闭）永远待在原地。
- 分类顺序按"作者多久来一次"排：**写的时候想调**的在前，**装了才看一眼**的在后。
- 分类表单独放 `editor/settings-nav.ts`（纯数据 + 纯函数），并配了 4 条测试：
  id 不许重复、**每一类的名字必须在界面字典里**（加分类忘加文案 → 导航上会露出一串键名，
  这类"点了没反应 / 露键名"的毛病最难查，交给机器盯）、打开时落在第一类、认不出的 id 说不认识。

### 为什么这么改

设置项只会越加越多。之前四组内容摊在一个竖向滚动条里，作者要找一个开关得先滚一遍；
分组标题与内容同字号、同颜色，层次全靠读文字——**结构没有体现在版式上**。

## 0.43.6 — 2026-09-14

> 设置里那处重复显示修掉了；顺手把"代码给人和 AI 读"这件事收了个尾。

### 修复

- **设置面板里稿子路径显示了两遍**：「稿子放在哪」一组与「关于」一组各摊了一遍绝对路径，
  看着像出了错，也让人不知道该信哪一处。现在**路径只在「稿子放在哪」显示一次**（它才是"位置"这件事的归属），
  「关于」保留「打开稿子所在文件夹」按钮（它的标签已经说清打开的是哪个文件夹）。
  README 与 SECURITY.md 的指引跟着改到同一处。

### 打磨（可读性）

- **公开条目的注释补齐到 100%**（核心 335 条、命令行 20 条、桌面壳 208 条 pub 条目全部有说明）：
  之前缺的主要是 `pub mod` 这类登记行、以及 `as_str` / `parse` 这种自明的方法——
  单看每一条都不值一提，但"打开文档一片空白"正是接手的人第一眼看到的东西。
- **注释里的 6 条死链修掉**：`[`私有模块`]` 这类写法的链接早就解析不到了（私有条目、搬走的函数），
  文案还在、链接已死。现在要么写成能解析的完整路径，要么改成纯代码标记。
- **`cargo doc` 零告警进闸门**（由一条独立守卫盯着：注释里的引用从此**只许指得到**）：顺带记一条口径上的坑：源码没变时 `cargo doc` 什么都不打印，
  "告警数 0"可能只是这次没重新生成文档——守卫会先碰一下源文件逼它重新生成，**不给假的绿**。
- **`CONTRIBUTING.md` 加「五分钟读懂这个仓库」**：三条铁律 → 目录地图 → 一条功能链怎么读 → 怎么改不被守卫拦。
  第一次打开仓库的人（和 AI）不用再通读代码找入口。

## 0.43.5 — 2026-09-14

> 第二次真跑自动出包又抓到一类"本地绿、CI 红"：Windows 跑手说西文，Python 打印中文直接崩。
> 这次不只修那一处，而是把这类差异**在本地就能预演出来**。

### 修复

- **出包成功后卡在"生成发布说明"**：`tools/release_notes.py` 最后那句中文回执
  （"已写入 xxx（N 字符）"）在 GitHub 的 Windows 跑手上抛 `UnicodeEncodeError`——
  跑手默认代码页是 **cp1252（西文）**，编不出汉字；而本机是中文代码页，本地永远看不出问题。
  结果是：**包里六步全绿、安装包都打好了，Release 却一步都没走到**。
  现在两个构建脚本都把标准输出/错误统一切到 UTF-8（编不出的字符退成 `?`）：**打印失败绝不该让流水线挂掉**。

### 加严

- **工作流**（`.github/workflows/release.yml`）：
  - 设 `PYTHONUTF8=1` / `PYTHONIOENCODING=utf-8`，让所有 Python 步骤**不依赖控制台代码页**；
  - 产物通配改成 ASCII（`dist/*.txt`），不在 CI 里赌非 ASCII 的 glob；
  - 加 `timeout-minutes: 60`：卡死就明确失败，不无声烧额度。
- **新增本地预演** `tools/rehearse-release.bat`：把代码页调成跑手那样的 1252，再跑一遍 CI 的那两步。
  **出包前跑一遍**，这类环境差异就不用等云端十几分钟才发现。
- **文件卫生检查加一条**：批处理文件必须是 ASCII。cmd 用本机 OEM 代码页读 `.bat`，
  UTF-8 的中文注释会让它**错行**（脚本跑到一半报"不是内部或外部命令"）——同一个坑踩过两次，这次上机械守卫。

## 0.43.4 — 2026-09-14

> 第一次真跑自动出包，抓到一处"本地全绿、CI 全红"的守卫毛病：它把检出时的换行符当成了源码内容。

### 修复

- **边界守卫在 CRLF 检出上会误报**（`app/tests/boundary.rs`）：状态栏那组样式有一条**跨行选择器**
  （`.editor__count,\n.editor__lang,…`）的断言，而断言里硬编码了 `\n`。Windows 上 `core.autocrlf=true`
  是默认，检出来的源码是 **CRLF**，于是这条断言**永远找不到**那段规则——报错还是"找不到那条规则"，
  看着像样式被人删了，查起来极绕。本地开发机是 `autocrlf=false`（LF），所以本地一直全绿。
  现在读源码统一把换行归一成 `\n`；并补了一条**回归测试**：拿一份 CRLF 的临时文件走一遍 `read()`，
  断言归一之后跨行选择器照样命中。
- 教训写进注释了：守卫盯的是"有没有这条规则"，**不该被检出设置影响**——凡按行匹配源码的断言，
  要么归一换行，要么别跨行。

### 演练（外部工具仓）

- 结论复核：在**真 CRLF 检出**的干净克隆里跑同一条构建命令，Rust 全仓 331 项全绿。
  （这就是自动出包第一个真跑的用处：本地那套加严检查看不见"检出设置"这一类差异。）

## 0.43.3 — 2026-09-14

> 命令行多一条**测试用**的备份命令：让"备份"这条链第一次能从外部机械验证。

### 测试能力（不影响作者看到的行为）

- `yanmo-cli backup --to <目录> [--keep <n>] [--tz <分钟>] [--device <名字>]`：
  立刻做一次备份并回报每个目标的结果（成功 / 跳过 / 失败 + 原因）。同样是**开发构建专属**
  （发行版里只有只读与导出，加这一条的判断在那个构建里根本不存在）。
- 为什么值得加：备份是整套数据安全里最要命的一层，而它此前**只挂在界面命令上**，
  外部工具够不着——"目标不可写会怎样""保留滚动收不收敛"两条判据一直没人能验。
- 对应验收 1 例：正常备份产出的包里该有的东西都在；目标不可达时记成**跳过并给原因**
  （不是"失败"，也不是整体崩），且两次尝试都进账本。

### 演练（外部工具仓）

- 十场演练 68/68 判据通过：新增的备份那一场验到"不可写时不假装成功、给原因、不动已写好的那份，
  恢复权限后照旧能写""连做 5 份 `--keep 3` 只留最近 3 份、别人的文件一个不碰"。
- 修掉演练台驱动层一个会造出假树的坑：把 Linux 侧路径交给 Windows 程序前要转成 Windows 形态，
  而原来的驱动层在**转不出来时静默退回原样**——于是被测程序把它当成相对路径，
  在盘根按那条路径造出一棵假树，命令还报成功。现在转不出来**当场报错**。

## 0.43.2 — 2026-09-14

> 命令行多一个**测试用**的开关：让外部工具能驱动"编辑器记账那条路"。

### 测试能力（不影响作者看到的行为）

- `yanmo-cli write` 支持 `--tz <分钟>`：给了就按作者本地时区**记进「每日码字」账本**，
  也就是**编辑器落盘走的那条路**；不给仍然只写正文（备份恢复、脚本灌数据这类
  "不是作者今天敲的字"照旧不记账）。它只存在于**开发构建**（写库命令本就不进发行版），
  加它的理由：外部演练工具要验"跨天/跨月/跨年记账对不对"，没有这条就驱动不了账本。
- 对应验收 1 例：不给 `--tz` 账本 0 行、给了 1 行、`--tz` 不是整数当场报用法错误。

## 0.43.1 — 2026-09-14

> 把「作品形态」三档逐条核对了一遍，顺手修掉一处**导出悄悄少字**的毛病。

### 修复

- **分章导出与合并导出会漏掉"单篇下面的节"**：导出按"**这种类型能不能放下级**"决定要不要往下走，
  而单篇在能力表里是叶子——于是数据里明明有下级，导出也不往下走，那一节的正文**一个字都没带出去**。
  JSON 成稿不受影响（它不按这个标志位走）——两个出口口径不一致，正是它藏了这么久的原因。
  现在两处都改成**按数据判**：有下级就往下走。验收：先写会失败的回归测试，确认它抓得住才动手。

### 核对（作品形态定调）

- 新增 5 例验收（`crates/yanmo-core/tests/work_forms.rs`）：
  - 三档形态各走一遍——零层级单篇 / 单篇 + 节 / 卷 → 章 → 节，**关库重开**之后分章导出与合并导出都得带上每个字；
  - **形态不写进表结构**：`nodes` 表没有专为卷/章准备的列（一旦有人加 `volume_id` 这类列就会被抓出来），
    且任意类型都能挂在任意父级下——深度与搭配由数据说了算；
  - 作品不是全局单例：两个不同形态的作品并存，各记各的光标、各用各的命名档，书架一视同仁列出；
  - 上面那处导出修复的回归；
  - **一条故意钉住的缺口**：定调里的「独立长文＝单篇 + 一层节」数据层已经支持（能存、能导出），
    但界面还建不出来——单篇在能力表里是叶子，而界面按这个能力位决定落点。接通它属于界面活，
    另行登记；这条测试会在接通那天先红，提醒来把两边一起改对。

## 0.43.0 — 2026-09-14

> 从成稿导入：库打不开、只剩一份备份包时，也能把整本书一个字不差地写回新库。

### 新增

- **从成稿导入（命令行 `import`）**：备份包里的 `成稿/<书名>/work.json` 是"结构与正文都在"
  的一份独立档案（当初设计它就是为了"不装研墨也读得出来"）。现在它能**读回来**：
  读这份 JSON → 新建一本书 → 把目录层级、顺序、每章正文原样写进去。
  这条路**不碰 SQLite 快照**——数据库文件彻底损坏、只剩导出时，它是最后一条回家路。
  - **默认只看不写**：不给 `--yes` 时只做"找成稿 → 验格式 → 与备份清单对账"，
    先把"这一份里有几章、多少字、跟清单对不对得上"摆出来给作者看；
  - **只新建，不动既有**：既有作品、正文、留痕一个字都不改（同名书会再建一本，绝不覆盖）；
  - **要么整本都进，要么一个字都不进**：结构与正文在同一个事务里写完；
  - **一个字都不猜**：成稿里没有的东西（每章一句话、版本历史、写作时间）不补、不编，
    所以导入的行**不计入「每日码字」账本**，只在变更留痕里记下"这一次导入"这件事——
    创作留痕是研墨的头号差异化，伪造它等于把这份数据的可信度整块毁掉；
  - **不接受"看起来像成稿"的坏 JSON**：少一格、多一格、类型不对、认不出的类型，
    一律当场拒绝并指出是哪一格（宁可让作者拿原件来问，也不导进去半本）。
- **成稿 JSON 带全"读回来需要的东西"**：同一份内容、同一份结构，导出→导入→再导出
  **逐字节一致**（四档编号规则各验一遍）。为此成稿多带三样：
  - `title_template`：作者写的**原文**（`第{$N}章 灯`）——只有它跟显示名不一样时才写这一格。
    只带显示名的话，号会在导入那一刻钉死成文字，之后插一章都不会重排；
  - `naming` / `language`：编号档与作品语言。"还没起名的卷"要按档才算得出名字
    （中文档给「第一卷」、补零档给「第001卷」），少了它读回来只能按新库的默认档渲染。
- **导入与备份清单对账**：章节数、三口径字数、分章文本指纹逐项比；
  指纹口径**只有一处**（备份时算它、导入对账用它），两处各写一遍迟早对不上。

### 修复

- **多作品备份包里的成稿 JSON 会互相覆盖**（真机上会让备份"看着完整、实际少书"）：
  包里所有作品的 JSON 都叫 `成稿/work.json`，后写的把先写的顶掉；而体检**发现不了**——
  清单里列的那个文件确实在，只是内容是别人的书。现在**一本书一个目录**
  （`成稿/<书名>/work.json` + 分章 txt），书名撞车（同名作品、超长标题截断后同名）
  补 `-<id>` 去重。老备份包照旧能读（清单里记的是相对路径）。

### 验收

- 核心 8 例（`crates/yanmo-core/tests/import.rs`）：四档编号规则逐字节往返、空章 /
  没起名的卷 / 三十二层嵌套、多本书各就各位且既有作品一字未动、导进来检索得到、
  留痕只记一笔且不计入码字、与备份清单对账（含"删掉快照只剩成稿"与"改坏成稿要报出来"）、
  十种坏 JSON 全拒且库里不留半本、新旧两种包布局都找得到；
- 命令行 1 例：干跑不动库、`--yes` 只新建、坏成稿报得出是哪一格、找不到成稿是用法错误；
- 备份 1 例：两本书各自拿到自己的成稿文件（这条先写过会失败的探针，确认过它抓得住）。

## 0.42.1 — 2026-09-14

> 正文编辑器只留「段落 + 文字」：不再有任何"按了看不出、落盘就消失"的格式。

### 修复

- **关掉正文编辑器里所有看不见的格式**：编辑器此前带着 StarterKit 的全套富文本能力——
  6 个标记（粗体 / 斜体 / 删除线 / 行内代码 / 链接 / 下划线）与 8 个块级节点
  （引用 / 有序与无序列表 / 列表项 / 代码块 / 标题 / 分割线 / 段内换行）。而研墨落盘的是**纯文本**，
  这些格式**落盘后一个字都不剩**：作者按了 `Ctrl+I` 之类，界面像改了、字数也不动——
  属于最难查的一类毛病。现在正文只剩段落与文字；撤销重做与"结尾补空段"照旧留着。
- **段内换行（Shift+Enter）一并关掉**：它是 `hardBreak` 节点，取文本时**不带它**。
  实测确认它会静默吃掉一次回车（`…然后换行：` + 换行后的字 → 两截被接到一行）。
  与其留着它偷偷丢，不如让这个键什么都不做——纯文本正文里本来也表达不出段内换行。
- 顺带的好处：**粘贴外部富文本不再带进隐形格式**（粘进来就是纯文字），
  以前"从网页粘一段、正文里悄悄多了加粗标记"的情况没有了。
- 扩展清单单独成模块（`editor/extensions.ts`），配 3 例测试：测试**去问真正的 schema**，
  而不是核对那份配置本身——配置键写错、StarterKit 换了名字、将来自增一个标记，都会红；
  另外钉住"关格式不许把撤销也关掉"。

### 验收

- 前端 206 → **209** 全绿（含 3 例新测试）、类型检查通过；Rust 312、生产构建与静态门全绿。
- "隐形改动"这件事是**实测**出来的，不是推断：搭一份带加粗与段内换行的文档，
  取文本得到 `正常一句。加粗，然后换行：换行后的字。`——标记消失、换行也被吞。

---

## 0.42.0 — 2026-09-14

> 发布链路：「这一包从哪来」开始有据可查——构建指纹 + 打 tag 自动出包 + 发版流程写下来。

### 新功能

- **构建指纹**（`dist/构建指纹-<版本>.txt`，每次出包自动生成）：这一包是哪次源码提交、
  有没有未提交改动、构建时间、平台、**用哪套工具链**（cargo / node / tauri / WebView2）、
  产物大小与 SHA256，以及两步核对方法（`certutil` 对哈希 / 按指纹里的提交自己构建）。
  我们对"可复现"的口径就写在指纹里：**不宣称逐字节一致**——Windows 安装包内含时间戳等
  非确定性内容，做不到就说做不到；给的是"同一提交 + 同一工具链 → 功能等价的产物"
  加一个可核对的产物指纹。
- **打 tag 自动出包**（`.github/workflows/release.yml`）：推 `v*` tag 就在 Windows runner 上
  跑和人一样的那条命令（工具链自检 → 版本一致 → 前端构建 → 跑测试，失败即停 → 打包 → 归集），
  上传产物、`.sha256` 与构建指纹，并用 CHANGELOG 最新一节生成发布说明。
  也可以手动触发：只构建并上传产物，不建 Release。
- **发布说明生成器**（`tools/release_notes.py`）：把「这一版做了什么」（CHANGELOG 里人工写的）
  与「拿到文件怎么核对」（每次都一样、机器生成的那一段）拼在一起——避免手抄时漏掉
  "校验和"或"没签名怎么继续"。
- **发版流程文档**（`RELEASING.md`）：一条命令出包、打 tag 自动出包、发布前检查单、可复现口径，
  以及**发出去之后发现严重问题怎么办**——明确写"不许用同名文件悄悄替换"
  （早下载与晚下载的人拿到同名不同哈希的文件，谁都说不清谁是对的），只能发新版本并写清影响范围。
- **版本一致性检查加上 README**：出包脚本现在要求 Cargo 工作区、应用配置、CHANGELOG 首条、
  README 的「当前版本」**四处一致**，不一致就不出包。这正是踩出来的：README 那处最容易忘，
  靠检查单写一句"记得改 README"不如靠机器拦住。

### 验收

- 构建指纹实跑验证过（本机出包，指纹里如实标出"工作区有未提交改动"）；
  发布说明生成器实跑验证过（取到 CHANGELOG 最新一节）；
  版本一致性新增 README 后做过反证：故意把 README 改旧，当场报出旧版本，还原后通过。
- 静态门全绿（公开内容脱敏 0 命中）、Rust 312 / 前端 206 / 生产构建全绿。

---

## 0.41.9 — 2026-09-14

> README 再改一稿：**先讲人话**——它是什么、帮你什么、不做什么、怎么护住稿子、以后会有什么；
> 讲"可靠性"的那一节改成普通人看得懂的凭据，而不是让人自己去跑命令。

### 变更

- **README 按"人话优先"重排**：
  - 开头一段说清四件事（是什么 / 帮你什么 / 不做什么 / 怎么护稿 / 未来有什么），并保留诚实的开发中声明；
  - 「能帮你什么」按**你遇到的麻烦**列（章节多翻不动、手滑删了、怕丢稿、要交稿、分心、界面打不开），
    而不是按模块名列；
  - 「不做什么」单列：不自动生成正文、不上云、不搞付费墙、不偷偷改你的稿子；
  - 「怎么保护你的宝贵数据」写成四步（稿子在哪一眼可见 → 写的时候不用管 → 关窗崩溃也不赌 → 备份与搬家），
    做不到的部分指向安全文档；
  - **「它凭什么可靠」改成普通人能看懂的六条**：出包先跑 518 项自动检查（过不了就不出包）、
    专治最坏情况的检查、专治偷偷联网的检查、手滑三条退路、中文字数按中文规矩算、
    以及"可以不信任我"（源码公开 + 一键构建 + 校验和）；不再贴 `cargo test` 之类的命令；
  - 「版本与下载」补齐：当前版本、Releases 链接、`certutil` 核对校验和、一键构建脚本做什么、
    模块开发工具等模块生态实装后再放。
- **CONTRIBUTING.md 的「开发须知」补齐**（README 指向它，之前那边其实没有）：前置条件逐项
  （含 VS Build Tools 的工作负载与 MSVC v143、Windows SDK）、跑起来、**两条跑测试的命令**、
  出安装包与两个本地参数；技术细节归到贡献者文档，README 保持人话。

### 验收

- 静态门全绿（公开内容脱敏 0 命中）；Rust 312 / 前端 206 / 生产构建照旧。
- 文档里的数字与断言都对得上现状：518 项自动检查（312 + 206）、停笔 0.25 秒落盘、
  每 2–5 秒核对指纹、三条退路（回收站 / 版本历史 / 备份）各自独立。

---

## 0.41.8 — 2026-09-14

> README 拆成两条路：来写小说的人看得懂，来改代码的人找得到；未签名的提示也写清了怎么继续。

### 变更

- **README 重写成「使用者 / 开发者」两段**（以前基本是给开发者看的架构说明）：
  - 顶部先给**诚实的现状**：地基（编辑器 / 数据安全 / 备份 / 导出 / 编译）已经能用，
    「叩问」与「创作留痕」两条线**尚未发布**（原文写得像已经有了），并劝一句"正式稿件同时留一份在别处"；
  - **使用者三步**：下载哪个（安装版 / 便携版，都附 `.sha256`）、怎么装（按当前用户、无 UAC、
    只写标准卸载信息与快捷方式、**不加开机自启**、末页「运行研墨」可取消）、
    首次启动会看到什么（先问稿子放哪 → 建第一本书 → 顶栏小字就是数据目录）；
  - **未签名怎么继续**：SmartScreen「更多信息 → 仍要运行」两步；为什么暂时不买证书；
    以及"源码公开 + 可自己构建"这条比签名更硬的凭据，附两条自查命令；
  - **常见坑表**：白屏（开发模式产物去连本地开发服务器）、杀软误报、稿子在哪、手动备份、
    重装或换机、卸载不删稿；
  - **开发者路径**：前置逐项（VS Build Tools 的工作负载 + MSVC v143 + Windows SDK）、跑起来、
    **跑测试**（`cargo test --workspace` / `npm test`）、一条命令出安装包。
- 「想做的三件事」一节标注了状态（地基已能用 / 叩问与创作留痕开发中），与首屏的现状一致。

### 验收

- 文档里关于安装行为的每条断言都对过真实配置与打包模板：当前用户安装、中英文向导、
  卸载信息、**末页运行勾选框确实存在**（初稿写反了，核实后改正）、无开机自启；
- 静态门全绿（公开内容脱敏 0 命中）；Rust 312 / 前端 206 / 生产构建照旧。

---

## 0.41.7 — 2026-09-14

> 把「我们保证什么、做不到什么」写清楚：安全文档扩成四问，应用里加了「关于」。

### 变更

- **安全文档从三问扩成四问**（保证什么 / 做不到什么 / 失败时怎么办 / 怎么自己验证）：
  - **七层防线**逐条写明"它挡住什么事、你怎么看见它"：写前先存快照、落盘后核对指纹、
    核不上就先留底再回写、关窗先过闸门、多处备份 + 写回体检、回收站与软删、
    界面起不来时的命令行取稿；另列三条性质：单实例锁、数据位置自己定、命令边界机械可见。
  - **做不到的坦白清单（八条）**：备份包不加密、单库仍是单点、最后约 250 毫秒的输入可能丢、
    强杀进程会丢手上那一份、不替你判断正文对错、没有云同步与自动更新、
    Windows 上数据默认在用户目录、安装包未签名——每条都写"意味着什么 + 你该怎么办"。
  - **失败模式与应对**：存不下去 / 库与手上这份不一致 / 上次不是正常退出 / 界面白屏 /
    库损坏打不开 / 时间被改 / 提示已经在运行。
  - **接口清单与默认状态**：壳与核心的命令通道、命令行入口、本机协议口（默认关、未实现）、
    出网能力（无）、自动化能力（不进发行版）。
  - **零出网的三步自验**：看依赖树 / 跑守卫 / 断网实测；并说明两处机械守卫盯着它，
    其中一处会**把注释剥掉再判**，所以"将来会做 WebSocket"这类说明不会被误报。
- **应用内「关于」**（设置面板最后一组）：安装包版本 + 引擎版本 + 库结构版本一行报全、
  稿子路径（可点开文件夹）、承诺与限制两句摘要 + 指向安全文档的说明。
  它经前端网关取数（组件不直接碰 Tauri），版本读不到只影响那一行，不会把整块打成错误。

### 验收

- 前端 206、Rust 312、类型检查、生产构建与静态门全绿；公开内容脱敏 0 命中
  （新增文档与界面文案都过词表）。
- 文档里每条承诺都对得上代码里的实现（备份读回体检 / 关窗闸门与逃生导出 / 单实例锁 /
  快照保留 / 命令行只读），没有写"以后会有"的东西。

---

## 0.41.6 — 2026-09-14

> 许可与分发边界上锁：内部测试工具不许流出去、安装包不许夹带、出货代码不许出网。
> 界面一个字没变——这一版是给「开源核心 + 专有增值」这套结构焊边界。

### 变更

- **内部工具留在仓外**（新增机械守卫）：`.gitignore` 必须挡住内部测试工具目录，
  并用 `git ls-files` 的真实清单核对——跟踪列表里出现工具路径就红。
  这类工具比本体更值钱，一旦进了公开仓就撤不回来（AGPL 会要求一并公开）。
- **安装包不许夹带工具**：打包配置里不许出现 `resources` / `externalBin` / `files`
  （那等于把仓库别处的文件一起塞进安装包），图标等资源不许用 `..` 跳出应用目录，
  `frontendDist` 与构建命令也不许指向内部工具。
- **出货代码零出网**（新增机械守卫，给对外承诺做背书）：扫核心 / 命令行 / 协议 / 桌面壳的源码
  （**注释剥掉之后再扫**，「将来会做 WebSocket」这类说明不算能力）与全部 `Cargo.toml`，
  不许出现套接字、HTTP 客户端、`"curl"` / `Invoke-WebRequest` 这类口子。
  README 里「用防火墙或抓包验证不联网」这句话，从此有一条随测试跑的机械依据。
- **Clock 注入点仍是编译期隔离**：守卫钉住「必须挂在编译期开关后面」——
  发布构建里那段代码根本不存在，时间戳不可伪造（这是「创作留痕能自证」的前提）。

### 验收

- 新增 4 例守卫（内部工具不入公开仓 / 安装包不带工具 / 零出网 / 时钟隔离），
  Rust 308 → **312** 全绿；前端 206、类型检查、生产构建与静态门全绿。
- 两条守卫都验过「真的会红」：投一个套接字 + 调外部下载命令的探针，以及一个链接核心的探针，
  均当场报出文件与行号，探针已删。
- 内部工具的独立仓另建完成（专有许可、不链接核心、只经命令行驱动），其自查 9 例自测全绿。

---

## 0.41.5 — 2026-09-14

> 首发前的防灾网：危险的写路径不许顺手写出来，跨版本升级与「只读一遍」都上机械断言。
> 界面一个字没变——这一版全是给"发布之后别把稿子写炸"上锁。

### 变更

- **危险写路径的机械守卫**（进 `cargo test`，出包前的构建必然跑到）：扫 `crates/*/src` 与 `app/src`，
  四条规矩——① `DROP TABLE` 一律禁止（`temp.` 探测表除外）；② `DELETE FROM` 稿件表
  （`nodes` / `node_contents` / `works` / `snapshots`）要过白名单；③ 不带 `WHERE` 的 `DELETE`/`UPDATE`
  要过白名单（upsert 的 `ON CONFLICT ... DO UPDATE` 不算）；④ `remove_dir_all(` 要过白名单。
  白名单**逐条写清"为什么不伤稿件"**，而且是**棘轮**：多一处、少一处都会红（逼人回来看一眼）。
  守卫自己也有测试（认得出这些形状、放得过安全写法、注释里的示例不误报），并钉住"至少扫到 20 个文件"
  以免路径改名后空转着全绿。
- **跨版本升级不丢稿**（`upgrade_safety.rs`）：程序化造一份"上一版 schema"的库——两本书、
  卷章树、正文（中文/标点/换行/英文数字）、三档字数、全局与每书两层偏好、每章快照、回收站、
  码字账本——退回 v6 形状后用当前引擎打开，逐项断言：**正文逐字节一致**、树结构与阅读顺序一致、
  三档字数一致、偏好与回收站与快照一个不丢。
  另加一条**迁移重跑**：只退版本号再打开（真机上"升级到一半被杀"就长这样），账本与正文同样一个不丢。
- **只读不动库**：打开后把"只读的一遍"跑全（目录树 / 逐章正文 / 检索 / 两种导出 / 回收站 / 体检），
  再比**每张表每一行**的内容快照——一个字节都不许变。判据用逻辑快照而不是文件字节：
  库是 WAL 模式，关库时的 checkpoint 会动主库文件（那是存储引擎的正常收尾），按字节比会误报。

### 验收

- 新增 6 例（守卫 3 例 + 升级/只读 3 例），Rust 302 → **308** 全绿；前端 206、类型检查、
  生产构建与静态门全绿。
- 守卫的"真的会红"也验过：往 `app/src` 投一个 `DELETE FROM nodes` 探针，守卫当场报出文件与语句，
  探针已删。

---

## 0.41.4 — 2026-09-14

> 底栏那行诊断信息收干净：只留作者用得上的三样，内部版本号收进悬停提示。

### 变更

- **徽标从 `engine 0.41.3 · proto v1 · data v1` 改成 `v0.41.3 · 库结构 v7`**（引擎 / 协议 / 数据格式收进悬停提示）：
  - `proto v1` 是本机协议版本，那个协议**现在还没实现**（只为第三方壳与闭源模块预留）——摆在这儿对作者没意义；
  - `data v1` 是**磁盘镜像与导出文件的格式**版本（与库结构是两件事），现在恒为 1；
  - 换成真正该看的**库结构版本**（数据库迁移版本，现在 v7）：它决定"这个库能不能直接打开、要不要迁移"。
    这一项核心早就报给界面了（`DataHome.schema_version`），只是以前没显示。
  - 版本号改为取**安装包版本**（`app.getVersion()`），与引擎版本区分开；读不到就退回引擎版本。
- 稿子路径与"点一下打开文件夹"照旧。

### 验收

- 前端 206 项全绿（本轮只动徽标显示与文案）；类型检查、生产构建与静态门全绿。

---

## 0.41.3 — 2026-09-14

> 改名不再见到函数宏：输入框里只编辑你自己的章名，号仍由核心按位置渲染。

### 修复

- **改名框不再摊出宏代码**：以前改名时输入框里放的是标题原文（`第{$N}章 复活节雕塑` 这种模板），
  一删就把编号删了，写作界面上也不该出现 `{$N}` 这种东西。现在输入框里**只有作者的名字**，
  前面用灰字标出渲染后的号（`第1章`）；宏骨架原样写回、一个字符都不动（号仍是位置的函数）。
  自起名（`序章` 这种本来就不占号的）照旧整串可编辑。
- **改完名树上立刻是渲染后的名字**：以前改名只就地改这一行、不重拉，而显示用的 `title_rendered`
  是核心渲染出来的——于是改完名树上挂着旧名字或原文模板。现在改名后**重拉它所在那一层**：
  改宏的有无会连带同层后面每一章的号，这一层必须重算。
- 标题的拆/合单独成模块（`editor/title-edit.ts`）：别的档位（中文数字 / 补零 / 卷）、
  宏写在中间的写法、拆了再合的往返一致性，都有测试钉住。

### 验收

- 前端 196 → **206** 全绿（新增 10 例标题拆合；改写那条"改名后重拉"的树测试，并让替身返回
  渲染后的标题好分辨"拉回来的"与"界面自己拼的"）；Rust 302、类型检查、生产构建与静态门全绿。

---

## 0.41.2 — 2026-09-14

> 专注模式的大纲卡片改成"浮在常驻留白里"：开关卡片时正文**一个换行点都不动**。

### 修复

- **专注态正文宽度恒定**：上一版让卡片占一列（正文让开），代价是开关卡片时正文重排——
  换行点整个变，真机反馈"观感不好"。现在专注态**常驻一条左侧留白**（`--zen-gutter`：
  窗口宽度的 24%、最多 324px），正文行宽按"窗口宽 − 两侧留白"算，卡片浮在这条留白里：
  开卡片只是"从留白里浮出来"，正文宽度与换行点全都不变。
- 窄窗口下留白按 `24vw` 收，卡片宽度跟着留白走（`calc(--zen-gutter - 24px)`）——
  小窗口里正文不会只剩一条，卡片也不会压到字。
- 留白**常驻**（不是卡片开着才有），这正是"不动"的前提：只有进出专注时才重算一次行宽。

### 验收

- 前端 196 项全绿（本轮只动布局与行宽，判定逻辑没改）；类型检查、生产构建与静态门全绿。

---

## 0.41.1 — 2026-09-14

> 两个真机反馈的修：换口径后目录里的章字数不刷新；专注时大纲卡片压住正文。

### 修复

- **目录里的章字数现在跟你的口径走**：以前落盘后只更新一个口径（按词），而树上显示哪个数是由
  你选的档位决定的——选「字」的时候，那个数就停在"上次重拉目录"的旧值上
  （真机上看到的是：状态栏 206 字，树上还写着 111）。现在落盘后**三个口径一起更新**，
  卷那一行「4章 · N」的合计也跟着差额走；容器行的合计与章行**统一口径**
  （不再出现"206 字旁边写着 200 词"）。
  顺带把 `TreeRow` 的类型改成**直接继承核心那份节点类型**：以前这里手抄过一份字段表，抄漏了
  `char_count` 等字段——运行时读得到、类型上"没有"，这类毛病在类型层面也照不出来。
- **专注模式下的大纲卡片不再压住正文**：卡片改成**占左边一列**（正文让开），不再浮在正文之上。
  窗口态正文左右各只剩 10% 边距，浮层必然压字；全屏时因为 64em 封顶生效、边距大，所以没暴露出来。

### 验收

- 前端新增 2 例：落盘后行内三个口径与卷的三个合计一起更新；容器行合计跟随口径。
  另把"没加载过的那一层更新不到"的单测钉住（展开时按库里的数进来，不带着旧账），
  前端 194 → **196** 全绿；Rust 302、类型检查与生产构建全绿。

---

## 0.41.0 — 2026-09-14

> 专注模式再走一步：正文跟着窗口放宽（有上限），大纲变成"唤出来看一眼"的悬浮卡片。

### 新功能

- **专注 / 全屏时正文行宽跟着窗口放宽**：常规态仍是 42em（约 42 个中文字——中文长文最舒服的行宽）；
  专注或全屏时放到窗口的 80%、**封顶 64em**（再宽，眼睛回行就开始串行）。只改显示，不进导出。
- **大纲悬浮卡片**：专注时左上角一个「大纲」小按钮，点一下把目录树唤成浮在正文上的卡片——
  再点收起，卡片右上角「关闭」也能关。卡片里就是**原来那棵目录树**（不另写一份），
  改名、新建、拖动、切章照常可用；卡片浮在正文之上**不挤压正文**。
- 收起卡片后**焦点自动回到正文**：焦点留在按钮上的话输入法没有落点，作者看到的现象是"打不出中文"
  （这条在焦点策略里踩过，别再踩第二遍）。
- **`Esc` 改成分层退**：卡片开着先关卡片，再按一次才退专注 / 全屏——一下按掉两层不是作者的预期。
- 叩问栏的卡片等叩问状态机落地再接进同一个容器（现在那边还是占位，不做空壳）。

### 验收

- 前端新增 5 例：卡片"一次只开一张 / 再点收起 / 关掉是关掉"、`Esc` 分层退（卡片开着时只关卡，
  且卡片里输入框"取消输入"的语义不被抢）；前端 189 → **194** 全绿，类型检查与生产构建通过。
- 快捷键判定上下文多了一项（卡片开着没有），键位表与"不抢键"四条分寸不变。

---

## 0.40.0 — 2026-09-14

> 写字的时候只留正文：专注模式（F9），外加一套全局快捷键。

### 新功能

- **专注模式**（顶栏「专注」或 `F9`）：收起目录栏与叩问栏、顶栏也一并收掉，只留正文与编辑器那条细条——
  章名、字数、今日进度、保存状态照旧一眼可见。退出按钮就挂在章名那一行（焦点退路不止一条），`Esc` 也能退。
  它是"当下这一会儿"的状态，**不落盘**：重开软件回到常规三栏，不会出现"一打开就找不到侧栏"。
- **一套全局快捷键**（键位只在 `editor/shortcuts.ts` 里定义一次，将来做自定义面板改的就是这张表）：
  - `F11` 全屏开关（只改窗口，不进偏好）
  - `F9` 专注模式开关
  - `Ctrl+Alt+←` / `Ctrl+Alt+→` 上一章 / 下一章（按阅读顺序跨卷；走正规切章通道——先落盘，再换章）
  - `Ctrl+S` 立刻落盘（不等防抖；内容没变时是空转，可以放心按）
  - `Ctrl+N` 在当前章后面新建一章并开写（与目录树的「+」同一条路，不另开一套流程）
  - `Ctrl+,` 设置 / `Ctrl+B` 书架 / `Esc` 回到常规（专注与全屏哪个开着收哪个）
- **绝不抢作者的键**（每一条都有测试钉住）：输入法组字中一律不认；焦点在输入框里时功能键让位
  （带修饰键的组合照认）；弹窗开着时界面快捷键全部让位（`Esc` 归弹窗自己）；长按不连发；
  单个字母与数字一律不劫持。

### 验收

- 前端新增 25 例：专注态"露哪几块"、键位匹配矩阵（组字 / 输入框 / 弹窗 / 长按 / 大小写 / 修饰键对不上）、
  接线链路（命中就拦下默认行为、不认的键不拦、解绑干净、**表里加了键却没接线就报错**）；前端 164 → **189** 全绿。
- `core:window:allow-set-fullscreen` 逐项写进权限集（`core:window:default` 只给"读"，没给"写"），
  Rust 边界测试照旧全绿——界面仍然不碰文件系统。
- 按钮上的键位提示从表里生成（文案写 `{key}`），改键位提示跟着走，不会两处对不上。

---

## 0.39.2 — 2026-09-14

> 图标收个角：直角换成 22% 圆角、四角真透明，跟桌面上的邻居们一个语言。

### 变更

- **应用图标改成圆角**：方形源图先裁成圆角矩形（半径 22%，四角透明、边缘抗锯齿）再成套生成——
  Windows `.ico` 六档、PNG 32 / 64 / 128 / 128@2x、方形标与商店标全部随之更新。
- 桌面、开始菜单、任务栏、安装器与卸载器用的是同一张圆角图。
- **只有图标变了**：功能、数据结构、老稿与设置一字未动，覆盖安装即可。

### 验收

- 圆角母图四角 alpha = 0（真透明）、边缘有半透明过渡；成品 PNG 均为 RGBA，`.ico` 六档齐全。
- 静态门全绿：版本四处一致（0.39.2）、内部资料未入库、公开内容脱敏、界面文案守卫无回潮。

---

## 0.39.1 — 2026-09-14

> 换一张脸：应用图标改成「研墨」水墨图（砚台 + 毛笔 + 印章）。

### 变更

- **新应用图标（全套统一替换）**：Windows `.ico` 内含 16 / 24 / 32 / 48 / 64 / 256 六档，
  PNG 出 32 / 64 / 128 / 128@2x，另有方形标与商店标各尺寸。
- **安装包自己也换了脸**：NSIS 安装器与卸载器的图标一并指向应用图标——下载与安装时看到的
  不再是一个与「研墨」无关的默认标；可执行文件、开始菜单、任务栏与窗口共用同一张图。
- **只有图标变了**：功能、数据结构、老稿与设置一字未动，覆盖安装即可。

### 验收

- 图标用同一张源图经 Tauri CLI `tauri icon` 一次成套生成（各尺寸同源、边距一致，不手搓）。
- 成品逐项验过：可执行文件里嵌的 256px 图标数据与 `icons/icon.ico` 逐字节一致；
  安装器与卸载器的关联图标已从 NSIS 默认标换成应用图标。
- 静态门全绿：版本四处一致（0.39.1）、内部资料未入库、公开内容脱敏、界面文案守卫无回潮。

---

## 0.39.0 — 2026-09-13

> 建书时一次问清：书名、作品类型、简介、卷/章命名规则——填完直接开写。

### 新功能

- **建书页**（书架点「+ 新书」）：一页问全四样——
  **书名**、**作品类型**（长篇 / 短篇集 / 单篇）、**简介**（可以以后再补）、
  **卷 / 章命名**（跟随设置 / 第1章 / 第一章 / 第001章 / 不编号）。
- **单篇与短篇集不显示"卷/章命名"**：那两类本来就不编号，页面换成一句说明
  （"名字由你自己起"），免得多带一个用不上的覆盖进库。
- **「跟随设置」会标出当前设置是哪一档**（例如「跟随设置（第一章）」）——一眼知道不选会是什么样；
  想给这一本单独一套再选别的，写进去的就是这本书自己的覆盖。
- 简介与命名规则**建书时一次落好**：作者不用建完再去别处补；建完直接切过去开写。
- 命名规则只影响以后新建的条目，已有名字一个字都不会被改（与设置里那条铁律一致）。

### 验收

- 前端新增 `shelf` 接线 3 例：建书页填齐时按 **建 → 简介 → 命名规则 → 开写** 的次序落库；
  空简介与「跟随设置」不写多余的覆盖；简介两头空白不算内容。
- 核心 302 项、前端 164 项、生产构建全绿；界面文案守卫仍在基线。

---

## 0.38.0 — 2026-09-13

> 写完三十章之后想换编号风格？设置里改一下，再点一下——**已有章节一起换成新写法**。

### 新功能

- **把整本书的编号写法一起换掉**：设置 → 新建条目里改好规则后，面板上会出现
  「把《这本书》已有的 32 章一起换成这个写法」；点开**先看清单**（`第1章 灯 → 第一章 灯` 逐条列出），
  确认后才动手。`第{$N}章` ⇄ `第{$N_ZH}章` ⇄ `第{$N:3}章` 三种写法随时可以互相换，也能换回去。
- **只换编号，不动章名**：`第{$N}章 灯` → `第{$N_ZH}章 灯`——后面写的字一个字都不动；
  手写的阿拉伯数字（`第12章 灯`）也会被收编成宏；**没有编号的一个都不碰**
  （`序章` / `楔子` / `番外` / 散文集里自起的名字）。
- 选「不编号」时这个动作不适用（只影响以后新建的条目）：已有章节的名字同样一个字不动。
- 偏移与「从 0 起」这类**作者对某章的特意安排**在换档时保留（`{$N+49}` → `{$N_ZH+49}`）。

### 验收

- 核心新增 `naming_rewrite` 套件 3 例：三十章整本换档（预览列全、执行后按位置显示 `第一章…第三十章`、
  换回去也干净）、手写数字被收编而 `序章`/`番外` 一个字不动、已经是这一档或选了「不编号」时清单为空。
- `numbering` 套件补两例：换档只动宏、保留偏移/从 0 起；手写阿拉伯数字换宏（中文数字手写体不认，原样不动）。
- 前端补 `appearance` 接线套件 3 例（写哪一层、改完重算清单、执行时原样交预览那份）。
  核心 302 项、前端 161 项、生产构建全绿。

---

## 0.37.0 — 2026-09-13

> 设置里能选"新建条目叫什么"了：阿拉伯数字 / 中文数字 / 补零 / 不编号，**全局默认 + 每本书可覆盖**。

### 新功能

- **新建条目的命名规则**（设置 → 新建条目）：四档——`第1章`、`第一章`、`第001章`、**不编号**
  （名字留给作者自己起）；「作用范围」可以选"所有作品的默认"或"只设《当前这本书》"。
- **默认按作品类型**：没选过时长篇给号、单篇与散文集**不编号**（散文随笔里硬塞"第N篇"只会碍事）；
  想统一改成别的，改一次全局默认即可（单本再单独覆盖）。
- 规则落定写在核心一处（`Store::naming_style`）：作者选过 → 听作者的；没选过 → 跟着**作品类型**走。
  选了「不编号」时新建的条目标题留空，界面按语言显示占位，作者一起名就覆盖。
- 规则会真的贯穿到新建的条目上：库里存的是对应模板（`第{$N}章` / `第{$N_ZH}章` / `第{$N:3}章`），
  显示时按位置渲染——所以改规则只影响**以后新建**的条目，已有名字一个字都不动。

### 验收

- 核心新增两例：① 命名规则跟着作品类型走、作者选了就听作者的、每本可各自覆盖、
  传 `auto` 清掉覆盖回到默认、乱写的代码被明确拒绝；② 四种规则新建出来的章，
  库里的模板与显示出来的样子都对得上（`第{$N}章`→`第1章`、`第{$N_ZH}章`→`第一章`、
  `第{$N:3}章`→`第001章`、不编号→空标题）。
- 核心 297 项、前端 158 项、生产构建全绿；界面文案守卫仍为基线（宏语法在界面上按"代码语法"豁免）。

---

## 0.36.0 — 2026-09-13

> 章节编号换了个主人：**号不再写死在标题里**，而是"标题模板 + 按位置渲染"（`第{$N}章` → `第3章`）。

### 新功能

- **自动编号宏**：标题里写 `第{$N}章 灯`，目录树、编辑器章名、上一章/下一章、编译与导出统一渲染
  成 `第3章 灯`。语法：`{$N}`（同层序号）、`{$N0}`（从 0）、`{$N+49}`（偏移）、`{$N_ZH}`（中文数字
  `第一章`）、`{$N:3}`（补零 `001`）、`{$N_RESET:101}`（本卷从 101 起号，与 `{$N}` 写在同一行）。
- **号 = 位置的函数**：中间插一章、删一章、拖动、从回收站捞回，号都会自己跟着位置走——
  **再也不需要"归位"或"改名补号"**。作者自己写名字的（`序章` / `楔子` / `番外` / 散文集里的一篇）
  **不占号也不推号**；卷这类容器按位置排（起了名字也照样占号）。
- **新建条目的默认名改成模板**：长篇的卷 / 章 / 节分别是 `第{$N}卷` / `第{$N}章` / `第{$N}节`；
  单篇与散文集**不编号**（名字留给作者，界面按语言显示占位）。建书时留白的第一卷也由核心渲染成
  「第1卷」——"第几卷"只在一个地方算。

### 移除

- **「删章路标」整个功能下线**：它的存在理由是"删掉第 N 章留下一个缺号"，而新模型里不存在缺号
  （删掉第 3 章，后面那章显示出来就是第 3 章）。连同它的弹窗、命令与文案一起删掉。
- 为旧模型加的补丁一并删掉：中文数字**解析**（壹/貳/兩/百/千/全角那一套）、按号归位、
  "新建时扫描同层取最大号 + 1"、建章的双规则。相关文件从 492 行降到约 310 行。

### 验收

- 新增 `numbering` 套件 7 例：按位置计数 / 偏移与从 0 / 中文数字 / 补零 / 重置起号 /
  不认识的 `{...}` 原样不动 / 中文数字的读法（`101` → `一百零一`、`1234` → `一千二百三十四`）。
- **创章随机压测**重写为盯新模型：固定种子 600 步随机"建章 / 删章 / 捞回 / 改名"，每一步都断言
  "要编号的章渲染出来正好是 1、2、3…n"、"同层次序号密集"、"显示出来的标题里不许留着宏"；
  另有四条定向压测（同一行连点二十次、删中间章再建再捞、把某章改名成序章后整体前移、
  分卷各数各的）。
- 核心 295 项、前端 158 项全绿；生产构建通过。

---

## 0.35.5 — 2026-09-13

> 创章这条线重做了一遍：**号与位置不许再各说各话**，并补上创章流程的随机压测。

### 修复

- **建章按号归位**：原来"号取同层最大 +1、位置插在点的那一行后面"，两条规矩各走各的——
  同层是 18/19/20/21 时在第20章上点「+」，号取到 22 却插在 20 后面，屏幕上成了
  `20 / 22 / 23 / 21`（真机报的"排序又乱了"）。
  现在：**标题留空（号由核心取）→ 按号归位**（落在同层最后一个有编号的章之后）；
  **作者自己写了标题 → 点哪儿插哪儿**（序章 / 番外 / 第X章·补 都听作者的）。
- **捞回来也按号归位**：从回收站恢复原来照抄"删除前那个 sort_order"，中间又删过、建过之后
  那个位置早就过期了——压测第 8 步抓到的正是这个（捞回第19章却排在 20/21 后面）。
- **没有编号的那一层（散文 / 文集）不再被"第N篇"接管**：这一层一个编号都认不出时，
  新条目插在**点的那一行后面**（原来会跑到整层最上面）。
- **认号加宽**：`壹` / `貳` / `兩` / `參` 等大写与繁体、`七十八` / `一百二十` / `两千零五`
  这类含百千的组合、以及中文输入法下的全角数字 `第１０章`，原来一律认不出（会被当成"没编号"，
  取号退回按数量算、位置也归错）。瞎写的组合（`十十` / `一百百`）仍旧不认——宁可不认，也不猜错。

### 验收

- 新增**创章流程随机压测**（`tests/chapter_flow.rs`）：固定种子 600 步，随机"建章 / 删章 / 捞回 /
  改名"，**每一步之后**都检查"编号不许倒退、同层次序号必须密集"；另加两条定向压测
  （同一行连点二十次「+」、删中间章再建再捞）。把建章规则改回旧写法，这套压测**第 1 步就失败**。
- 章节套件 13 例、核心全仓 24 个套件全绿；前端 171 例通过。

---

## 0.35.4 — 2026-09-13

> 今日达标后不再常驻一条满格的进度条——它让位给一个「达标」小记号（占的是同一个槽位）。

### 修复

- 满格的进度条是一条**没有信息量的常驻占位**（真机提的）：今天写够了目标之后，它就一直挂着。
- 现在达标后进度条让位给一个短记号「达标」（今日那串数字本来就会变强调色）。
  ⚠️ 记号**沿用进度条那 36px 的槽位**：槽位不变，作者写到目标那一刻整条状态栏不会重排。

### 验收

- 界面守卫补一条：达标记号不许自己另定宽度（必须沿用进度条的槽位），免得改这里时又把
  "达标瞬间整条栏抖一下"带回来。边界套件 8 例、前端 171 例与生产构建通过。

---

## 0.35.3 — 2026-09-13

> 状态栏回到原来的紧凑排布：格与格之间恒是那 10px，不再"隔开太多"。

### 修复

- 0.35.2 为了治左右跳动，给状态栏**每一格**都钉了宽度，多出来的宽度正好落在格与格之间——
  看着明显稀疏了（真机反馈"隔开太多，没之前顺眼"）。
- 现在**只钉住唯一频繁变长变短的那一格**：保存状态（`待落盘…` 4 字位 ↔ `已保存` 3 字位），
  而且那格内容**左对齐**——多出的那点宽度落在状态栏右缘（看不见）。字数 / 语言 / 今日一律
  按内容自然排，格与格之间恒为 10px；它们只在进位数（9→10）或设 / 清目标时才变宽，且
  **往左长**，右边邻居与前面那排按钮都不动。

### 验收

- 界面守卫改成双向盯：保存状态那格必须固定宽度 + 左对齐；字数 / 语言 / 今日**不许**钉宽度
  （一并拦住"隔开太多"）。边界套件 8 例、前端 171 例与生产构建通过。

---

## 0.35.2 — 2026-09-13

> 状态栏不再被文案长短推着晃：「待落盘…」与「已保存」来回切时，整条栏不再左右跳。

### 修复

- 状态栏右边那一组（字数 / 语言 / 今日 / 保存状态）原来宽度随文案变：保存状态在
  「待落盘…」（4 字位）与「已保存」（3 字位）之间来回切、字数每敲一下涨一位，整组就跟着变宽变窄，
  把前面那排按钮（版本 / 排版 / 一句话）一路推着小幅左右跳——正写着字的人会被晃到。
- 现在每一格**钉死最短宽度 + 右对齐**（要变长只能往左长，右边界不动），今日那格另给
  「设过目标」留更宽的一档；长度变化只发生在设 / 清目标那一刻，不跟着击键抖。

### 验收

- 界面守卫新增一条（`app/tests/boundary.rs`）：状态栏每格必须声明固定宽度、状态那格必须是能拿
  宽度的盒子、共用规则必须 `flex: none` + 右对齐 + 不换行。这类毛病只有真机连续打字才看得出，
  交给机器盯着。边界套件 8 例全绿；前端 171 例与生产构建通过。

---

## 0.35.1 — 2026-09-13

> 修「每日目标设置了不保存」：点「存下」看着没反应、面板里还是"还没设目标"。

### 修复

- 目标输入框是 `<input type="number">`，而 Vue 的 `v-model` 挂在这种输入框上时会给回
  **数字**（runtime-dom 里 `castToNumber = number || props.type === "number"`）。
  原来按字符串处理（`.trim()`），作者一点「存下」就抛异常，界面上一片安静——看着就是"不保存"。
- 现在解析收进一个纯函数（清空 / 非正数 / 认不出来一律当"不设目标"），并**专门钉了数字入参
  那条回归**：这条路径只有真机上点一次才会暴露，必须由测试盯着。

### 验收

- 前端新增「每日目标」套件 4 例（数字入参回归、字符串/小数/负数/空值解析、
  设本书目标与设默认目标的写入层次与回读、没有书时不瞎写）；前端 171 例全绿。

---

## 0.35.0 — 2026-09-13

> 每天写了多少，从今天起有本账：状态栏一行「今日 1,234 / 2,000 字」+ 码字日历
> （热力月视图、连续天数、本周/本月合计）。每日目标可以全局设一个，也能每本书单独设。

### 新功能

- **每日码字账本**（核心新增 `writing_days` 表与迁移 v7）：编辑器每次落盘时，把这一笔
  **净增减**记进当天——与正文写入**同一个事务**，两个都成或都不成。快照回滚、备份恢复、
  导入这类"不是今天敲出来的字"走另一条路，**不记账**（否则恢复一章旧稿就会在日历上凭空多一笔）。
  逐字（含标点）/ 逐字（不含标点）/ 按词**三个口径各记一列**，历史不会因为以后换口径而变意思。
- **今日进度**：编辑器状态栏字数旁显示「今日 …」，设过目标就画一条细进度条（当天写完目标就
  换个颜色，超额封顶不满出格子）；跨零点后下一笔落盘自动刷新，不另开定时器。
- **码字日历**：点状态栏那行打开——本月热力网格（周一起算）、连续码字天数、本周与本月合计；
  可以切「本书 / 全部作品」看。
- **每日目标**：全局一个默认 + 每本书可单独覆盖（复用外观偏好那套"全局打底 + 每书覆盖"），
  面板里能选"只设这本书"还是"设为所有作品的默认"，也能一键"跟随默认"。

### 验收

- 核心新增 `writing_days` 套件 8 例：只有编辑器落盘记账 / 净增减（删字扣回）/ 零增减不落行 /
  按作者时区分桶 / 多本书各记各的与合计 / 区间闭区间且跳过没记录的日子 /
  连续天数（今天没写从昨天数、断档就停、净删的一天不算写）/ 目标全局与每书覆盖；
  `time` 另加日期正反换算与"读不动的日期当没有" 2 例。
- 前端纯函数 11 例：口径取数、没目标不画进度、超额封顶、本地日期键、闰年与跨月、
  周一起算、月视图补白、热力分档；Rust 全仓测试与前端 167 例全绿。

---

## 0.34.6 — 2026-09-13

> 「补写这一章」的落点改成**按号归位**：连着补几章，会按 11、12、13… 往下排。

### 修复

- 补写原来把新章放回**删除前那个 `sort_order`**。同一处位置删过好几章时，它们记着的是**同一个**
  旧位置，于是连续补写会把每一章都塞进同一个槽——屏幕上看着是倒着长的（连点几次得到
  第17章…第11章 从上往下排）。
- 现在按**编号**归位：插在"最后一个编号比它小的同层兄弟"之后（11 落到 10 后、12 落到 11 后……）；
  标题里认不出编号的（`序章` / `楔子`）仍旧退回原来的位置。

### 验收

- 删章路标套件 10 例全绿，含新回归「连着补写两处 → 顺序是 第11章、第12章」；
  "补回原来那一带"的既有验收也照旧通过。

---

## 0.34.5 — 2026-09-13

> 连点同一个「+」：新章**接着往下排**（原来是倒着长的）。

### 修复

- 在最后一章后面连点「+」，新章会一遍遍插在同一位置——屏幕上看着是倒着的
  （第10章 → 第12章 → 第11章）。现在界面记住"上一次从这一行建出来的那一章"，
  连点时就接着它往下排：第11章 → 第12章 → 第13章。
- **位置规矩一点没动**：换一行点「+」仍旧是"点哪儿插哪儿"。这次改法一开始动到了核心那条
  既有规矩，被全仓回归里的 `new_chapter_lands_where_you_clicked` 当场拦下——最后改在界面侧
  （谁记住"续接点"是编排的事，不是位置规则的事）。
- 顺手补齐上一版挪位置时漏提交的一处删除（前端那份会读文件系统的布局守卫）。

### 验收

- 前端新增 2 例（连点接续 / 换一行不接续）；前端 158 例、Rust 全仓、类型检查全绿。

---

## 0.34.4 — 2026-09-13

> 修章号取号的 bug：标题里带章名（`第6章 灯`）时认不出编号，新建的章号会从头重数。

### 修复

- 取号用的 `parse_serial` 原来要求标题**以「章」结尾**才算编号，于是 `第6章 灯` 这种最常见的
  写法认不出来；认不出就退回"同层现有几章 + 1"——作者连点几次「+」，第二卷里就长出一串
  `第6章…第16章`（每次都插在同一位置，看上去像章号倒着排）。
- 现在**只管开头**：`第12章` / `第12章 灯` / `第十二章（上）` 都认；`序章` / `第一次见面` /
  `第1-2章` 仍旧一概不猜（自起的名字不会被误判成编号）。
- 新增测试：`parse_serial` 表驱动 14 例 + 一条集成回归（第二卷已有 第6~10 章时，新建要继续
  数到 第11章，再点一次是 第12章）。

### 验收

- 核心 lib 64 例 + 章节目录套件 10 例全绿（含新回归）；全仓其它套件不受影响。

---

## 0.34.3 — 2026-09-13

> 滚动条不再跟着系统深色偏好跑偏；编译面板能一键打开产物目录；书架动作收进「更多」。

### 修复

- **滚动条**：`:root` 原来写 `color-scheme: light dark`，系统一开深色，WebView 就把**默认滚动条**
  按深色画——浅色稿纸配一条黑滚动条，很跳。现在只写 `light`（界面目前只有亮色一套；等暗色模式
  那条任务落地再改成动态），顺带把滚动条调细、跟稿纸同色系。这条不变量进了壳的边界守卫
  （`app/tests/boundary.rs`），以后谁再写成 `light dark` 会当场红。

### 新增 / 打磨

- **编译面板加「打开目录」**：编译（或预览）之后一键在文件管理器里打开这一种预设的产物目录。
  路径**由壳自己算**（界面不传路径，与"命令不接受路径参数"同一条纪律）；目录不存在就先建出来，
  免得点了却弹一句"路径不存在"。
- **书架动作收进「更多」**：日常只留「打开 / 编译」两个，导出、简介、改名、删除收进「更多」，
  一行不再挤六个按钮。

### 验收

- 壳 34 例 + 边界 7 例（含两条新守卫：亮色声明、正文只在自己区里滚）、前端 156 例
  （含「打开目录只叫这一个动作」）、类型检查与生产构建通过。

---

## 0.34.2 — 2026-09-13

> 修一个**长章节才露头**的界面毛病：正文一长，整个页面跟着滚，顶栏与两侧栏一起飘出视野。

### 修复

- 三栏骨架的行高没钉住：CSS 网格的隐含行会按内容高度长，正文够长就把整页顶开。
  现在 `.shell__body` 显式写 `grid-template-rows: minmax(0, 1fr)` + `overflow: hidden`，
  编辑栏补上 `min-height: 0`（网格项默认 `min-height: auto` 正是那个坑）。
  结果：**只有正文区自己滚**，顶栏、目录栏、右侧栏一律不动。
- 新增布局守卫（壳的边界检查里，`app/tests/boundary.rs`）：把这两条不变量钉进测试——这类毛病
  短稿永远测不出来，只有内容够长才会犯，正适合机器盯着。
  （放壳里而不是前端测试里，是因为**界面连测试都不许接触文件系统**——这条由同文件的另一条守卫盯着。）

### 验收

- 前端 157 例全绿（含 2 例新守卫）、类型检查与生产构建通过。

---

## 0.34.1 — 2026-09-13

> 投稿版 docx 两处修好：**在只认 twips 的阅读器里也能看见段首缩进**；不再被当**兼容模式**打开。

### 修复

- **首行缩进改成"字符为主、twips 兜底"**：`w:ind` 同时给 `w:firstLineChars="200"`（两字符；
  按 ECMA-376，两者同时出现时它以字符单位为准）与 `w:firstLine="420"`（五号 10.5pt 下两字符）。
  前者保证在 Word 里"改字号、缩进跟着变两格"，后者让**不解析 `*Chars` 变体**的阅读器
  （LibreOffice 一直如此，WPS 同类）也能看见缩进。
- **补齐标准包该有的几件**：`word/settings.xml`（写死 `compatibilityMode=15`）、
  `docProps/core.xml`（书名进文档属性）、`docProps/app.xml`。缺了它们，Word / WPS 会按
  **兼容模式**打开——版式默认值与编辑那边不一样。`core.xml` **不写时间戳**，否则
  "编译两遍逐字节一样"就失效了。

### 验收

- 核心测试加严：件清单（八件都在）、同时断言 `firstLineChars` 与 `firstLine`、兼容模式 = 15、
  核心属性里有书名且**不含时间戳**；"两遍编译逐字节一样"仍然成立。

---

## 0.34.0 — 2026-09-13

> 编译能用了：书架里点「编译」，选一种成品，**先看清会生成哪些文件，再动手**。

### 新增

- **书架**每本书多一个「编译」按钮，打开编译面板：
  - 三种成品：**投稿版**（单个 Word 文档：正文 + 大纲）、**分章纯文本**、**合并纯文本**；
  - 投稿版可调"正文只带前 N 字"（默认 3 万；留空＝全书都带）与要不要附大纲——
    参数初值由核心给，界面不另编一份默认；
  - **「看看会生成什么」**先列出会生成的文件与大小（不落盘），确认了再点「编译」。
- 产物落在导出目录里**各自的子文件夹**（submission / chapters / merged）：换一种编译不会把
  上一种的产物（刚拿去投稿的那份）冲掉；同一种再编译只清它自己的旧产物。
- 投稿版正文按段落累加，不会把一个自然段截成两半；大纲照旧覆盖全书。

### 验收

- 壳新增 1 例（二进制产物原样落盘、换预设不动别人、同预设清自己的旧产物）、
  前端新增 5 例（默认值来自核心、预览不落盘、编译才写、空上限＝全书都带）、
  预设码与界面字典穷举对表；全仓测试、类型检查、生产构建、公开内容脱敏检查全部通过。
- 投稿版 docx 的**可打开性**由核心测试用另一份 ZIP 实现验过（用 Word / WPS 真机打开仍待做）。

---

## 0.33.1 — 2026-09-13

> 编译管线的**核心**先落地：一份原稿 → 投稿版 docx / 分章 txt / 合并 txt（界面入口还没接）。

### 新增（核心侧）

- 新模块 `compile`：预设码表（`submission_docx` / `chapters_txt` / `merged_txt`）、
  按阅读顺序读原稿（卷/章标题 + 每章一句话 + 正文段落）、三个渲染器。
  - **投稿版 docx**：书名 → 简介 → 正文（默认前 3 万字，按段落累加，不切自然段）→ 大纲
    （照阅读顺序列卷/章标题与每章那句话；没写就不补占位）。
  - **合并 txt**：全书拼成一个文件，顺序照阅读顺序。
  - **分章 txt**：沿用既有渲染（一章一个文件、结构用目录表达）。
- 每个预设写**自己的子目录**（`submission/` / `chapters/` / `merged/`）：换一种编译不会冲掉上一种。
- **docx 的 ZIP 是自己写的**（不压缩 + 自实现 CRC-32 + 时间戳写死）：一半为了"编译两遍字节一样"，
  一半为了不引第三方打包库。缩进走样式层 `w:firstLineChars="200"`（两字符），正文里不塞空格。
- 渲染产物统一成**字节**（`RenderedFile.content`）：二进制与文本走同一条渲染 → 落盘通道；
  导出、备份、命令行三条路径跟着改，"内容一样就不重写"改成按字节比（docx 不会被反复重写）。

### 验收

- 新增 `tests/compile.rs` 7 例：阅读顺序、预设子目录、**docx 用另一份 ZIP 实现打开验件**
  （五件套都在、缩进键是 `firstLineChars`、正文在大纲之前、XML 特殊字符被转义）、正文上限
  （卡住正文但标题与大纲给全）、两遍编译逐字节一样、预设码往返与不认识的码报错。
- 全仓测试 31 个套件全绿（含导出 / 备份 / 命令行三条受影响路径）。

### 还差什么

- 界面入口（编译面板：选预设 → 看见会生成哪些文件 → 编译并给落点）还没接；
  接完再用 Word / WPS 真机打开投稿版验收。

---

## 0.33.0 — 2026-09-13

> 作品简介与每章一句话**能填了**：书架里填简介，编辑器里给当前章留一句话。

### 新增

- **书架**：每本书多一个「简介」按钮，行内多行输入；存着就在书名下面显示一行小字（点「简介」接着改）。
- **编辑器**：章名那一行多一个「一句话」按钮，点开就在正文上方写；存下之后收成一行小字，点它接着改。
- 两处都存进核心（`set_work_summary` / `set_node_summary`）：**存的是作者原话**（不 trim、不改标点），
  **与正文分开存**——写不写它，正文一个字都不变。存不下去会明确报错、输入框留着，不让作者白写。

### 为什么现在就做

投稿包要"正文 + 大纲"，而大纲不能只有一串标题：编辑要看的是**这本书讲什么**、
**每一章发生了什么**。这两样作者手填最准，所以先把地方给出来。

### 验收

- 核心 2 例（空值往返与覆盖写、老库升级后加列且内容不丢，含单条读与不存在的书/章报错）、
  前端 3 例（存下了才改显示、存不下去显示不动且框留着、切章以核心给的为准）全绿；
  全仓测试、类型检查、生产构建、公开内容脱敏检查全部通过。

---

## 0.32.1 — 2026-09-13

> 投稿包的**数据层**先就位：作品简介与每章一句话（两个手填字段）。

### 数据格式

- 数据库结构升到 v6：`works.summary`（作品简介）与 `nodes.summary`（每章一句话），两列默认空串。
  **老库打开自动加列**，已有正文与结构一个字不动。
- 核心读写：`set_work_summary` / `set_node_summary`；目录树那一次查询顺带把每章一句话带回来，
  导出与大纲不必为每章各跑一趟。
- 两个字段存的都是作者原话：不 trim、不改标点；变更留痕只记字数，不把整段话抄进日志。

### 还差什么（下个版本）

- 编辑入口（在书架里填简介、在编辑器里填一句话）与投稿包本体（编译管线：投稿版 docx /
  分章 txt / 合并 txt）尚未落地——数据先落地，是为了让渲染那条路一次写对。

### 验收

- 核心新增 2 例（空值往返与覆盖写、老库升级后加列且内容不丢）；全仓测试与 QA 门通过。

---

## 0.32.0 — 2026-09-13

> 排版清理再添两类检查：**手滑**（同一个字/词连打两遍）与**配对缺失**（引号括号书名号缺一半）。

### 新增

- **手滑检查**（在「排版」面板里，属"自己勾"那一档）：
  - 同一个字连着打了两个：`他说的的`、`很很`、`在在`；
  - 同一个词连着打了两遍：`已经已经`、`因为因为`。
  - 只认那些**绝不会叠着用**的字词（虚词、代词、连词一类）；"看看""慢慢""研究研究"
    这些正常修辞一个都不碰。对话里的结巴（`你你你说什么`）会列出来，但**默认不勾**——由作者定夺。
- **配对提醒**（只报告，不改稿）：引号、括号、书名号缺一半时逐段列出来，
  例如「第 2 段有个双引号没配对：开着的那个还没关上」。
  - 为什么不动手改：少了的那个右引号该补在哪儿只有作者知道，**猜着补就是替作者改稿**。
  - 提醒与"能改的改动"在面板里分开放：提醒不可勾选，也不进"改这 N 处"。

### 验收

- 核心新增 4 例（衍字与重复词改正、正常叠字叠词不误伤、提醒只报告不改稿、逐段计数与配对齐全时安静），
  前端新增 1 例（提醒不可勾、不进应用）；全仓测试、前端类型检查与生产构建、公开内容脱敏检查全部通过。

---

## 0.31.0 — 2026-09-13

> 排版清理：先看清"哪里会怎么改"，勾中的那几处才动——**正文一个字都不会被悄悄改掉**。

### 新增

- **排版清理面板**（正文上方「版本」旁边的「排版」按钮）：把当前这一章常见的排版小毛病先扫出来，
  **一处一处列清楚**（第几段、原来是怎样、会变成怎样），由作者决定勾哪几条。
  - 默认只勾最没有争议的那一档：省略号统一成 `……`、破折号统一成 `——`、去掉段首多余的空格
    （从别处粘贴过来的稿子常带着两个全角空格，看上去就成了四格缩进）。
  - 会动到书写习惯的（中文里的半角标点、连着打的标点、引号）与纯风格的（中英文之间加空格）
    **默认一律不勾**，想改再勾。
- **引号两套偏好**：`“ ”` 或 `「 」`，在面板里选一次就记住；选哪一套，引号就统一成哪一套
  （半角引号成对换掉，落单的那一只不动——宁可不改，也不制造没配对的引号）。

### 安全边界

- **扫描与应用分家**：扫一遍只报告，不改一个字；应用只改勾中的那几处。
- **动手前先留底**：应用之前自动给这一章留一个版本，随时能在「版本」里换回来。
- **过期预览拒绝执行**：看完之后稿子又改过，旧的勾选序号就不再有效——面板会重新扫一遍，
  绝不拿旧账去改新稿子。
- **不误伤**：英文缩写、小数、版本号、网址、命令行参数（`e.g.` / `3.14` / `v0.16.2` / `--self-test`）
  一律绕开；段落缩进仍由显示层负责，正文里不塞空格。

### 验收

- 核心新增 15 例验收，前端新增 8 例；其中含"应用过一遍之后再扫必须一条不剩"的幂等判据、
  "没勾的条目一个字都不许改"与各类反例（小数、版本号、网址、英文撇号、代码参数）。
- 全仓测试、前端类型检查与生产构建、公开内容脱敏检查全部通过。

---

## 0.30.1 — 2026-09-13

> 「启动打不出中文」**真机复验通过**（用户那台 Win10）：输入法预热这条路走对了。

### 结论（三次误判之后，靠真机日志定的案）

- **不是**窗口没被激活（`is_focused=true`）、**不是**旧运行时（152.0.4191.66 很新）、
  **不是**焦点没给正文（日志显示 `div.tiptap ProseMirror-focused` 可编辑）。
- **是** WebView2 的输入法（TSF）第一次没挂到正文上：焦点了，但组字事件一个都不来；
  切一次输入语言就恢复，此后焦点进出都正常。
- **解法**（v0.30.0）：定焦点之前先用一个隐藏的真输入框把输入法引到 WebView 上，再把焦点交给正文。
  用户复验：**正常了**。

### 记下来的东西

- `ACCEPTANCE.md` 的「中文输入法」边界改成"v0.30.0 起已解决 + 根因 + 绕法"（换 WebView2 运行时可能再遇到，
  所以根因要留着）。
- `tools/acceptance/README.md` 增加「启动体检」一节：`diagnose-ime.bat` 量什么、怎么看、
  以及"装了多个运行时还会对照跑一遍"。这套诊断是这次定案的工具，留着给以后同类毛病用。

---

## 0.30.0 — 2026-09-13

> 输入法预热：先用一个隐藏的真输入框把 WebView2 的输入法引上来，再把焦点交给正文。

### 背景（真机日志钉死的）

用户那台 Win10 上：窗口有焦点、正文也拿到了 DOM 焦点，但**输入法组字事件一个都不来**；
切一次输入语言再切回来就好，此后焦点进进出出都正常。这是 WebView2 在 `contentEditable` 上的
**上游未修缺陷**（同一页面里的普通 `<input>`/`<textarea>` 一直正常）。

### 做了什么

- **输入法预热**：启动定焦点之前，先在页面上放一个真实但看不见的输入框（屏幕外、1×1、
  `aria-hidden`、`tabindex="-1"`）并聚焦它——输入法会随它挂到 WebView 上；60 毫秒后把焦点交给正文，
  此时输入法已经在岗。用完即删，不进阅读顺序、不可见。预热过程写进诊断日志。
- 界面上原来的焦点策略照旧（**新增**：打开最新章且有"上次读到哪"的记录时，光标回到他停下的位置
  **并交进正文**——"接着写"就该直接能写；历史章仍然不聚焦，那条是防误触）。
- `tools/acceptance/diagnose-ime.bat` 增加**运行时对照**：如果这台机器装了不止一个 WebView2 运行时，
  它会用当前运行时跑一遍、再用较旧的那个跑一遍（`WEBVIEW2_BROWSER_EXECUTABLE_FOLDER`），
  这样能分清"是运行时的锅"还是"我们的锅"。

### 验收

- 前端类型检查、138 例、生产构建；Rust 247 例；静态 8 项全过。
- 真机复验待用户：装上 0.30.0 → 打开 → **不点任何地方，直接按输入法热键打拼音**。
  若仍不行，跑 `diagnose-ime.bat`（它现在会同时跑新旧两个运行时）把两份日志发回来。

---

## 0.29.2 — 2026-09-13

> 「打不出中文」的真凶找到了：**开机后光标不在正文里**，输入法没有落点。真机诊断日志定的案。

### 怎么定的案

用户在自己那台 Win10 上跑 `--diagnose`，日志说得非常清楚：

```
473ms   窗口状态：可见=true 有焦点=true          ← 窗口本身被激活了，没问题
1.5s    焦点元素=body 不可编辑                  ← 但光标不在正文里（前 6 秒一直是 body）
7.35s   页面被点击 → 正文拿到焦点（div.tiptap 可编辑）
17.3s   输入法按键（229/组字中）→ 18.6s 开始组字 → 结束组字（高浮雕）
```

**输入法本身没问题**（一旦焦点进了正文，组字事件全来、中文也敲出来了）；
问题是**启动时没人把光标放进正文**——按输入法热键时没有任何输入元素在接收，
作者看到的现象自然就是"打不出中文"。同时排除了旧运行时的可能（那台机器 WebView2 是 152.0.4191.66，很新）。

### 做了什么

- **焦点策略加一档 `restore`**：打开的是**最新章**且库里有"上次读到哪"的记录时，
  把光标放回他上次停下的位置**并交进正文**（以前只放位置、不聚焦——位置对了，但输入法挂不上）。
  "接着写"就该让作者直接能写。**历史章仍然不聚焦**（那条是防误触：旧章是"看"）。
  作者的「接着写（跳到段末并聚焦）」开关关掉时，这一档也不抢焦点。
- 诊断日志改成**逐次**记录点击与按键（上一版"只记第一次"把"作者又点了一下"这个关键事实吞掉了）。

### 验收

- 前端测试 138 例全过（含焦点策略矩阵的新断言：最新章+有记录 → `restore`；开关关掉 → `leave`）；
  前端类型检查、构建、Rust 247 例、静态 8 项全过。
- 真机复验待用户：双击安装包 → 打开 → **不点任何地方，直接按输入法热键打拼音**，应当直接出中文。

---

## 0.29.1 — 2026-09-13

> 修一个**会让人打不出中文**的真 bug（自己引入的），并给前端补上类型检查这道闸。

### 做了什么

- **修致命错误**：0.29.0 里诊断接线那一行在 `onMounted` 中用了不存在的变量 `instance`，
  它一抛异常，**后面那句"给编辑器初始焦点"就永远执行不到**——症状正是"中文输入法点不出来，
  要切英文打几个字母再切回来"。这类错误本该被类型检查拦住。
- **前端类型检查进闸门**：`npm run typecheck`（`tsc -p tsconfig.app.json --noEmit`，只查应用代码，
  测试文件要 `@types/node` 另算）接进 QA 门的"逻辑"层，排在 `vite build` 前面。
  背景：`vite build` 只转译**不做类型检查**——一行"用了不存在的变量"能一路溜进发布版。
  顺手补了 `.vue` 的模块声明垫片，并修掉四处历史类型不一致（TipTap `setContent` 的新签名、
  `useEditor` 的 `undefined` 与 `null`、回收站 `resolveConflict` 的返回值声明）。
- **弹窗与焦点的先后关系**（同一个症状的另一半原因）：启动时若正弹着首启引导等弹窗，
  原先会"急着给正文定焦点"，而焦点随即被弹窗拿走、关掉后没人还回来。现在：
  **弹窗盖着就不定焦点，弹窗一关就把焦点还给正文**（正文已经拿着焦点时不动，免得跟切章/新建打架）。
- 首启引导第一步点「✕ / 关闭」现在等于接受推荐位置（稿子本来就在那儿）并**把位置记录写上**——
  否则每次打开都弹这一下，而每次弹都会打乱焦点。

### 验收

- 一键构建重出包；QA 门全过（含新加的前端类型检查）。
- 复现路径：便携沙盒里开窗 → 首启引导关掉 → 正文应立刻能打中文（真机复验由用户做）。

---

## 0.29.0 — 2026-09-13

> 启动诊断：给"输入法挂不上来"这类只在个别 Win10 机器上出现的毛病装一台体检仪——**拿现场时间线说话，不猜**。

### 做了什么

- **`研墨.exe --diagnose`**（可配 `--out <文件>`）：正常开窗，但把这条链逐条记成时间线——
  进程启动 → 页面加载完成 → `show()` + `set_focus()` → 窗口有没有真的拿到焦点 → 数据层就绪 →
  界面就绪，以及**窗口焦点事件**（拿到/失去）、**每秒钟"文档/焦点元素是谁"**、
  **正文元素拿到/失去焦点**、**输入法组字事件**（compositionstart/end）、**IME 按键（keyCode 229）**、
  **页面被点击**。
- 为什么这些字段：那条毛病的分水岭只有两个——"**窗口到底有没有被激活**"（Windows 的输入法是随窗口激活挂上去的）
  与"**输入法有没有真的挂到正文元素上**"（组字事件来没来）。没有这两样，只能靠猜。
- **激活重试**：`show()` + `set_focus()` 之后会隔 400 毫秒看一次窗口焦点，最多 6 次，
  没焦点就再要一次（前台进程切换在某些机器上会静默失败），全程记进诊断日志。
- 诊断日志写**文件**（`tools/acceptance/diagnose-ime.bat` 会先写进 Windows 版本与 **WebView2 运行时版本**，
  跑完再把日志 `type` 出来）——GUI 子系统程序在控制台里没有可用的标准输出，这一点 0.28.3 已经踩过。
- 平时这些打点等于空转：不写文件、不做多余的事；界面上报焦点/组字事件的命令在没开诊断时立即返回。

### 验收

- 一键构建重出包；QA 门静态项（含脱敏、文案、命令网关对表）全过；
- 前端 138 例、Rust 247 例全过。
- **诊断本身要真机跑一次才算数**：那台 Win10 上双击 `diagnose-ime.bat`，按提示做三步（不点直接打 /
  点一下再打 / 切英文再切回来），把日志发回来定位。

---

## 0.28.4 — 2026-09-13

> 「不丢稿」补上**真机实测**证据：硬杀一次，稿子一个字没少。

### 做了什么

- 用 `tools/acceptance/drill-safekeeping.bat` 在用户自己的机器上做了一次**真的硬杀**
  （等价于断电 / 任务管理器结束进程），前后各记一次账：
  铺好 50 章 = 13251 词 → 硬杀 → 重开 = **13259 词**（当场敲进去的字一个没少），
  章数 50 不变、库体检 `ok`；全程只动 `%TEMP%\yanmo-drill` 沙盒，真实稿库一字节未碰。
- 数据与做法写进 `ACCEPTANCE.md` 的「不丢稿」一节；边界照旧写明：写完到落盘之间有一个
  几十毫秒的窗口，断电正好卡在那儿最多丢最后几个字，但落盘是原子的，不会有"半个文件/打不开的库"。

### 验收

- 只改公开文档与版本号，产品代码未动；QA 门静态项全过。

---

## 0.28.3 — 2026-09-13

> 修验收模式自己的一个 bug：**`--check` 的结果在控制台里根本看不见**。

### 做了什么

- 现象：用户跑 `drill-safekeeping.bat` 时，`before:` 与 `after:` 两行是**空的**——体检明明跑了，数字却没打出来。
- 原因：研墨是 **GUI 子系统**程序（`windows_subsystem = "windows"`），在 cmd 控制台里拿不到可用的标准输出，
  `println!` 写了也白写。我自测时是从管道里读的（所以看得见），一放到控制台就全丢——**这类 bug 只有真机才现形**。
- 改法：体检结果一律**写文件**（`--check <目录> --out <文件>`，脚本用 `type` 打出来），
  不再依赖标准输出；文件本身也留作证据。演练脚本因此固定留两份账：`drill-check-before.json` / `drill-check-after.json`。
- 顺带：两个 `.bat` 跑完会自己**列出产出的文件名**（报告路径原来是靠程序 stdout 报的，同样看不见）。

### 验收

- 沙盒实测：`--check --out` 写出的 JSON 正确（`integrity` / `chapters` / `words` 三项齐全），
  脚本 `type` 出来一眼能比 before/after；QA 门静态项全过。

---

## 0.28.2 — 2026-09-13

> 低配机实测数据补进公开验收报告：**十年前的机器上，百万字依然可用**。

### 做了什么

- `ACCEPTANCE.md` 的「低配机」一栏由用户在自己的机器上跑 `run-acceptance.bat` 填上
  （i7-4790 / GTX960 / 15.9 GB / Windows 10，8 逻辑核，0.28.1），并加了**两台机器并排的对照表**与"怎么读"：
  - 写字与检索在低配机上仍是"无感"级：写一章 0.76 毫秒、检索 25 毫秒；
  - "打开"类动作是几百毫秒量级：打开一章 164 毫秒、**界面冷启动 1.04 秒**；
  - 一次性铺 1000 章（300 万字）3.45 秒，是最吃整机的一条，日常写作不会反复出现；
  - 有意思的一条如实写进去：**备份那一步低配机反而更快**（5.9 秒 → 2.3 秒），
    说明它主要吃磁盘顺序写、不吃 CPU——两台的差异在这里不能比 CPU。
- 强调这些数字**不是承诺**：同一份脚本、同一份数据规模，自己在自己机器上跑一遍才算。

### 验收

- 只改公开文档与版本号，产品代码未动；QA 门静态项（版本四处一致、脱敏、文案）全过。

---

## 0.28.1 — 2026-09-13

> 给验收模式补一道**防呆**：老版本上跑验收脚本会闷头弹窗干等，现在会明确停下来说清楚。

### 做了什么

- `研墨.exe --version`：报版本号，并明说"这一份有没有验收模式"（`0.28.1 acceptance=1`）。
- `run-acceptance.bat` / `drill-safekeeping.bat` 开跑前先**核对 exe 的文件版本**：**老版本（0.28.0 之前）
  根本没有验收模式**，它会把 `--self-test-*` 当普通参数忽略掉、直接弹个窗口，脚本就会一直等下去——
  那看着像卡死。现在这种情况会明确提示"这一份太旧、没有验收模式"，然后退出。
  特意去读**文件版本**而不是运行 `--version`：老版本连 `--version` 都不认，那道检查自己也会挂住。
- 顺手把脚本横幅里的版本号打出来（一眼看清这次量的是哪一版）。

### 验收

- `--version` 实测：安装版与本次构建都回报 `acceptance=1`；QA 门全过。

---

## 0.28.0 — 2026-09-13

> 「百万字不卡」「不丢稿」不再只是我们说的话：**程序自带验收模式**，你在自己电脑上双击一下就能跑出数字。

### 做了什么

- **验收模式**（只由启动参数进入，正常双击图标的行为一字不变）：
  - `研墨.exe --self-test-bench`：**不开窗口**，在系统临时目录里造一份 1000 章 × 3000 字（≈300 万字）的库，
    量一轮写入 / 目录树 / 读一章 / 写一章 / 检索三种形态 / 库体检 / **一致性快照（备份）** / 关库重开；
  - `研墨.exe --self-test-ui`：开窗打开同一份库，量**界面冷启动到可用**。**单独一个进程**量，
    因为同一个进程里先跑完基准、程序文件早进了系统缓存，那样量出来的"冷启动"会偏乐观；
  - `研墨.exe --check <目录>`：只读体检一份库并打印 JSON——"不丢稿"演练用它核对账目。
  - 验收模式**绝不碰真实稿库**：数据目录是临时目录，位置记录一个字节都不动；不开任何网络通道。
  - 界面那一步有 90 秒看门狗：界面没就绪也会**出报告并退出**（如实写"这一步没量到"），绝不挂在那儿；
    另有 `-ui.log` 命令级日志，界面没就绪时一眼看出停在哪。
- **`tools/acceptance/`（公开、可自己跑）**：`run-acceptance.bat`（双击跑完整验收，报告写在脚本旁）、
  `drill-safekeeping.bat`（"不丢稿"演练：沙盒里铺 50 章 → 写字 → **硬杀** → 重开核对账目；
  只杀沙盒里那一个实例，不碰你打开着的研墨）、`README.md`（量什么、怎么比、已知边界）。
- **`ACCEPTANCE.md`**：公开的验收数据与说明——三条承诺各自的验证方法、实测数字（含**低配机一栏**）、
  **CJK 三个口径的定义与对照表**、已知边界（同步盘/移动盘、单实例、WebView2 输入法上游缺陷）、
  以及故障记录（发布以来丢稿报告 0 起）。
- **口径对照表由测试锁住**：`tests/wordcount.rs` 里那张表和 `ACCEPTANCE.md` 同源，
  改了口径算法测试会红，逼着人回头改文档——文档漂移比算错更难发现。

### 验收

- 一键构建重出包；QA 门全过（静态 / Rust / 前端 / 规模）。
- 发布版实测：300 万字库——写入 941 ms、目录树 60 ms、读一章 60 ms、写一章 0.5 ms、
  检索 14 ms、体检 115 ms、**备份一次 6.1 s**、关库重开 73 ms（开发机 16 核 / 31.8 GB）。
- **低配机（i7-4790 / 16 GB）那一栏留给真机**：用户跑一遍 `run-acceptance.bat` 把数字贴回来。

---

## 0.27.1 — 2026-09-13

> 修一个刚发现的洞：**绿色版会去"认领"这台机器的系统数据目录**。

### 做了什么

- 便携/绿色形态在"没有位置记录、程序旁也没有 `data/`"时，会一路走到**老位置认领**那一步；
  而认领那一步**没看便携标记**，于是把本机 `%APPDATA%` 里的安装版库当成了"老位置"——
  绿色版插到谁机器上就打开谁的稿子，这正是绿色版最不该做的事（拿演练目录试的时候当场暴露：
  它打开了安装版的库，还把那条路径写进了演练目录的位置记录）。

  改法：**系统数据目录只在非便携形态下认领**。便携版没有记录时老实走首启推荐（推荐程序旁的
  `data/`），"插到任何机器上都只动自己文件夹"这件事才算真的。便携版自己程序旁的 `data/` 里有库时
  照旧认领——那是它自己的东西。

### 验收

- 核心新增 1 例：本机系统数据目录有库时，便携版仍走首启推荐、且落点不在系统数据目录里；
  程序旁 `data/` 有库时照旧认领。`tests/location.rs` 11 例全过。

- 踩过的演练目录里可能已经留下一条指向 `%APPDATA%` 的位置记录——**删掉那个文件再试**，
  否则"记录优先"这条规矩会忠实地继续跟着它走。

---

## 0.27.0 — 2026-09-13

> 稿子放在哪，现在由**作者说了算、并且记得住**：第一次打开会推荐一个你能找到的位置
> （默认「文档\研墨」），设置里随时能把稿子搬到别处（**复制过去、旧位置不删**）。
> 老用户的稿子会被**就地认领**，不会看见"首次使用"。

### 做了什么

- **位置记录（指针文件）**：作者选过的位置写进一个小文件（便携形态在程序旁，否则在系统数据目录），
  **永远是它第一个说话**。记录指向的目录哪怕此刻不在（U 盘没插），也**绝不静默换地方**——只明确报错。
  命令行救援入口与图形界面读的是同一份记录，两个壳说同一句话。
- **老位置认领**：没有记录、但老位置（程序旁的 `data/`、或原来的系统数据目录）里已经躺着库 →
  就地认领并补上记录。从老版本升上来的作者不该看见"首次使用，请选数据目录"。
- **首启引导**：第一次用先推荐一个位置，并说清**为什么推荐它**——「文档」里一眼找得到；
  如果「文档」被 OneDrive 之类的同步盘接管（同步盘来回上传、偶尔回滚文件会撞坏稿库），
  改推荐主目录下的「研墨」并说明原因；便携版推荐程序旁的 `data/`。第二步提醒"别把稿子只放在一处"，
  一键跳去设备份位置（默认不勾任何盘、可跳过）。
- **设置里换位置**：关库 → **复制**整个数据目录 → 在新位置按"备份体检"的同一条 SQL **核对规模账**
  → 记下新位置 → 重启。三条拒绝：原目录里没有库、新位置在现在目录里面、新位置已经有一份稿子。
  **旧位置一个字都不删**（删不删由作者看过之后定）。搬家前先把手上这一章落盘，落不下就不搬。
- **选位置时的风险提示**（只提示、不替作者决定）：同步盘 / 桌面 / 盘根目录 / 可移动盘或网络盘；
  两种真危险的（往自己里面搬、目标已有稿子）由壳直接拒绝。
- 顺带把盘符探测（卷标 / 序列号 / 是否可移动）从备份命令域抽成独立模块：备份与换位置共用一份。

### 验收

- 核心新增 `tests/location.rs` 10 例（记录优先/坏记录当没记录/老位置认领/同步盘改推荐/风险清单/
  复制与三条拒绝/核对拦得住坏副本/手改过的记录仍读得回）。
- 壳侧新增 4 例：搬成功＝库关着＋旧位置不删＋记录指向新位置；搬不成＝库重新挂上＋记录不动；
  往自己里面搬被拒；首启确认写记录、待定位置只让取一次。界面新增 `editor/location.test.ts` 7 例。
- 全程只在临时目录里动，作者的稿库与真实数据目录一字节未碰。

---

## 0.26.8 — 2026-09-13

> 两处中文输入（IME）相关的修复：**字数不再把拼音字母算进去**，以及启动后中文输入法能直接用。

### 做了什么

- **字数不再被"组字中途"带偏**：输入法正在拼（正文里还是 `nihao` 这样的拼音字母）时**不算改动**，
  等选完字（`compositionend`）再按最终正文算一次。以前状态栏会"先跳一个数、选完字再刷新"，
  给人一种"字数到底准不准"的怀疑感。落盘同理——组字中途也不会把拼音存进去。
- **启动后输入法能直接用**：两处一起改——① 窗口在"页面加载完成"显示之后**再显式激活一次**
  （Windows 的输入法是随窗口激活挂到输入元素上的；只显示不激活，Win10 上会出现"中文输入法点不出来，
  要先切英文打几个字母再切回来"，同样的代码在 Win11 上恰好不露）；② 编辑器**等窗口真的被激活后再决定
  初始焦点**（最多等 3 秒，到点照常聚焦，绝不为了输入法把启动卡住）。

### 验收

- 一键构建重出包；构建门全过（静态 / Rust / 前端 / 规模）。
- **这两条都要真机验收**（尤其是 Win10）：中文输入法点开直接能打、组字时状态栏字数不动、选完字再跳。
- 已知上游边界：[WebView2 在 `contentEditable` 上的输入法集成有未修的缺陷报告](https://github.com/MicrosoftEdge/WebView2Feedback/issues/5625)
  （首字丢失、全角标点要按两次；同一页面的 `<textarea>` 正常）。本次改的是我们能控的激活/焦点时序；
  若 Win10 上仍有残留，先看那台机器的 WebView2 运行时版本。

---

## 0.26.7 — 2026-09-13

> 两件小事：右上角那个「数据 …」点一下就能打开文件夹；出包脚本不再可能挑错安装包。

### 做了什么

- **点一下右上角的「数据 …」用文件管理器打开**：作者最常见的疑问是"我的稿子到底在哪"，
  以前只能看着那串路径（还很窄、被省略号截断）。现在它是一个按钮，点一下就在资源管理器里
  打开稿子所在目录。两条边界照旧：**界面不传路径**（壳只打开自己持有的那个目录）、
  **不做任何文件操作**（只是让系统去显示它）。
- **修掉出包脚本里一个会挑错包的隐患**：它原来"按文件名排序取最后一个"来选安装包——
  等到 `0.26.9` 之后的 `0.26.10`，字典序会把 `9` 排在 `10` 后面，于是**发出去的可能是旧包**。
  现在**按当前版本号精确匹配**，匹配不到就直接报错（不猜）。顺带：`dist/` 里还留着别的版本时
  明确提醒一句，免得又拿旧包去装。

### 验收

- 一键构建重出包；产物属性用系统接口读回来核对（`FileVersion` 必须等于当前版本）。
- 「点一下打开文件夹」要真机点一次（会在资源管理器里弹出稿子目录）。

---

## 0.26.6 — 2026-09-13

> 窗口出生点固定：每次打开都在屏幕正中。

### 做了什么

- **启动位置不再"看系统心情"**：窗口配置 `center: true`。之前我们没给位置，
  Windows 就按它自己的默认放置规则来（会错位/挪动），于是每次打开的位置都可能不一样；
  现在每次都出现在**主屏正中**，可预期。

### 验收

- 一键构建重出包（`dist\研墨-0.26.6-setup.exe`）。
- 机械探针：便携沙盒里启动构建产物，读窗口矩形与屏幕尺寸，确认它落在屏幕正中
  （不靠肉眼，也不碰真实稿库）。

### 备注

- 只在主屏居中，没有"记住上次位置"。要那个得单独做（还要处理"上次那块显示器拔了"的兜底），
  已登记成一条小待办。

---

## 0.26.5 — 2026-09-13

> 启动不再闪白：窗口等界面画好再露面。

### 做了什么

- **首帧白屏的正解**（不是调参玄学）：窗口在配置里**先隐藏**、底色设成纸色，
  等网页**加载完成**再把窗口显示出来——作者看到的是已经画好的界面，而不是先一块白再变内容。
- **配一条兜底**：万一"页面加载完成"那个事件没来（前端资源卡住、页面崩了），
  三秒后也强制把窗口显示出来——**绝不因为一个观感优化，把软件变成「点开没反应」**。

### 验收

- 一键构建重出包，产物在 `dist\研墨-0.26.5-setup.exe`（附 SHA256）。
- 冒烟探针：拿便携沙盒跑一遍构建出来的程序，确认**窗口在启动后确实变成可见**
  （这条防的是"窗口一辈子藏着"的反向事故）；观感本身要真机看一眼。

---

## 0.26.4 — 2026-09-13

> 安装器说中文：**一个安装器内置中英两套语言，按系统语言自动选**——不做两个壳。

### 做了什么

- 安装器与卸载器都编进了**简体中文 + English** 两套：Windows 是中文环境就走中文，
  英文环境就走英文（多语言安装器的标准行为：按系统语言自动选，不用人挑）。
  不做"两个安装壳"——两份要维护、要发两个文件、用户还得挑对的那个，好处几乎没有。
- 中文覆盖面是**两层**，所以不再是"英文夹中文"：
  ① 安装器框架自带的界面文字（下一步 / 取消 / 选择安装位置 / 安装完成 / 已完成…）；
  ② 打包工具自带的自定义串（创建桌面快捷方式 / 添加-重新安装组件 / 正在安装 WebView2 /
  已经安装了… / 无法卸载…）。
- **没加语言选择页**：关掉 `displayLanguageSelector`，让系统语言决定。将来真需要手动切换，
  把它打开就是一个语言页，不用改模板。

### 验收

- 生成的安装脚本里两份语言都编进去了（`MUI_LANGUAGE "SimpChinese"` + `"English"`，
  并各带一份语言串文件），卸载器也跟着记住/回退到同一套。
- 视觉验收：在中文 Windows 上装一遍应全中文（这一步要真机点，随装机冒烟一起看）。

---

## 0.26.3 — 2026-09-13

> 一条命令出安装包：**陌生人拿到仓库，跑一条命令就能得到一个能双击安装的产物**。

### 做了什么

- 新增一键构建脚本（Windows 上 `tools\build-release.bat`），六步走完，任一步失败就不出包：
  ① 工具链自检（cargo / node / tauri CLI 缺了就给人话提示，WebView2 只提示不拦）；
  ② 版本一致（Rust 工作区 / 应用配置 / 更新日志首条三处）；顺带机械断言 **`data/` 不进安装包**；
  ③ 前端依赖与生产构建；④ **跑测试**（优先跑仓库自带的 QA 门，没有就退到公开的 Rust + 前端两件）；
  ⑤ 打包（`cargo tauri build`，NSIS 安装器）；⑥ 产物归集到 `dist/`、文件名带版本号、附 SHA256，并自检产物存在且不为空。
- README 补上「出一条能发给别人的安装包」，并把那个**最容易踩的坑**写成显著警告：
  单独 `cargo build --release` 出来的是**开发模式**可执行文件（启动后去连 `localhost:1420`，
  双击一片白屏）——要能双击的产物必须走 Tauri 自己的构建路径。

### 验收

- 真跑一遍（同一条命令）：工具链自检 4 项通过 → 版本一致 → 前端构建 → 仓库 QA 门全过
  （Rust 231 + 前端 131 + 规模基准）→ NSIS 打包 → 归集自检；
  产物 `dist\研墨-0.26.3-setup.exe`（约 2.8 MB）+ `dist\研墨-0.26.3-setup.exe.sha256`。
- 冷启动验收（清空 `target/` 与 `node_modules/` 后同一条命令再出一次）见提交说明。
- 脚本自己也在 QA 门里：脱敏扫描抓到过它首版里的作者本机路径（公开脚本不该有），已改。

---

## 0.26.2 — 2026-09-13

> 给 QA 门加两只"审计眼"：一只看**有没有做了但没接上的代码**，一只看**依赖方向有没有越界**。

### 做了什么

- **脱轨审计**（开发用，脚本在独立工具仓里，不进主仓）：找"定义了但真实业务链路从不触发"的代码——
  没人调的写方法、壳里没接上界面的命令、建了表却没人写。它知道几处容易误判的地方：
  壳里的命令是前端按**字符串**调的（判据看网关清单）、Rust 允许**裸函数调用**、
  文件末尾的测试模块不算生产代码。
- **依赖方向判定**：crate 级与核心模块级的依赖图，外加把三条铁律做成机械判定——
  核心零界面依赖、命令行零网络、协议独立；crate 与核心模块都不许成环。
- 两只眼都接进 QA 门（默认层，秒级）：找到独立工具仓就跑，**找不到就明确标"未跑"**，不假装通过。
- 审计件自己也有单测（用临时造的迷你仓库验证"越界能被抓住""死方法能被抓住"）——
  **守卫要是假绿，纪律就形同虚设**。

### 验收

- 首跑结论（真实仓库）：**依赖无越界、无成环**；**死写 0、壳里命令全部接上界面**；
  建表无写只有一张为将来功能预埋的表；另有 13 项"生产里没人调"的公开 API 已登记待逐个定去留。
- QA 门：静态 8 项（含两只审计眼）、逻辑 231 + 131 项、规模基准，全过。

---

## 0.26.1 — 2026-09-13

> 开发流程收口：把散着的检查收成一道门——**一条命令给结论**，不然没人知道到底绿没绿。

### 做了什么

- 新增一道 QA 门（开发用；脚本与词表不进公开仓，与既有的脱敏、文案守卫同一条纪律），三层：
  - **静态**：版本号四处一致（Rust 工作区 / 应用配置 / 项目状态 / 更新日志首条）、内部资料不会被误提交、
    公开内容脱敏、界面文案守卫、文件卫生（无 BOM、无乱码、JSON 与 TOML 可解析）、代码健康巡检（只报警）。
  - **逻辑**：Rust 全仓测试 + 前端测试 + 前端生产构建。
  - **规模**：1000 章 / 约 315 万字的规模基准（`--full` 才跑；`--stress` 再加病态的单章 300 万字）。
- 两条纪律写进门里：**跑不起来算失败**（找不到工具链就报红，不许假绿）；
  界面文案的存量欠账用**棘轮**记录——只许减不许增，新增即红。
- Windows 上一个批处理入口，失败带非零退出码。

### 验收

- 门自己跑通（一条命令）：静态 6 项全过（脱敏 0 命中；161 个文件卫生；文案守卫 39 处已知欠账、本次新增 0；
  28 个文件超软上限只报警）；逻辑 231 + 131 项通过、生产构建通过。
- 规模基准真数字：1000 章 / 约 315 万字——写入 925ms｜全树 59ms（1001 节点）｜检索 13ms（命中 50）｜
  读一章 99µs｜库 21.9 MB。

---

## 0.26.0 — 2026-09-13

> 同一个数据目录只允许开一个研墨：防的是「两个窗口同时写一个库」这类真实的损坏来源。

### 做了什么

- **按数据目录上锁**：启动时先占住这个数据目录，占不到就弹一句人话提示再退出——**绝不静默失败**
  （不会让你以为打开了，其实什么都没发生）。锁的名字由数据目录算出，所以**不同数据目录可以同时开**：
  便携库、将来的多库、以及「拿一份副本做演练」的沙盒，互不打扰。
- **用系统命名互斥体，不用锁文件**：进程被杀、崩溃、断电时句柄由系统收走，锁自动消失。
  锁文件会在崩溃后留下一具尸体——下次启动要么误报「已经在运行」（软件再也打不开），
  要么得靠进程号猜它死没死，两种都不要。
- **换库重启不会把自己锁在门外**：换库成功后壳要重启，而重启是「先起新进程、旧进程随后退出」，
  旧进程会在换代之前把锁松开，新进程才拿得到；另有最多 1 秒的等待窗口，覆盖「刚关掉又马上打开」。
- **同一目录换个写法不算另一个目录**：锁名字先取规范路径再算，`文档\研墨` 与
  `文档\..\文档\研墨` 是同一把锁。
- **诚实边界**：这把锁管的是**同一台机器上的多个实例**。两台机器通过同步盘各开一个，它管不了
  ——那是「数据目录别放同步盘」的问题，启动引导会提示。

### 验收

- 壳侧新增 4 项验收：同一数据目录第二次启动被拒（提示里带目录路径）、关掉之后又能开、
  不同数据目录互不打扰、同目录的另一种路径写法仍是同一把锁、等一会儿能等到即将释放的锁
  （换库重启靠这条）。
- 全仓 Rust 231 项、界面 131 项通过；前端构建通过。

---

## 0.25.0 — 2026-09-13

> 从备份恢复：稿子真出事的那天，能把备份拿回来——而且敢按这个按钮。

### 做了什么

- **备份面板多了一个入口：「从备份恢复…」**。它只做一件事：把某一份备份换回数据目录。
- **换之前先体检，不完整就不给换**：读清单 → 快照能不能打开且结构完好 → 逐书比对章节数与字数 →
  成稿文件还在、指纹对得上。任何一条不过就明说「这份不完整」，并把问题逐条列出来；
  来自更新版研墨写的备份也会被拦下（当前引擎不猜着读）。
- **换之前先摆事实**：这一份是什么时候做的、来自哪台机器、里面有多少本 / 多少章 / 多少字、
  它记到的最后写作时间；再跟现在的库比一比，**会退回几天、大约少多少字**，都写在按钮上面。
- **换之前必须由作者确认**：勾一句「我知道现在的稿子会被这份备份替换掉」，按钮才按得动。
- **原库留底，不删**：换库时把当前库连同 `-wal` / `-shm` 一起改名搬进数据目录里的「旧库留底」，
  文件夹名带时间戳；坏库也留证（只搬 `.db` 会把不一致的 WAL 留给新库，那比坏库更难查）。
- **换上去当场再体检一次**：不过就把新库撤掉、把留底搬回来，一个字节都不少——恢复失败不该
  让作者连原本还能用的库一起失去。失败后软件照常开着原库，作者可以接着写。
- **库坏到读不出来时，恢复照样能走**：算不出"会丢多少"就如实说算不出来，
  绝不让一个报不出来的数字把救援路堵死（那正是最需要它的时刻）。
- **恢复来源有两种**：备份位置里的备份包（按时间从新到旧列出来），以及**作者自己挑的库文件**
  （手上只有一份库文件、或者备份盘换了位置时用）。挑文件走系统自带的打开对话框，
  界面依旧拿不到文件系统能力。
- 换库成功之后软件会**自己重启**（重启由壳发起，不依赖界面是否还活着）。

### 验收

- 核心恢复验收 14 项：列包不误列（别人的目录、名字像但没有清单的）、体检结论与「会丢多少」、
  快照坏 / 成稿缺 / 来自更新版一律拒绝、换库换内容且原库与 WAL/SHM 一起留底、
  回滚一个字节不少、裸库文件也能恢复、正在用的库不能当来源。
- 其中 6 项是**现场模拟**（盘与库都是临时目录里的普通文件，不碰任何真盘真库）：盘不在时不列出来、
  插回来照样能恢复、预览之后来源消失、快照只写了一半、坏库现场（软件起不来时直接换库救回、
  坏库原样留证）、现在的库读不出来时仍然允许恢复。
- 壳侧恢复验收 2 项：换库失败必须把原库重新挂上（换了库却打不开软件比不换还糟）、
  换库成功后句柄是关着的，重挂就读到备份里那一版。
- 界面恢复验收 8 项：先落盘再换、体检不过不动作、没选中不动作、换一份来源清掉上一份结论、
  取消选文件不动作、失败可重试、坏库（算不出会丢多少）照样能换。
- 全仓 Rust 227 项、界面 131 项通过；前端构建通过。

---

## 0.24.1 — 2026-09-13

> 修备份面板上被真机试出来的三个问题（都是"看得见的错"，谢谢反馈）。

### 做了什么

- **弹窗不再和正文糊在一起**：备份面板漏引了弹窗的公共样式，点开之后是一团文字直接叠在
  编辑器上。现在跟书架、回收站、设置用同一套遮罩与盒子（居中、有边框、内容超出可滚动）。
- **还没备份过的目标不再被误报「连不上」**：以前把"目标目录还不存在"当成"盘没插"——
  可目录本来就要等第一次备份才创建。现在分开看：**盘在不在**按卷序列号认（真拔了才提示
  "这块盘现在不在"），目录没建只给一句中性说明（"第一次备份会自动创建"）。
- **剩余空间与快照大小的量纲修对**：之前把容量换算写错了，几十 GB 的剩余空间会显示成
  「11 MB」（相反方向也可能把 KB 说成 MB）。现在 KB / MB / GB 一位小数，只在展示层换算一次；
  顺带把"这一份快照多大"也修对了（原来一律显示 0 MB）。
- 「跳过」的原因也分开说：**盘不在**（拔了）与**写不进去**（权限/路径被占）不再混成一句
  「盘没插？」，免得查错方向。

### 验收

- 界面新增容量换算验收（含"50 GB 不该显示成 MB 级小数"这条量纲守卫）；界面 123 项、
  全仓 Rust 211 项通过。

---

## 0.24.0 — 2026-09-12

> 多处备份：把稿子的一致性快照与成稿导出，写到作者指定的几个位置（含 U 盘 / 移动硬盘）。

### 做了什么

- **备份包里有两样东西**：库的**一致性快照**（不是复制正在写的库文件——那种做法会拿到
  不一致的状态、而且不报错），以及**成稿导出**（txt/json，不装研墨也能读）；再带一份清单
  （时间 / 来源机器 / 库格式版本 / 逐书章节数与内容指纹 / 上一份的指纹）。
- **三个触发点**：手动「立即备份」、每天第一次打开、正常关窗（关窗那次异步，不拖慢退出）。
- **写完读回体检**：快照结构、逐书章节数与字数、成稿指纹都对得上才算成功；
  没通过的那一份**不会被当成备份**（删掉并如实记失败）。
- **备份账本存在库之外**：库打不开的时候，也知道备份做没做。设置页一眼看到每个位置
  最后一次成功是哪天、最近 7 天缺了哪几天、上回为什么没成。
- **目标不可达就跳过并记账**（盘没插不会静默补做）；一个位置写不进去不影响别的位置；
  备份失败**绝不阻断写作与关窗**。
- **每个位置保留最近 N 份**（默认 7），滚动清理**只删研墨自己写的旧备份**。
- **还没有第二份时给一条不打断写作的小条**：插个盘，或加一个异地目标；拒绝过就不再自动弹。
- 盘按**卷序列号 + 卷标**识别：只看盘符的话，换个 USB 口就"失联"了。

### 验收

- 核心新增 7 组备份验收：包内容与体检、体检抓得住被改坏的快照与丢失的成稿、
  拔盘跳过且不挡别的目标、保留滚动不误删别人的文件、链式指纹连得上、快照失败逐目标报告、
  配置默认值（一个目标都不预勾）。
- 界面新增 6 项：每日首启只做一次、没目标/关掉自动就不做、关窗才做、拒绝提示会被记住、
  备份失败不吞错。
- 全仓 Rust 测试 211 项、界面 122 项通过。

---

# 更新日志 · 研墨

> **版本规则**：新功能 / 新机制落地记 `0.x.0`；修复 / 优化 / 内部施工批次记 `0.x.y`。
> **写作纪律**：本文件是**公开**的，只写"修复了什么、优化了什么、更新了什么"，且**必须自包含**——
> 不引用内部路径、内部项目名、内部任务编号或私有文档章节。

---

## 0.23.0 — 2026-09-12

> 字数口径**贯通全应用**：目录树、卷与全书合计、书架、回收站里的提示，都跟状态栏用同一个口径。

### 做了什么

- **目录树每章的字数、卷与全书的合计、书架每本书的字数**——全部跟着作者当前选的口径走
  （以前只有状态栏跟，同一章在两处会差出标点那部分）。
- 「按词」口径下这些地方的分量单位是**词**，不再是「字」。
- **老库打开时自动补算**：以前只存了一个口径的数字，现在三个口径各存一份；升级后第一次
  打开会一次性补齐，**不需要一本本点开**，之后的打开不再重算（不拖慢启动）。
- 顺带修掉一处遗留：口径规则修正过（假名/谚文、零宽字符），老库里没再编辑过的章节
  之前还挂着旧数字，这次一并重算。

### 验收

- 新增 3 组验收：三口径在节点/卷合计/书架三处**逐列一致**、三个口径下"合计 = 逐章求和"、
  老库打开即回填（含旧规则算错的假名字数被修正）且只做一次、重复打开数字不变。
- 界面 116 项测试通过；全仓 Rust 测试 202 项通过。

---

## 0.22.0 — 2026-09-12

> 字数有了**三个口径**，状态栏上点一下就能换；作品可以标语言，默认口径跟着它走。

### 做了什么

- **三个字数口径**（都在同一处算，不再各写一份）：
  **逐字（含标点）**——非空白字符全算；**逐字（不含标点）**——只算汉字、假名、谚文、字母、数字；
  **按词**——汉字与假名逐字算，英文数字串算一个词。
- **状态栏那个数字点一下就能换口径**，鼠标停上去会告诉你当前是哪个口径；
  选择会记住，下次打开还是它。
- **作品语言**（中文 / 英文 / 日文，跟书走、不跟界面语言走）：新书默认中文，
  字数默认口径跟着它——中文逐字、日文逐字、英文按词。英文小说不必再手动去换。
- **修了一个口径漏洞**：日文假名与韩文谚文以前被当成"英文串"，一整句只算 1 个词；
  现在按逐字算（写日文、韩文时字数才对得上原稿纸口径）。
- **零宽字符不再计入**：从网页复制来的正文常混进零宽空格之类看不见的字符，以前会把字数撑大。
- 老库打开时**自动升级**（加一列语言，存量书一律按中文），不需要任何手工操作。

### 验收

- 核心新增 6 组字数与语言验收 + 5 组口径单测：三口径在真实读写后一致、假名/谚文逐字、
  全角字母数字、emoji、零宽、缩进空白、语言默认口径、老库升级后是中文。
- 壳层与界面的命令面守卫（"声明 = 注册 = 有网关出口"）保持全绿；界面 116 项测试通过。

---

## 0.21.0 — 2026-09-12

> 命令行多了「找一句话」：不开窗也能在稿子里检索——长词走全文索引，短词自动回退全句扫描。

### 做了什么

- **新增只读命令 `search`**（任何构建都有，与体检、导出同一档）：
  `yanmo-cli --data <目录> search --query <文字> [--work <id>] [--limit <n>]`
  默认在所有书里找，`--work` 只搜一本书，`--limit` 调结果条数（默认 50，上限 500）。
- 每条结果给出**章名、命中片段、标题本身是否命中**；搜不到就是空结果，**不是报错**。
- 中文里两个字太短，全文索引在结构上就表示不了（而「大纲」「伏笔」这类恰恰最常搜），
  所以短查询**自动回退全句扫描**——慢一点，但结果是对的。
- 写错的参数**当场报用法错误**（命令不认识的选项、`--work` 不是数字），不会被静默忽略。

### 验收

- 新增 2 组命令行验收（正例 + 反例）：长短语、两字短词、只在标题里命中、搜不到、按书限定
  五种情形结果都对；缺 `--query`、`--work` 写错、选项名写错都明确报错。
- 该命令**全部只读**：不改稿库、不写文件。

---

## 0.20.0 — 2026-09-12

> 数据位置改由**发布形态**决定：带便携标记的版本，数据就放在程序旁边的 `data/`；没标记的照旧。

### 做了什么

- **便携判定**：程序目录里出现 `yanmo-portable.txt` 时，数据目录就是 `<程序目录>/data`；**没有这个标记一律走系统数据目录——默认行为完全不变**。
  用标记而不是"程序目录能不能写"来自动判断，是因为 exe 被单独放到桌面或下载目录时，那些目录**通常也可写**——自动判定会把稿库落到桌面，那是最容易被误删、最容易被同步盘扫到的位置。
- **写权限真的试一次**（建目录 → 写探针 → 删掉）：便携目录写不进去时**明确报错并给人话提示**（"请把研墨放到你有写权限的位置"），绝不静默换地方——否则作者会以为稿子在程序旁边，实际却写去了别处。
- 命令行救援工具用**同一份判定**找库——两个壳认同同一个位置，不会一个找得到、一个找不到。
- 路径约定集中在核心（`paths` 模块）：桌面程序与命令行不再各写一份"数据在哪、导出到哪"。

### 验收

- 核心单测覆盖：没有标记 → 系统数据目录；有标记 → `<程序目录>/data`；写权限探测会真的写入探针文件并清理干净（不留垃圾在作者目录里）。
- 全仓测试全绿。
- 默认可执行文件所在目录里没有标记 → 位置与上一版**完全一致**，不产生任何迁移。

### 关键决策

- **便携与否由发布形态决定，不由文件位置猜**：这样"单文件 exe 放桌面"永远不会把数据库落到桌面。
- **写不进去就报错，不悄悄换地方**：数据位置这种事，静默回落比报错危险得多。

---

## 0.19.0 — 2026-09-12

> 命令行入口改成**给作者用的备用导出工具**：双击打开就是中文菜单，不用记任何参数。

### 做了什么

- **不带参数运行 = 中文菜单**：读取稿子（列书 → 列章 → 看正文，确认字还在）/ 检查数据库健康度 / 导出某一本或某一篇 / 导出全部 / 退出。作者不需要知道任何命令，也不需要看文档。
- **数据目录自动找**：按系统约定去找（Windows 的 `%APPDATA%`、macOS 的 `Application Support`、Linux 的 `~/.local/share`）；找不到就请作者粘贴路径，并告诉他去哪儿看（桌面程序顶栏那行小字）。**绝不静默新建一个空库**——那是救援工具最不可原谅的失败。
- **导出落点沿用桌面程序的同一份约定**（`文档/YanmoExport`；早先版本用过的 `研墨导出` 还在就继续用它），并且导到 `RescueExport` 子目录里，不动作者已有的导出。
- **定位写明**：这是**极限灾难情况**（显卡崩溃、图形界面崩溃、驱动失活等）下的备用导出工具。它只读与导出，一条能改稿库的命令都没有。
- 路径约定**下沉到核心**（`paths` 模块）：桌面程序与命令行不再各写一份"数据在哪、导出到哪"——两个壳认同同一份约定，免得改一处漏一处。

### 验收

- 命令行入口共 19 条测试全绿，其中菜单 8 条：体检说人话 / 读稿能显示正文 / 导出单本与导出全部 / 乱输入不崩也不退出 / **目录里没有库时绝不建空库** / 库在就不追问作者 / 只给 `--data` 也能进菜单。
- 桌面程序改用共享路径约定后，全仓测试全绿（默认导出目录的选择行为有单测盯着）。

### 关键决策

- **面向作者，而不是面向脚本**：脚本能力（JSON 命令 + 退出码）保留不变，但**默认路径**是"双击 → 中文菜单"。
- **绝不静默建空库**：救援工具里最贵的错误，是让一个正着急的作者以为稿子没了。
- **两个壳共享路径约定**：这类"磁盘上叫什么名、放在哪个目录"的约定一旦各写一份，迟早漂移；漂移的代价由作者承担。

---

## 0.18.0 — 2026-09-12

> 新增**命令行入口**（`yanmo-cli`）：界面起不来的时候，还有一条不依赖界面的路把稿子取出来。
> 发行版里它**只能看和导出，改不了稿库**；会写库的命令只在开发构建里存在。

### 做了什么

- **新增命令行入口**，且**按构建分两档命令面**：
  - **发行版（只读 + 导出）**：`verify`（库完整性 + 结构版本）/ `works`（列出书架上的书）/ `nodes`（列一本书的目录，只有元数据）/ `read`（读一章正文）/ `export`（把一本书导出成文件）。**没有一条能修改稿库。**
  - **开发构建**（额外有）：`begin` / `report` / `note-open` / `write` / `fingerprint` / `end` / `abandon` / `new-work` / `new-node` / `hold`——给自动化与故障复现用（例如"写到一半把进程杀掉、重启看稿子在不在"）。这部分代码在发布构建里**根本不编译**，不是藏起来。
- **输出统一 JSON**：成功 `{"ok":true,…}`；失败 `{"ok":false,"code":…,"params":{…}}`。退出码分三种：成功 / 命令失败 / 用法错误。**写错命令或选项当场报错**，绝不静默忽略——脚本最怕的就是"参数写错了却照跑"。
- **数据目录由 `--data` 明确给出**：命令行程序不猜路径，与桌面程序各用各的，互不干扰。
- 核心顺带补上两个驱动面：**只读**的会话交代（看一眼不会把"正在运行"的标记盖掉）与**完整性快检**（`PRAGMA quick_check` 的结论，供体检与自动化判断）。
- 边界守卫加了一条：命令行入口**不得依赖界面框架，也不得依赖网络库**。
- 新增 `SECURITY.md`：把"命令行入口有哪些命令、发行版为什么改不了稿、你怎么自己验证"写明——这类能力应当可以被核对，而不是只写在更新日志里。

### 验收

- `cargo test --workspace` 全绿；命令行入口自带 6 条端到端用例：建书建章写读一条龙 / 书架列表 / 「上次没正常退出」判得出来且只读看一眼不盖状态 / 体检结论 / 导出落盘 / 写错的命令与选项报用法错误。
- **发布构建实测**：`--help` 只列上面那 5 条；拿 `write` 去调会被明确拒绝（`cli.usage`，退出码 2）；带测试特性的调试参数在发布构建里同样报"不认识选项"。
- 依赖树只有核心与 JSON 序列化，**没有任何网络库**。

### 关键决策

- **入口做成独立程序，而不是给桌面程序加无窗模式**：它不该拖着界面依赖，也不该把"没有窗口也能干活"长进壳里。
- **发行版的命令面收得比"救援所需"更紧**：一律只读或只写文件。多一个能改稿库的官方入口，就多一份"被脚本误用、绕过界面上的确认"的风险——而这个风险换不来作者的任何日常收益。
- **会写库的能力不藏、但也不发**：开发构建里保留（自动化与复现要用），发布构建里连代码都不编译——"藏起来"和"不存在"是两回事。

---

## 0.17.0 — 2026-09-12

> 新增**每章版本历史**：系统替你自动留版本，也可以亲手留一版；任意两版可对比差异，一键回滚。

### 做了什么

- **自动留版本**：关窗、落盘卡住、库里的正文与手上这份对不上时，先把这一章存成一版。**同一份内容不重复留**——关窗是高频动作，每关一次都堆一版的话，版本表很快就没法看了。
- **滚动保留**：每章自动版本只留最近 20 份，更老的自动清掉；**你亲手留的版本不在清理范围内**，一份都不会少。
- **手动留一版**：正在写的这一章可以随时「留一版」，用来标记"这一段改好了、这块可以放心了"。
- **差异对比**：选任意一版，看它和当前正文差在哪。段落级逐行对比：删掉的标红、新写的标绿，相同的段落折起来，只在差异前后留几行上下文——改一版通常只动中间几段，这样才读得下去。
- **回滚**：一键把正文换回某一版。**回滚前会先替当前这一版留个底**，所以回滚错了还能再换回来；回滚同时把字数与全文检索索引一起带回旧版本。
- **入口**：编辑器里章名那一行新增「版本」按钮（版本跟当前这一章绑定，换章就跟着换）。
- 版本历史落在数据格式 v3：**旧库打开时自动升级，不需要手动迁移，也不会动已有数据**。

### 验收

- 核心验收用例 6 条全绿：列表只给摘要且新的在前 / 滚动保留只清自动版本、手动版本一份不动 / 内容没变不留重复版本 / 回滚先留底并把正文、字数、检索索引一起带回 / 删除某一版不动正文 / 版本不存在或节点已进回收站时明确报错且不留垃圾。
- 差异算法单元测试 5 条：相同文本没有差异行、中间改一行、纯新增、空文档、超大文本降级为整块增删（不拿内存去赌）。
- 界面状态机单元测试 7 条：打开即比最新一版、内容没变不留重复版本、回滚先落盘再回滚、**落盘失败时一个回滚动作都不发**、删掉或已被清理的那一版不会把差异留在屏幕上。
- 全仓测试与界面测试全部通过。

### 关键决策

- **滚动保留只清自动版本，手动版本不设上限**：清理的边界必须是"系统替你记的可以扔，你亲手留的不能扔"。
- **回滚先留底再覆盖**：回滚本身也是一次覆盖，顺序与抢救一致——先保险，再动手。
- **不另建表**：版本与既有的自动快照共用一张表，只加一个「手动留」标记；差异算法自己实现（几十行、无第三方依赖），不为一个小功能引一个库。

---

## 0.16.2 — 2026-09-12

> 本节**没有代码改动**：调研了「把写作流程本身做成可复用资产」（SOP）的做法，并把相关能力**先登记、不实现**——等它真正需要的基础落地再动工，免得做出来是悬空的。

### 为什么先登记、不动手

一套「个人写作流程」要有用，得先有承载它的东西（素材/创作流面板、大纲、提问系统）；否则做出来只是一个孤立的面板，谁也不会天天去开。所以这次把能力记下来并**挂上依赖条件**：等基础落地时，**第一件事是接通，而不是直接开工**。

### 调研到的行业定义（值得记下来）

主流的 SOP 写法有**六要素**：**目的 / 适用范围 / 职责 / 步骤 / 引用 / 附录**；另有两条纪律——**版本控制与修订历史**、**用模板复用而不是每次从零写**。对照我们已有的能力，几乎一一对得上：目的对应作品题旨、适用范围对应"哪本书用哪份流程"、职责在单人工具里就是"人 vs 模块"、引用对应人物与设定、附录对应素材；**唯一缺的就是"步骤"本身**。

### 登记下来的能力（暂不实现）

- **写作流程本身成为一等资产**：步骤、检查项、可选的跳转；**跟人走**（写作方式因人而异），但可以**按作品覆盖**——沿用已有的两层继承机制，不新建第二套设置。
- **流程面板与运行状态**：记住走到哪一步、可跳过可暂停、跨天接着来；**只提示、不催办**（不做倒计时、不做打卡惩罚、不把"没走完"写成失败）。
- **模板的创建与导入导出**：模板是**数据不是代码**（导入的东西绝不会被执行）；导出幂等（同样内容永远同样的字节）、格式自带版本、来源可查；**导入后不自动生效**，先看再用。
- **内置写作模板**：把此前调研里已经录入的几种成熟方法论（雪花写作法十步、七步法、网文黄金三章等）做成**填空向导**；流程模板与结构模板是两种数据，不混在一起。
- **提问按作者自己的流程排序**：作者**显式声明的写作流程**比系统慢慢猜出来的偏好更可信——用它决定"现在该问哪一类"，例如写初稿那一步就**不推结构类问题**。它只影响排序与时机，既有的克制纪律（主动/被动分开、每日配额、只问不写）一条都不动；**没有设流程的用户，排序结果与现在完全一致**。

### 验收

- 只登记与调研，无代码改动；登记项已挂依赖条件，并在它所依赖的能力上留了「落地时解锁并接通」的提醒。

---

## 0.16.1 — 2026-09-12

> 一次**针对「界面文字什么时候会显示崩坏」的压力测试与加固**：专门找"什么情况下会变成方块、乱码，或者把版式挤坏"，找到一处修一处，并把防复发的机械校验补上。

### 压测怎么做的

在真机上用调试协议直接驱动界面：① 用画布逐字比对"未分配码位"的像素，**机械判定字体缺字**（不靠肉眼）；② 把二十多类极端字符灌进已知文本，再看渲染结果；③ 往真实元素里塞超长名字、方向覆盖符、换行，量行高与横向溢出；④ 直接给内核发非法数据，看界面拿到的是什么。

### 压出来的问题（都已修）

- **粘贴里有「半个字符」时，界面会弹一句英文解析错误。** 界面这一侧的字符串允许只留一半代理项（典型场景：粘贴的表情符号被截断），而另一侧的 JSON 解析器直接拒收整段文字，只回一句英文——用户看到的就是"读不出来"。现在参数在过边界前先把落单的一半去掉（**正常的表情符号不受影响**），操作照常完成；万一还有别的数据读不出来，提示也是中文句子，原始文本留在括号里供排查。
- **报错信息一长，章名就被挤没。** 实测：往标题栏右侧那一格塞 300 字，章名宽度会被压到 **0**，整条栏还横向溢出近 1900 像素。现在那一格最多占半条栏、超出用省略号，完整内容鼠标悬停可看；实测章名宽度恢复到 298 像素、横向不再溢出。
- **时间戳损坏会显示 `NaN-NaN-NaN`**（或把很远的未来时间说成"今天"）。现在这类值一律显示"时间未知"；字数不合法显示"字数未知"。
- **外壳有 8 处错误文案永远填不上技术原因**：像"无法创建目录 X：{detail}"里那个 `{detail}` 从来没被交上来——磁盘满、没权限这类真正有用的信息反而看不见。这是上一版拆分文案时留下的漏洞，已修好。

### 防复发的机械校验（这次的重点）

- **参数名对表**：每个错误码在码表里**声明自己允许带哪些参数**，调用点给的必须与之一致（不一致在开发构建里当场失败）；界面字典里那句文案的占位符，也只能是这些名字。少了这道校验，改一个参数名就会让界面静默显示成 `{node_id}`——不报错、不空白，只是把内部名字露给作者看。
- **外壳的 `{detail}` 必须真的接上**：文案里写了 `{detail}` 的码，调用点必须把底层原因交上来，否则测试红。
- **文案缺少参数当场喊一声**：渲染时若模板里还有没填上的占位符，控制台立刻警告（同一句只报一次），不让它静默留在界面上。
- **字典字节体检**：查出带 BOM、不是合法 UTF-8、已经出现乱码替换字符、键重复、空值、占位符写歪——这几样都会让整片界面变乱码，而且都是"另存为"一下就发生的事。

### 验收

- **字体缺字**：常用汉字、扩展 A/B/C/E 汉字、日文假名、谚文、阿拉伯文、天城文、泰文、数学符号、表情符号（含组合）全部正常显示；只有**私用区**码位显示为方块——那是字体库里本来就没有的码位，只可能来自使用者在别处复制的内容。
- **已渲染的界面上**：乱码替换字符 0 个、没填上的占位符 0 个、未翻译的键名 0 个、控制字符 0 个。
- 内核、外壳、界面的全部测试与生产构建通过。

---

## 0.16.0 — 2026-09-12

> 这一节是**语言层解耦**：把「界面该说的话」从核心与数据里挪出来，集中到一处。
> **只搬家，不翻译**——界面上你看到的中文一字未改。

### 核心不再生产界面文案：只给「码 + 参数」

- 核心报错从「一句中文」改成 **错误码 + 参数**（例如 `work.not_trashed` + `work_id=7`），由界面查字典渲染成句子。这样「核心零界面依赖」才算真的成立：**换语言不用改核心**，模块的报错也不必由核心替它说话。
- 界面渲染只有一条路径：拿码查字典、用参数填模板。**码表有穷举测试盯着**——核心与外壳产生的每一个码都必须在字典里查到，字典里也不许有对不上的孤儿条目。漏一个的下场是界面上出现「未知错误」，那是最难查的一类故障。
- 外壳自身（命令层、文件写入）的失败走同一形状，不再自己拼中文句子；只有开发日志照旧是中文句子——那是给人排查问题看的，与界面文案是两件事。

### 数据里不塞文案

- **新建长篇不再往库里写「第一卷」这样的默认卷名**，首次运行也不再写死一个默认书名：名字由作者起，界面按当前语言补占位（卷显示成「第 1 卷」）。**已经存在的名字一个字都不动**——那是你的数据。
- **写到磁盘上的名字与界面语言无关**：空书导出的说明文件改叫 `empty.txt`；没有名字的容器用结构标识（`volume` / `chapter`）兜底；文件名兜底从中文改成 `untitled`。
- 导出目录换了名字，**但老目录还在就继续用它**——不会让你已经导出的东西看上去「丢了」。

### 界面文案集中到一处

- 界面文字全部搬进 `app/frontend/src/locales/zh-Hans.json`，组件与编辑器逻辑统一从 `t(键, 参数)` 取字。语言码用 `zh-Hans`，为将来简繁分开留位。
- **没有引入 i18n 框架**：只有一种语言时引库是过度设计；键的形状与主流方案兼容，将来真要加语言，是「加一份字典」，不是「改一遍代码」。

### 防回潮守卫

- 新增一项自动检查：界面文案必须住在字典里，**扫出没搬进去的硬编码即失败**（注释与测试数据不算数）。确实不该搬的（开发日志模板、磁盘上已有的目录名、产品名）必须**写明理由**才放行。

### 验收

- 核心与外壳全量测试通过（含新增的码表穷举与错误形状测试）；前端测试与生产构建通过；新增的文案守卫在仓库上跑出 0 命中。

---

## 0.15.11 — 2026-09-12

> 本节**没有代码改动**：把这一批讨论里够格当「公开立场」的四条写下来。其余属工程细节，留到模块系统真正动工时按实现写——**先做出来，再谈规则**。

### 立场：模块不是可执行程序，是沙箱载荷

- 模块**不再是一个能自己跑的程序**，而是统一打包标准的**沙箱载荷**——脱离本应用，它**无法自行运行**（双击没用）。
- 真正的收益不是「更安全一点」，而是**把最坏情况的形状改小**：从「你的电脑被控制」压到「**你授权范围内的数据可能被读走**」。而「**模块改不坏你的稿子**」这条保持不变。
- 我们是从别人的事故里学到这一点的：**「弹风险警告 + 让愿意冒险的用户自己负责」这条路最终会失败**——一旦允许任何人分发可执行程序，出事时只能把整个功能永久砍掉。我们选择从一开始就不走那条路。

### 立场：认证可以被伪造，所以我们只承诺「可核对」

- **认证标识是能被伪造的**——我们把这件事直接说出来，而不是假装它牢不可破。三条路：模块自己撒这个谎、任何人编译一个「全认证版」的客户端、山寨渠道。前两条我们**拦不住**，第三条只能靠商标与用户教育。
- 所以认证的含义是：**我们审过它、签了名、它扛过压力测试、它承诺不越权、来源可查**——**不是**「我们查过它没有病毒」，**也不是**「不可能假冒」。
- 我们能做的是让**核对**这件事变简单：认证绑定到模块文件的**指纹**（内容一变、认证自动失效）、标识**由主程序渲染**（模块无权自己画「官方」字样）、标识旁给**短指纹**与「怎么自己核对」的方法、并建议认证模块做**代码签名**。
- 一句话：**我们不承诺「不可能假」，我们承诺「你来核对，能核对出来」。**

### 立场：创作自证是「如实记录过程」，不是「防 AI 检测」

- 如果作者用 AI 生成整章再粘贴进来，我们的记录**不会失效，而是会把它如实记下来**——因为记录看的是**过程**（版本演进、输入来源、有没有大段插入），不是文本特征。**从空章到一整章「一跳完成」、中间没有任何状态，这恰恰是最显眼的痕迹。**
- 我们能证明的从来不是「这是人脑想出来的」，而是「**这段文字的产生过程被如实记录，且记录未被篡改**」。
  **能证明**：时间线、编辑事件序列、是否整段出现、粘贴量、当时有没有模块在运行、记录链有没有断。
  **不能证明**：思想来源、是否 AI 生成、是否他人代打、是否口述。
- 把这两句分开写，是底线：**不卖假的安心。**
- 因此我们**不会**把它宣传成「防 AI 检测」——写作平台的判定依据是文本特征与它自己的模型，**不看我们的本地记录，我们改变不了平台的判定**。一个能**如实、可核对地说明 AI 参与情况**的工具，比一个声称「证明我完全没用 AI」的工具更值钱，也更诚实。

### 立场：核心不判断授权，只当传声筒

- 主程序**不知道谁付了钱**，也不做任何「你有没有买」的判断。模块是不是正版、有没有解锁，由**模块自己**验证与说明；主程序只负责**把你的操作、模块的话、以及它自己观测到的事实**呈现给你，并标明**哪句话是谁说的**。
- 推论：把一个已购买的模块复制到另一台机器，**主程序既不阻止也不判断**（文件能被复制，这是拷贝的本质）；它仍然显示为**认证模块**——因为认证说的是「这份代码是我们签发的」，不是「这个人付了钱」——而需要授权的功能由模块自己说明情况、并说清怎么购买。
- 这条是为了保住一个更重要的承诺：**开源版永远不检查授权，也永远不会因为「卖模块」而在核心里多出一行付费逻辑。**

---

## 0.15.10 — 2026-09-12

> 本节同样是**登记**，但内容是这一批里最重的一档：**模块的沙箱与安全加固**（面向未来，不影响现在的本体）。

### 决定：模块不是可执行程序，而是沙箱载荷

- **模块不再是一个能自己跑的程序**，改成统一打包标准的**沙箱载荷**——脱离本应用，它**无法自行运行**（双击没用）。
- 真正的收益不是「更安全一点」，而是**破坏半径变了**：最坏情况从「你的电脑被控制」压到「**你授权范围内的数据可能被读走**」。而「**模块改不坏你的稿子**」这条保持不变。
- 技术选型：**WebAssembly + 组件模型**描述的接口；跨平台——一份载荷在 Windows / macOS / Linux、x86 / ARM 通用，**将来出别的平台，插件不用重发**。执行放在**独立的宿主进程**里，与主程序隔离。
- 语言档：**先只官方支持 Rust**（编译后接近原生性能）；之后再做一档 JavaScript，**明确标注「慢档」**（解释器路线，明显更慢），让作者自己权衡。**不开「原生程序」这一档。**
- 代价照实说：插件作者的门槛比「扔个程序过来」高；需要调用本地大模型、语音合成、特殊文件格式这类原生能力的事，沙箱做不到。

### 决定：沙箱的「膜」——靠结构，不靠小心

- **不把任何宿主对象交给模块**，哪怕它是「只读的」也不行；模块能拿到的只有**字符串和数字**；宿主能力用**一张表 + 编号**暴露。
- **不给模块文件系统**：它根本没有「读文件」这个调用；需要文件时由**宿主代读**（就像浏览器让你「选文件」那样），而且每次都留痕。
- 这条纪律是结构性的：模块那一侧**没有对象可以顺着往下摸**，所以不存在「小心一点就不会漏」这种依赖人的前提。

### 决定：逃逸审计是认证的硬门槛

- 认证必须跑过一份**逃逸审计清单**：环境权限、经典越狱手法、边界上的具体陷阱、资源上限（比如死循环能不能被强行中断、中断后运行时还能不能用）。
- 清单**公开，第三方可以自己先跑一遍**——不做黑箱背书。并且必须写清「什么样算通过」：这份清单自己也会骗人，曾经因为「某个属性应该是空的、实际不是」而误报过。

### 决定：读数据要留痕；未认证的模块读多了要问你三次

- 核心侧记录**谁、什么时候、读了你哪些书、读了多少字**——**只读也要留痕**。
- 新装的未认证第三方模块**首次只能拿到一篇文章**；系统会主动告诉你「它是未认证的第三方模块、出于数据安全我们只给了一篇、是哪一篇」。
- 短期内再要 → 再问一次；**第三次**会明确告诉你「这是第三次高频获取你的内容，**本次允许后系统不再介入**」——你确认，就放手，之后不再拦。
- 但**放开 ≠ 不再记录**：留痕与告警继续，你随时可以在管理页收回。
- **认证模块免走这套**（这正是认证的价值所在），但留痕与告警照旧。
- 另有**恐慌按钮**：一键停用所有第三方模块，本体照常可用。

### 决定：插件不得静默自动更新

- 更新必须**你确认**（显示版本号与变更说明），不后台替换；**权限声明一旦扩大必须重新确认**（跟手机应用加权限一个道理）。
- 理由很直接：**静默自动更新是供应链投毒的标准剧本**——作者账号被盗、推一个恶意小版本、所有用户自动中招。
- 更新支持**回滚**（保留上一版本，出问题能退回去）。

### 红线：认证不是安全认证

- 认证的意思是：**我们审过它、签了名、它扛过压力测试、它承诺不越权、来源可查**。
- 认证**不是**「我们查过它没有病毒」。这句会写死在公开文档里——否则认证本身可能变成最大的误导工具，**比没有认证更坏**。
- 未签名的模块，安装时会明确提示「没有签名，无法核验来源」。

### 登记：平台责任的选择

- 现在「不做商店、只做发现入口」是稳妥的位置（不承担分发方的责任）。但**发现入口一旦存在，就带上了推荐性质**。
- 生态真正起来时，要在「做商店（要背审核、下架、投诉与责任）」和「只提供协议、不提供目录」之间**清醒地选一次**——别糊里糊涂成了分发方。

---

## 0.15.9 — 2026-09-12

> 本节仍是**登记**：把模块生态里「生命周期 / 链路 / 提问 / 商业化」的口径定完（面向未来，不影响现在的本体）。

### 决定：唤起链路按「人」不按「书」，断掉是持久的

- 切断一次**全库生效**，不用为每本书断一遍；每次唤起带着「当前是哪本书」的上下文。
- 断掉之后**重启仍然是断的**，一直断到你自己接回去。

### 决定：卸载模块时数据默认保留

- 卸载对话框三选一：**只卸载、数据保留**（默认）／**卸载并导出一份**／**彻底删除**（二次确认，说清删什么、多大）。
- 顺便把词分清楚：**冻结 ≠ 卸载**——冻结只是停用，数据原样在；卸载是移除程序。

### 决定：模块的提问与你的提问走同一个出口

- 模块可以提问（在权限声明里勾了这一项才行），但**必须标明来自哪个模块**，**不得冒用本应用的名义、图标与口吻**。
- 每模块有**频率上限**，超了就丢弃并记录，不排队堆积。
- 你可以「**不再听这个模块的提问**」——只静音它，不影响别的模块，也不影响本应用自己的提问。
- 未激活、或没声明这一项的模块，**一次都提不进来**；模块提问**不得挤掉或打断**本应用自己的提问。

### 决定：模块怎么卖

- **一个模块一份通行证，一次性买断、永久有效**。不做订阅、不在模块内部再分档、不拆功能包。
- **通行证不绑版本**：它只认「哪个模块 + 哪台机器」。要卖大版本的新东西，就**发新模块**——绝不给老通行证加版本上限（加上限就是「停用」，我们不干这个）。**也不许把原本一个模块里的功能拆成两个分别收费。**
- **换机免费重发（默认 2 次）**：凭原码或购买凭据人工核对；**用完了可以商量**（宁可宽松）。不承诺时限，写「尽力尽快」。
- **应用内不收款、不登录、不校验**：未授权时只放一段「怎么买」的说明与外部链接，你自己在浏览器里完成。分发目前是手动交付；**下载不等于授权**（通行证才是授权）。
- 一条纪律：**收钱的流程永远不进核心。**

---

## 0.15.8 — 2026-09-12

> 本节同样**没有代码改动**：登记「模块管理页」与模块生命周期规则（面向未来）。

### 决定：模块管理页——一个页面管所有模块

- 不搞「每个模块一套自己的设置面板」——**一个页面**统一管：图标、名字、来源与作者、版本、
  **认证状态**（认证 / 未认证）、**激活状态**、**联网方式**（走统一出口 / 自行联网 / 已禁止），一眼分得清。
- 可以**收藏**、**置顶**——只是列表排序与收纳偏好，不影响功能，也不会跟着作品导出。
- 两个开关：
  - **后台运行（允许 / 禁止）**：默认禁止。
  - **联网（允许 / 禁止）**：模块级开关，压在「全局离线开关」之下——
    优先级是 **全局离线 > 单模块禁止 > 你的同意 > 模块自己的声明**；被拦下的调用在日志里看得到。
- **开关只管策略，不当立即动作**：点「禁止后台运行」不会掐掉正在跑的东西，只是不许它下次自己起来。
  想立刻结束，用旁边的**「停止运行」**按钮——任何正在运行的模块都有它，会先请对方优雅退出，超时再强制结束。
- 一个页面也集中处理：模块数据**存在哪、一键导出**、**卸载**（默认保留数据并告诉你位置）。

### 决定：模块不许私自启动；未激活即冻结

- **默认任何模块都不许自行启动**——没有外部触发就常驻，等于偷你的性能和电。
  只有你明确给了「允许后台运行」的模块，才可以自己在后台跑。
- 允许启动的来源只有三条：**你主动打开它** / **你主动触发的动作** / **被其他模块唤起**（受下一条约束）；
  其余一律拒绝并留痕。
- **未激活 = 冻结**：调不动、唤不醒、不占进程，**只保留历史数据和图标**（数据不删、导出照旧）。
  「保留图标」指的是管理页里的记录——写作界面里不会给它留任何入口。
- **激活是你的开关，认证是我们的背书，两件事别混**：已激活但未认证的模块照样能跑（界面标明未认证），
  未激活的一律冻结。
- 停用时**干净退出**：不留残进程、不半途写坏数据。

### 决定：唤起链路可见、可断

- 模块之间可以互相调用，但**谁唤起了谁必须看得见**：调用关系画成一张图，
  带调用次数、最近时间、耗时、被拒次数。
- 你可以**切断任意一条链路**（比如不许 A 再唤起 B）。断链后那个唤起会被**明确拒绝**，
  调用方收到「用户已禁止这条链路」，界面看得见、日志有痕，但它自己不会崩。
  **断掉是持久的**——一直断到你自己接回去。
- **防频繁唤醒**：每条链路有频率上限与冷却时间；超了自动降级为「需要你确认」或直接拒绝，
  并在链路区标出来通知你——谁在骚扰，一眼看到。
- 没在权限声明里写过的调用链路，**一次都过不去**（不用你自己动手断）。

---

## 0.15.7 — 2026-09-12

> 本节**没有代码改动**：只把「模块生态」的四条地基级口径定死（面向未来，不影响现在的本体）。

### 决定：模块长什么样、界面怎么出现

- 一个模块 = **一个目录**（一份清单文件 + 模块自己的可执行程序）。应用**用到它时才启动它**，
  不用的模块不占内存。清单里写清：标识、名字与来源、版本、入口、**协议版本**、
  要哪些数据（只读）、要不要联网、界面形态、认证状态。
- 界面分三档，由模块在清单里声明：
  - **内嵌**（主线）：模块自己提供页面，宿主给它一块内嵌浏览器区域，看起来就是应用的一部分
    （可以做成侧栏 / 抽屉 / 弹窗 / 全屏工作台）；
  - **独立窗口**（兼容档）：模块自己开窗口，留给「本来就是个大工具」的模块；
  - **无界面**：只做后台处理的小模块。
- ★ 配套一条**硬约束**：内嵌的那块浏览器区域**不得访问宿主的能力接口**——
  它只能读模块自己提供的内容，需要数据得**向宿主申请**。
  否则一个页面的脚本就能反过来操控整个应用。实现时必须专门验证这一点。
- 明确**不做**「模块只出数据、由宿主渲染」这一档：那等于把每个模块的概念都长进宿主肚子里。

### 决定：权限分三档

| 身份 | 你的稿子（核心数据） | 它自己的数据 | 联网 |
| --- | --- | --- | --- |
| 核心 | 读写（唯一真相源） | — | 从不出网 |
| 应用外壳 | 读写 | — | 从不出网 |
| **模块** | **只读** | 自己的区域可读写 | 认证模块走统一出口 / 未认证的自行联网（明确标注） |

口径一句话：**客人不动主人家的稿子。**

### 决定：按库安装，按书使用

- 模块**装一次**（跟人跟机器，不跟作品）；但它的数据**按作品分**，
  每条数据都带作品标识，索引也带——**也允许「跨作品」的数据**（比如写作习惯统计、词典）
  单独存放，不硬塞进某一本书。
- 模块界面默认**跟着你当前打开的书走**（切书它自己换数据）；也允许做成不跟书的全局面板。
- 导出作品时，按书带走该模块在这本书里的数据；跨作品的那部分单独出。

### 决定：对「未认证模块」说实话

- **认证的意思是**：我们审过它、签了名、它在压力测试下跑过、它承诺不越权——
  所以你能**一眼分清谁审过、谁没审过**。
- **未认证的意思是**：它就是一个跑在你电脑上的独立程序，和你从网上下的任何程序一样。
  它能不能读你的稿子，**取决于它守不守规矩，不取决于我们拦不拦得住**。
- 我们能做的实事只有：不给它写接口、不给它宿主权限、不让它碰你的密钥。
- 所以结论写在明面上：**别装来路不明的模块**——跟别装来路不明的程序是同一句话。

---

## 0.15.6 — 2026-09-12

### 登记：核心能力的请求与赞助（收劳动，不收租）

- 模块作者与第三方可以**提能力请求**（核心还没有的只读数据、通用钩子…），也可以**赞助**让它更快实现——
  这是一种比抽成干净的形态：收的是**开发劳动**，不是平台租金。
- 赞助能得到：**优先级 + 参与设计讨论**。赞助**换不到**：
  ① **数据与安全边界**（判据审不过，出钱也不做）；
  ② **排他**（做出来的能力所有模块一视同仁）；
  ③ **专有权利**（成果按 AGPL 并入核心、开源给所有人；想独占就自己 fork）；
  ④ **运行时特权**（核心不会因此多出任何授权检查）。
- 流程公开：请求、结论（做 / 不做 / 为什么）、赞助状态都写在公开处；
  **路线仍由项目自己定——赞助可以插队，不能决定路线**。
- 配套：能力清单**按版本演进**——模块声明"需要能力 vN"，核心版本不够时明确提示用户"请更新研墨"，
  而不是静默失败。

---

## 0.15.5 — 2026-09-12

### 登记：第三方模块生态的两条边界，与「目前不抽成」

- **目前不对第三方模块抽成**：核心是 AGPL，第三方可以做自己的壳，也可以做自己的模块（闭源、收费都行），
  **我们不要分成**。将来若提供官方商店、收款、认证这类增值服务，会**事先把规则说清楚**——
  愿意用就用，不愿意用照样能自己分发。
- **两条硬边界**（比抽成重要得多）：
  ① **不得使用「研墨」作为产品名或图标**（官方壳才是研墨，免得用户被误导）；
  ② 模块必须**标明来源与作者**，并声明它需要哪些数据权限。
- 顺便一句邀请：如果某件事是**人人都需要**的基础能力，欢迎提 issue —— 它很可能应该进核心、
  **永远免费**，而不是做成模块。

---

## 0.15.4 — 2026-09-12

### 登记：什么进核心、什么做模块（边界判据）

- **判据（不是功能清单**——清单会过时，判据能一直用）。按顺序问，命中即定：
  ① 动到你的**稿子数据**（写正文 / 树结构 / 数据模型 / 导出）→ **核心免费**；
  ② 在**写作热路径**上（击键 / 渲染 / 落盘 / 切章 / 光标）→ **核心免费**；
  ③ **没有它会丢稿或被锁** → **核心免费**；
  ④ 要**联网 / 模型 / 第三方服务** → **模块**（核心保持零出网）；
  ⑤ **人人都要** → 倾向核心免费；特定题材 / 工作流 / 专业向 → 模块；
  ⑥ 只是「**视图 / 组织 / 输出**」（读数据、不写数据）→ 模块；
  ⑦ **变化很快**、要不停跟外部世界对接 → 模块（独立发版，不拖核心节奏）；
  ⑧ **拿不准** → 先放**核心免费**。要单独卖的东西必须做成模块。
- **发布即免费（焊死）**：一个能力只要随核心**发布过**（哪怕只有一次），就**永远属于核心免费**——
  不会哪天被搬进付费模块。这条可以机械核对（更新日志与提交历史都在）。想卖钱，只能去做**新的**能力。
- **反向（模块并入核心）是允许的、甚至是好事**：并入后**已购用户的通行证继续有效**，该模块随后停售。
- 同一个模块可以有「**免费档 + 付费档**」，但**免费档承诺永久**——发过的功能不砍。
- **第三方可以发布模块**（含免费模块）：权限同样走能力清单，但**必须在界面与文档里标明来源与作者，
  不得使用官方名称与图标**（官方壳才是"研墨"）。
- 核心的角色是**模块宿主**：它知道装了哪些模块、给什么只读能力，但**不知道谁付了钱**
  （授权由模块自己离线核对）——所以开源核心里永远不会出现「你还没买」这种字。

---

## 0.15.3 — 2026-09-12

### 登记：增值模块的生态政策（面向未来，不影响本体）

- **允许改**：核心自己改、自己用，**不必开源**（AGPL 只在**分发**时触发义务）；模块个人自用随便折腾，
  但**不提供支持、不得再分发**；模块会尽量留**公开扩展点**，让用户不碰二进制也能加自己的东西。
- **单独定制：接，但设四道护栏**——不进主线；只基于公开扩展点；按工量计价、维护另计；
  同一需求被问到 2–3 次就做成标准模块（大家买，比一对一便宜）。
- **买断的更新承诺**：**永久可用**；**修 bug 免费**；**适配新版核心免费**（核心升级不能变成逼用户重复付费，
  这是最容易招骂的一点）；跨大版本的新能力可选收费升级，**旧版继续可用、绝不停用**；
  即使某个模块停更，**已签发的通行证继续有效**。
- **用户建议**：**永远免费进核心**的是写作本体、数据安全、核心编辑体验——绝不把这些从免费挪进付费；
  被采纳的建议若做成模块，**送该模块通行证 + 公开致谢**（也**不做**"这点子只有你提过"的独家承诺）。
- **贡献与许可**：提意见 / 建议**不需要签任何协议**；核心代码贡献走现有的贡献者许可协议——
  它已允许以 AGPL **或商业许可**再许可，**双许可自由因此保住**；**付费模块仓库只收 issue / 建议、
  不收代码 PR**；真正的红线只有一条——**核心代码绝不复制进闭源模块**（模块只能通过协议调用核心）。

### 第三方壳与模块生态（同一批登记）

- **第三方壳可以存在、可以闭源、甚至可以卖**——这不是漏洞，是"开源核心当平台"的代价：AGPL 只禁止
  「fork 核心闭源卖」，不禁止「写独立壳通过公开协议调用核心」。取舍本来就摆在两边：走协议 → 可闭源可收费，
  但拿不到进程内性能；**链接核心图性能 → 整个壳必须以 AGPL 开源**。这是许可本身的结果，不是我们额外设的卡。
- **我们的付费模块拿不走**：闭源、不含核心代码、只通过协议调用核心；别人想要同样能力，只能自己实现一遍。
- **模块支持跨壳**：通行证**绑机器不绑壳**——第三方壳的用户也能买、能用我们的模块，所以壳可以是**渠道**
  而不只是分流；第三方壳**可以谈代销**（分成或导流），一单一议。
- **不做**：不用技术手段识别或限制第三方壳（那既违反 AGPL，也会重演"军备竞赛"），也不指望协议保密——
  护城河在实现与体验，不在协议。
- **品牌与命名**：官方壳才是「研墨」；fork 核心或做独立壳**必须换名**，不得用「研墨」当产品名误导用户。

---

## 0.15.2 — 2026-09-12

### 登记：增值模块的授权方式（面向未来，不影响本体）

- 定案：增值模块**独立进程、单模块定价**；授权用**离线唯一码**——本机码 = 稳定硬件信息 +
  安装时生成一次的软件自身码，**只做哈希**，而且**只在你手动复制时离开你的机器**（不采集、不上传）；
  收到款项后按这个码签发一份**离线签名通行证**，模块本地验签，**不需要联网激活**。
- 边界（写死）：**开源核心不含任何付费开关、也不感知授权**；签发工具属内部工具，
  **不进安装包、不与核心链接**——核心是「可执行程序 + 公开协议」这一条不因商业而动摇。
- 换机：同一用户支持**免费重发（限量）**，不是「硬件变了就得重买」。
- 采集口径带版本号：将来改进硬件信息采集方式时，**老用户的码不能失效**。
- 现在只登记不实现，等本体基础落地（公开门槛）之后再动。
- **反破解立场**（写下来，免得以后有人"顺手加个 DRM"）：离线单机授权本来就可被破解，**不打算在反破解上投入**——
  不做混淆 / 加壳、不做联网校验与静默遥测、不做机器锁死、不做未授权时的惩罚性降级，
  更不为了防破解去牺牲正版用户的体验；力气花在**让买与重发更省事**上。
  付费模块被破解传播属于侵权，保留"平台举报"这种**低成本**动作，但不搞技术对抗。
- 校验时机：只在模块**头一次加载**时完整验一次，之后靠本机凭据做轻量判断（模块更新 / 换通行证 / 换机 / 凭据坏了才重验）；**不搞「启动时全量握手」**——那不只是性能问题，更是边界问题：**核心完全不参与授权**，模块按需启动、各自管自己的授权。

---

## 0.15.1 — 2026-09-12

### 会话装配收口（内部整理，行为不变）

- 编辑器会话里**有先后要求的创建收进一处**：以前依赖顺序散在几百行里（编译器看不见、单测也碰不到），
  把新模块插错位置会在挂载时直接失败（真机上表现为白屏）。现在顺序写在一段代码里、一眼可见，
  并把约定写在旁边：谁必须先建、谁最后建。
- 顺带把"文件行数巡检"的判据写进工具说明与**外置工具源**：**行数只是体温计**——
  组合根（装配 + 生命周期）与纯登记表（命令 / 路由清单）允许偏长，拆它们反而把依赖顺序与清单打散、
  更难维护；拆分理由要能落到"变化理由 / 连锁改动 / 重复副本"，行数本身不算理由。

### 测试

- Rust 136 个、前端 86 个全绿；真机复核：界面正常渲染、设置面板可开、开关状态与关闭前一致。

---

## 0.15.0 — 2026-09-12

### 打开章节时的光标规矩：历史章只看不写，该写的章接着写

- **打开历史章节不再把光标放进正文**：回看旧章是"看"，不是"写"。光标要是落在正文里，
  随手敲一下就在稿子里插进了乱字符——这类改动最难发现。现在打开旧章时光标一律不在正文里，
  点一下正文即可照常编辑。
- **两种"接着写"的情形会跳到段末并聚焦**：刚新建（或"补写"出来的）空章、第一次打开全书最新
  那一章。**已经有"上次读到哪"的记录时仍回原位**——不会因为"这是最新章"就把你拽到段尾。
- 顶栏新增**「设置」**（第一版只有这一项，外观各项会陆续加进来）：可以关掉"打开最新一章时跳到
  段末并聚焦"。偏好只有一份、跟人不跟书，关掉后重启仍然生效；面板里还有「恢复默认」。
- 这些偏好只影响显示与手感：**不进导出，也不改动正文一个字**。

### 测试

- Rust **136 个**全绿（新增 4 例偏好：没设过用默认 / 只存改过的项 / 每本书的覆盖只覆盖它真设过的
  项、其余继承全局 / 记录坏了当没设过而不是报错）；
- 前端 **86 个**全绿（新增 5 例焦点策略：历史章一律不聚焦、首次打开最新章聚焦、开关关掉不聚焦、
  读过的最新章回原位、刚新建的章接着写）。

---

## 0.14.2 — 2026-09-12

### 目录树视图瘦身：点「+」之后的流程搬出视图

- 「点 + 之后先问一嘴、再决定建不建新章」这段流程原先写在目录树面板里（视图既画树、又管建章），
  现在搬到会话层的独立模块：**视图只回答"作者点了哪一行"**，流程（问 → 补写 / 接着建章）
  连同它的依赖一起注入，因此可以脱离界面单测。以后往建章流程里加动作，不必再动视图。
- 三个弹窗（书架 / 回收站 / 删章路标）的**公共样式收成一份**（遮罩、盒子、标题、按钮、交代条、
  空态），组件只留自己那部分——同一套规则此前抄了三遍。
- 开发期加了一个**代码健康巡检**（文件行数 / 方法数：超限只报警不失败，提示按「三问」复核）：
  它放在内部工具目录，不进安装包，也不影响运行时。
- 顺手修掉一处开发期的接线顺序问题（组合式模块比它依赖的对象先初始化，真机上是白屏）——
  这类问题构建与单测都看不出来，靠真机渲染检查兜住。

### 测试

- Rust **132 个**全绿；前端 **81 个**全绿（新增 8 例点「+」之后的编排：没得问就直接建 /
  有得问先摆弹窗 / 补写后刷新并切过去 / 稍后再说后接着建 / 答不上来不往下走 / 去回收站看看不记答复）。

---

## 0.14.1 — 2026-09-12

### 删章留下路标（续）：点「+」时会先问一嘴，界面这一半接上了

- 目录里点某一行的「+」时，先看看**这一层是不是少了一章**：回收站里还躺着被删的那一章、
  能认出是第几号、而那个号现在没人用——就先问一句「这里缺了第2章，昨天删的，旧稿 1234 字
  还在回收站里」，然后给三条路：
  - **补写这一章**：在原来的位置新建一个**空章**（用原来的名字），建完直接打开来写。
    这是**新建一章，不是恢复旧稿**——回收站里那份原文一个字都不动，想去捞随时能捞；
  - **稍后再说**：这次先不补，下次在同一层点「+」时再问一次；**再问一次之后自动降为「不用了」**，
    免得变成"每次点「+」都弹一下"的纠缠；
  - **不用了，别再问**：不再主动问（交付前的汇总检查仍会把这一处列出来）。
- 弹窗上还有一条「去回收站看看」：只是想先看一眼旧稿的话，**不记答复**，下次点「+」还会问——
  没拿主意就当成"没拿主意"，不替他做主。
- 卷里点「+」（往里加一章）与章上点「+」（往后插一章）问的是**同一层**的同一个空缺，
  各卷各问各的，不会问错地方。
- 空缺与答复的算法全在核心：**只记"问过没问过"，空缺本身从数据算**——
  不另存一份会跟节点表打架的真相。

### 测试

- Rust **132 个**全绿（新增 9 例删章路标验收：有据才问 / 号被占的不算空缺 / 三态答复 /
  补写落在原位且不动回收站里的旧稿 / 改名造成的空档仍进汇总清单 / 各卷各问各的）；
- 前端 **73 个**全绿（新增 8 例：问一嘴的时机、补写与答复的走向、答不上来不往下走、弹窗文案）。

---

## 0.14.0 — 2026-09-12

### 新章的默认名回到最简单的顺序号

- 默认名重新只按「这一层已用过的**最大号 + 1**」取（中文数字也认，「第一章」「第十章」都算数）。
  删掉第2章之后新建拿到的是「第4章」——**空档不再自动补**。
- 上一版那套"按第几位猜缺号"的做法**删掉了**：删了中间那一章、想补回「第2章」时，
  与其让程序猜，不如**问一句**（见下）——规则越少越可预测，也就越不容易出岔子。

### 删章会留下路标（内核就绪，界面接线进行中）

- 内核新增"空缺路标"：**被删掉的章会留下一条可查的路标**——它属于哪一卷、第几号、
  几点几分删的、旧稿多少字，都能问出来；号已经被别的章占着的（作者自己改名补上了）就不算空缺。
- 答复有个**三态机**（只在核心一处实现）：没问过 → 问一次 → 选「稍后」→ 下次再问一次 →
  又选「稍后」→ 自动降为「不用了」。**系统可以问两次，但不该第三次还问**。
- 答复只记"问过没问过"（存在设置里，按卷+号归档），**空缺本身从数据算**——不另存一份会打架的真相。
- 另外还提供按编号算出的**全书空缺清单**（含没有删除记录的：改名造成的、旧稿已被彻底删除的），
  给交付前的汇总检查用。
- ⚠️ **这一节目前只有内核**：界面上的"点「+」时问一句"与"交付前检查"还没接上，下一步做。

### 测试

- Rust **123 个**全绿（取号规则表驱动重写为 10 例；新增"只看这一层、只看这一类型"的取号用例，
  以及空缺路标的三态答复、按编号算全书空缺、补写会新建空章而不动回收站里的旧稿等）；
  前端 **65 个**全绿（本次未动前端）。

---

## 0.13.1 — 2026-09-12

### 新章的默认名：跟着落点走

- 规则收成一句话：**紧挨着的两个号中间正好缺一个，就补那个缺号；其余一律取最大号 + 1**。
  - 删掉第2章之后，在第1章后面点「+」→ 新章**直接就叫「第2章」**（不用再手动改名）
  - 在这本书末尾接着写 → 「最大号 + 1」
  - 层里的编号不从 1 开始（比如 5、6）→ 一律往后取，**绝不凭空给一个跟这一段编号无关的号**
  - 全是自起的名字（序章 / 引子）→ 退回"现有几章 + 1"
- 取号时**中文数字也认**（作者手打的「第一章」「第十章」都算数）。

### 说一句测试

这套判断有分支，所以除了逐条例子，还加了一条**不变量测试**：不论层里是什么标题、插在哪，
**取出来的默认名都必须与活着的同级同类节点都不同名**——用一批层内容 × 每个落点穷举着压。
写的时候它当场抓出两个错：一个中文数字的分支被前面的分支吃掉（「第十章」认不出来），
以及"按第几位取号"在编号不从 1 开始的层里会算出「第2章」这种无关的号——后者促成了上面这条更紧的规则。

### 测试

- Rust **123 个**全绿（新增：取号规则表驱动 14 例、不变量穷举、混排同级（单篇与章同层）不数偏位次）；
  前端 **65 个**全绿（本次未动前端）。
- **真机端到端**：删第2章 → 在第1章后面点「+」→ 直接得到「第2章」且落在第二位；末尾点「+」→ 第4章；
  编号从 2 开始的层里插章 → 第5章（不硬套位次）；同级序号仍密集。

---

## 0.13.0 — 2026-09-12

### 删掉中间那一章之后，章节号和名字的账对上了

- **名字绝不自动补位**：删掉中间的「第2章」，后面的「第3章」仍然叫第3章——目录是作者的结构，
  系统不该替他重排（大纲、投稿要求、跟人讨论时说的"第19章"都指着这个名字）。
- **但新建不再撞号**：默认名从"当前有几章 + 1"改成"**这一层已用过的最大号 + 1**"。
  删掉第2章之后再新建，拿到的是「第4章」，而不是跟还在的「第3章」撞名。
- **同级序号不再留洞**：删掉一章之后当场把这一层收成 0、1、2…；恢复时按"它被删那天在第几位"
  锚回原来那一带——所以恢复出来的章回到原位附近，也不会和谁同号。
- 顺手把新建长篇时第一章的默认名统一了（原先叫「第一章」，新建的是「第2章」，两种数字混着）。

### 恢复遇到同名：把决定权交给作者

删掉「第19章」、又重写了「第19章」，然后从回收站点恢复——"误触"和"故意"在数据上长得一模一样，
而猜错的代价不对等（猜错可能盖掉他重写的东西）。所以：

- 恢复前先**看一眼**：会不会与还活着的一章重名、原来在第几位；
- **有冲突才问**（没冲突直接恢复，不给正常路径加步骤），三个选项：
  **照原样恢复**（两章同名，我自己收拾）/ **恢复并改名**（新名字**由他填**）/ **取消**；
- 无论选哪个：**绝不覆盖**他已经写的新章、**绝不合并**两条线——旧稿带着它自己的正文与快照回来，
  新章一个字不动。想把旧稿某段搬进新章，那是版本差异对比要做的事，不是回收站的职责。

### 测试

- Rust **119 个**全绿（新增：默认名跳过已用过的号、**删中间章→新建→改名补号之后的取号行为**、
  **不改名时那个号会被跳过**、删中间章后同级仍密集且恢复回原位、恢复可以带新名字而不动正文、
  恢复前预检摆出同名的那一章）；
  前端 **65 个**全绿（新增回收站 8 例：无冲突直接恢复、有冲突先不动、
  照原样恢复 / 改名恢复 / 取消各自发出什么动作）。
- **真机端到端**：建三章 → 删中间章（名字不补位、序号收密）→ 新建拿到「第4章」→
  把新章改名成「第2章」造出冲突 → 回收站恢复时弹出冲突框（列出同名章的已写字数、说明不会覆盖、
  也确实没有偷偷恢复）→ 取消无副作用 → 改名恢复后旧稿回到原位、重写的那一章完好。

---

## 0.12.0 — 2026-09-11

### 回收站：删错了能捞回来

- 目录里每一段多了「×」：删掉的一章（或一卷）会**连同里面的内容**进回收站，不再是直接消失。
  删掉的正好是"正在写的那一支"时，编辑器会自动落到这本书里还活着的那一章，
  不会攥着一个已经不存在的节点。
- 书架的「删除」进同一个回收站：书架面板里多了「回收站」入口，里面写着删了什么、什么时候删的，
  以及它连带了多少内容。
- 回收站里每一条都有两条路：**恢复**（回到原来的位置）与**彻底删除**（真的抹掉，先问一句）。
  恢复一段时会连同**它上面还在回收站里的层级**一起捞回来——否则捞出来的东西挂在一个看不见的父级下，
  等于没恢复；恢复一整本书时，书里那些**单独删过的章仍然留在回收站里**（那是另一次删除，
  不该被"恢复整本书"顺手撤销）。
- 「清空回收站」一次清掉全部；要是正在写的那本书也在里面，会自动落到另一本书，
  一本都不剩时自动建一本新的，不让人对着空界面。

### 导出：把稿子拿走，而且不会每次导出都刷出一堆 diff

- 书架每本书多了「导出」：一次导出两种格式，落到「文档 / 研墨导出 / 书名」下。
  - **分章纯文本**：卷用目录表示，文件名带三位序号（顺序就是阅读顺序），一章一个文件；
  - **单个 JSON**：结构与正文都在里面，方便再读回来。
- **幂等**：同样的内容不会重复写、渲染里不写时间戳、换行统一成 LF、顺序按阅读顺序稳定输出——
  所以这个文件夹可以放心交给 git 或同步盘，diff 里只会出现真正的改动。
- **不留孤儿**：改了章名、删了章之后再导出，上一次留下的旧文件会被清掉
  （只清理它自己写出的 txt / json；作者自己放进这个文件夹的东西一概不碰）。
- 书名里的路径分隔符会被安全化，导出**绝不会跑出导出目录**。

### 测试

- Rust **113 个**全绿（新增回收站 5 例、导出渲染 6 例、导出落盘 3 例：写文件与二次导出不重写、
  清残留但不碰别人的文件、书名带路径分隔符也跑不出导出目录）；
  前端 **57 个**全绿（新增删除与"是不是我正在写的那一支"的判断 2 例）。
- **真机端到端**：删章 → 回收站里看得到 → 恢复（正文完好）→ 删整本书 → 恢复 → 彻底删除
  （库里真的没了、节点被级联清掉）→ 清空回收站；导出 txt + json、再导一次文件没被动过、
  改名后旧文件被清掉、json 能读回来。
  这一轮真机又抓出两个单测覆盖不到的问题：删除当前章时"先切走再落盘"会撞上"节点已删除"，
  人卡在原地（改成先落盘再删）；随后给已删除的节点记光标会拦下整个切换（改成先把落盘控制器摘掉）。

### 说明

- 备份的**滚动保留**（自动 N 份轮转、手动备份永不删）与整库备份不在这里做：
  细粒度恢复交给"每章快照 + 变更日志"，整库备份按低频轮转，落在备份包那件事里。

---

## 0.11.0 — 2026-09-11

### 书架：多作品成了默认形态

- 顶栏多了「书架」：**列书、开书、建书、改名、删书**。每本书显示形态（长篇 / 短篇集 / 单篇）、
  章数与字数、最近打开时间；正在写的那本标着「正在写」。
- **每本书各记各的"读到哪了"**：上次写的是哪一章、光标与滚动位置都存进库里、按书分开记。
  今天写这本、明天写那本，切回来就回到原位——两本书之间不会互相覆盖。
- **换书与换章走同一套纪律**：先落盘、记光标，再换内容；存不下去就不换。
- **删书是软删除**：删掉的书不在书架上，但正文与历史都留着（回收站接上后能捞回来）。
  删掉正在写的那本，会自动落到书架上剩下的那一本；一本都不剩时会自动建一本新的，不让人对着空界面。
- 新建书时可选形态：长篇（卷 → 章）、短篇集（一篇一篇）、单篇（零层级）；建好直接进去写。
- 顺带把"字数与时间怎么念"的展示口径收成一处（一万以上报 `x.x万`，最近打开报"今天 / 昨天 / N 天前"）。

### 测试

- Rust **99 个**全绿（新增书架 5 例：书架的规模与排序、每本书各记各的光标、切书回到原位、
  没有记录时回退到第一章、老库的单槽光标只兜一次并搬进新位置）；
  前端 **55 个**全绿（新增展示口径与书架规则 5 例）。
- **真机端到端**：建一本长篇新书 → 自动落进它的第一章 → 写正文 → 切回旧书，正文与光标都在
  （没有串稿）→ 改名 → 删掉正在写的那本并自动落到剩下的那本；读库对账：删除是软删、
  名字已落库、每本书各有一条光标记录、旧的全局单槽记录已经退场。

### 待办

- 封面与分组还没做：封面要碰图片文件，得先定它与"界面不碰文件系统"这条边界怎么划。

---

## 0.10.1 — 2026-09-11

### 记录

- 明确**公开时机**：先不公开，等基础落地。理由是"能给别人用"至少要有三样东西，现在都还没有：
  ① 一条命令构建出的**可运行产物**（安装包 / 绿色版）并真正装一遍跑通；② **README 能让陌生人自己跑起来**
  （下载哪个文件、装完会看到什么、构建前置有哪些、踩坑怎么绕）；③ 能**把稿子安全带走**的核心能力
  （导出与多作品管理）。这三条齐了再谈公开——那时可以直接公开，也可以先打"提前预览"的标签。
- 同时登记公开前要补的几件事：一键构建脚本、首次端到端打包与装机冒烟、使用者向 README、
  发布链路与校验和、代码签名（或未签名的兜底说明）。

---

## 0.10.0 — 2026-09-11

### 目录树：本卷进度与字数

- **卷这一行现在有自己的数字**：`12/30章 · 3.4万字`——几章、共多少字，一眼看得出这一卷写到什么程度。
  字数是把子树里各节点的字数**加总**（写入正文时早已算好，不为了显示再扫一遍正文）；
  章数是子树里章的个数（场景卡这类卡片算字数、不算章）。
- **每卷目标章数**：目录上方多了一栏「每卷 __ 章」，填一次就长期有效（**按作品分别记**，
  互不干扰），填了之后那一行显示成 `12/30章`。它**只影响这行小字的显示，绝不改结构**，
  随时清空就回到只报章数。
- 目录列从 220px 加宽到 240px，给进度小字留出位置；零层级作品（没有卷）不显示这一栏。
- 卷里增删章节、拖动搬章、落盘写字之后，各层容器的这行小字都会跟着更新
  （落盘走差额、不重读目录；结构变动才重读看得见的层）。

### 测试

- Rust **94 个**全绿（新增 2 例：子树汇总必须与"逐节点相加"一致且软删后跟着少；
  每卷章数按作品分开存、≤0 与清空都当"没设过"、已删除的作品不给设）；
  前端 **51 个**全绿（新增 4 例：容器行小字三种形态、万字换算、汇总来自核心、落盘后各层容器字数跟着挪）。
- **真机端到端**：建一卷两章 → 写正文 → 卷行字数与各章之和一致；设「每卷 30 章」后显示成
  `2/30章` 且落库；清空后回到 `2章` 且库里不留残行；页面上没有报错提示。

### 说明

- 这是"按作者意图提议分卷"的前置：有了本卷章数与目标章数，程序才知道什么时候该提醒
  "这一卷差不多该收了"。提醒与一键成卷本身留待后续。

---

## 0.9.1 — 2026-09-11

### 目录树的两处收尾

- **打开就定位到正在写的那一章**：章在收起的卷里时，目录会顺着它所在的那条路一层层展开，
  并在那一行上标出当前位置；切到别的卷里的章时同样会展开过去。已经展开过的层不会重复读，
  所以频繁切章不会变成频繁读目录。
- **新建章节的默认名改按同层取号**：以前按全书阅读顺序取号，分卷之后会接着整本书往下数
  （第一卷两章、第二卷里再新建就成了「第4章」）；现在各层各数，第二卷里新建就是「第1章」。
  卷名同理（第1卷 / 第2卷）。命名规则只在核心留一份实现，两条新建入口（章后插章、卷内建章）
  走的是同一条，标题由作者填时一切照旧。

### 测试

- Rust **92 个**全绿（新增 2 例：默认名按所在卷取号、祖先链从根往下列且根级节点为空）；
  前端 **47 个**全绿（新增 4 例：定位时逐层展开、重复定位不重读、根级不用展开、作品未打开时不动手）。
- **真机端到端**：搭出两卷、收起其中一卷再切进去，确认那一卷自动展开且当前章被标出；
  读库对账：两卷里各有一个「第1章」、卷名是「第1卷 / 第2卷」；页面上没有报错提示。

---

## 0.9.0 — 2026-09-11

### 卷 / 章目录树

- 左栏换成了**真正的目录树**（取代原来的"上一章 / 下一章"按钮条），卷里套章、章里还能放场景卡，
  层级不写死：节点的类型决定它能不能写正文、能不能收下级。
- **一层一层读**：展开哪一卷才读哪一卷的章。几百章的长篇不会在启动时被一次读进来；
  目录条目只带标题与字数，**取不到正文**——正文只在真正切过去时才读。
- **每章字数就写在行上**（写入正文时已经算好，不为了显示字数去扫一遍正文）；还没写过的章显示「空」。
- **拖动排序**：拖到某一行的**上 / 下**两段＝排到它前面 / 后面，拖到**中间**＝放进它里面。
  落定后同级序号重新收成 0、1、2……（幂等、可复现）；把节点拖进自己子树的动作会被直接拒绝。
- **双击改名**：就地改，回车生效、Esc 放弃；空标题视为放弃，不会把章名改没。
- **新建**：标题栏「+ 卷」新建一卷；**章上的「+」在它后面接着新建一章并直接切过去写**；
  卷上的「+」往这一卷里新建一章。
- 底部保留全书位置与上一章 / 下一章，换章仍按阅读顺序、**跨卷**。
- 目录为空时给一句说明，不摆一个空白栏。

### 测试

- Rust **90 个**全绿；前端 **43 个**全绿（其中目录树状态机 16 例：懒加载只拉该拉的、展开 / 收起、
  改名就地生效、同层拖动的索引换算、搬空后收起展开箭头、拒绝成环、换作品清空旧树……）。
- **真机端到端**：用内嵌前端的构建实跑，覆盖新建卷、卷内建章、章后建章、双击改名、跨卷拖动、
  上一章 / 下一章，并直接读数据库对账（同级序号确认是密集的 0..n-1，页面上没有报错提示）。
  这一轮又抓出一个单测覆盖不到的真问题：往卷里建完章之后，**卷这一行不会立刻长出展开箭头**
  （拉取子层时父行还留着旧副本），已修并补了断言。

### 待办

- 打开时不会自动展开到"正在写的那一章"所在的卷：章在收起的卷里时看不到高亮，下一步补定位。
- 新建章节的默认名按全书阅读顺序取号，分卷之后会跳号，待改为按同层序号取号。

---

## 0.8.4 — 2026-09-11

### 记录

- 决定把**智能辅助相关计划整体延后**（正文摘要与事件抽取、本地模型的获取与启用、面向本软件场景的小模型特训），
  **先把软件本体做完**。相关设计结论已完整留档，将来启动时不需要重新讨论。

---

## 0.8.3 — 2026-09-11

### 记录

- 明确本地模型的**获取路径**：模型文件由项目方托管（国内可达的源为主、代码托管平台的发布页为备源，
  两份校验和一致），软件内一键下载，支持分片与断点续传；直连不通时由软件**自动探测本机代理**再试；
  再怎么不通，也只需"下载一个文件、放进指定目录"（并提供文件选择入口），绝不要求用户去模型站自己找。
- 明确**联网边界**：只有"用户主动下载模型"这一个动作会出网，其余能力保持零出网，并会写进安全说明。
- 登记一项后续计划：**面向本软件场景的小模型特训与评测流水线**——先用通用小模型验证够不够、
  把跑不通的样本攒成数据集，再决定是否特训（先有评测判据，再有训练）。

---

## 0.8.2 — 2026-09-11

### 记录

- 明确智能辅助的接入口径：**所有需要模型的能力共用一个出口**（统一配置、统一模型生命周期、
  统一超时与降级），不允许各功能各自接模型；并且**优先自动发现机器上已有的本地推理服务**，
  应用内下载只作兜底；模型档位由软件按硬件自动选，不把参数丢给作者。

---

## 0.8.1 — 2026-09-11

### 记录

- 定案一项功能形态：**自动分卷提示**——按作者设定的卷长（章数为主、字数为辅）在临近阈值时
  **只提议候选收卷点**，由作者一键成卷、可撤销；软件绝不自动改动作品结构，更不动正文一个字。
  第一版只依赖本地统计（章数 / 字数 / 章末时间跳跃 / 场景切换 / 作者自己历来的收卷习惯）。
- 登记两项前置计划：**章节摘要与事件抽取层**（给"理解正文"的能力打地基，供大纲对照、伏笔回收、
  分卷判断共用）、**本地智能模型的友好启用**（自动发现已有环境 / 应用内一键获取 / 没有模型时优雅降级为规则模式）。

---

## 0.8.0 — 2026-09-11

### 编辑器只挂当前章

- **一个编辑器实例从头用到尾**：切章只是换内容 + 换落盘控制器，**整本书永远不会同时挂进编辑器**。
  实测切一章约 **12 毫秒**（上一章/下一章来回切 8 次：P50 11.7ms / P95 12.5ms）。
- **切章先落盘**：当前章没存干净就**不切**——停在原地并说明原因，切换过程不会变成丢字的窗口。
- **上一章 / 下一章按阅读顺序**：不是简单的同级前后，**会跨卷**（第一卷最后一章的下一章在第二卷）；
  已删除的章不参与导航；卷这类容器节点永远不会成为导航目标。
- **记住读到哪了**：光标位置与滚动位置跟章节一起存进库里，重开或切回来都回到原位
  （文档变短时位置会夹进合法范围，不会跳飞；已删除章节的旧位置不会被套用）。
- **「+ 新章」**：在当前章后面插一章（同级、紧随其后、序号保持密集）——
  这是目录树功能落地前的临时入口，目录树的「+」将来走同一条路。
- **界面与会话编排分开**：落盘、切章、关窗闸门、光标记忆从编辑器组件里抽成了独立模块，
  改排版不会碰到落盘逻辑，反之亦然。

### 测试

- Rust **89 个**全绿（新增章节导航与光标记忆 7 例：跨卷下一章、删章后重排、场景卡参与阅读顺序、
  光标只认自己那一章、新章插在当前章之后且不跨卷……）；
  前端 **28 个**全绿（新增切章 7 例：落盘成功才切、落盘失败不切、目标章没了停在原地、
  连点忽略、新建路径复用同一套流程……）。
- **真机端到端**：通过界面新建 3 章并在两章之间来回切 8 次，全部 12 毫秒级；
  这轮实跑**顺带抓出一个单测覆盖不到的真问题**（新建按钮的事件没有接到会话编排上，
  点了没反应——模板绑定这一层只能靠真跑或专门的界面测试来兜）。

---

## 0.7.5 — 2026-09-11

### 记录

- 补测两项长时间/重复操作下的内存表现，结论是**没有泄漏**：
  - **连续写作 5 分钟**：堆在 14–47MB 之间锯齿波动（回收正常），**每千字内存从 1084KB 收敛到 340KB**；
    全过程保存状态始终是「已保存」——落盘回路在持续输入下没有被拖垮。
  - **写一段再撤销、反复 200 轮**（文档大小始终不变）：堆爬到 8–9MB 后**平台化**
    （前半段每轮 +0.039MB，后半段 ±0.001MB）——不是线性增长，属正常预热与回收滞后。

---

## 0.7.4 — 2026-09-11

### 记录

- **完成一轮前端承重实测**（25.5 万字 / 约 2500 段的文档）：冷启动到可输入 **519ms**；
  击键到下一帧 P50 **10.8ms** / P95 **17.5ms** / 最长 23.6ms；长文档滚动帧间隔 P95 **6.4ms**、
  最长 **7.5ms**、**卡顿帧 0**；持续写作 15 秒 JS 堆增长 **9.1MB**、DOM 未膨胀。
- **压测能力本身不随本仓分发**：它是独立程序，不链接本仓代码、不进安装包；
  后续体量变化时可照同一套流程复测并与基线对比——这是「现在能受住 ≠ 以后还承重」的答案来源。

---

## 0.7.3 — 2026-09-11

### 记录

- 登记一项后续计划（本次不实现）：**前端承重压测工具**——独立程序、不链接核心、一条命令跑完并给出可回归对比的数字，
  用它在体量变大之后回答"前端还顶不顶得住"。

---

## 0.7.2 — 2026-09-11

### 修复与优化

- **修复"界面无响应时窗口关不掉"**：关闭窗口会先拦下来交给界面落盘，但界面若已经死了
  （例如 WebView 进程被系统回收），就永远没人回应——窗口会变成关不掉的死窗，只能去任务管理器结束进程。
  现在加了**应答期限**：界面在 3 秒内没有回话就判定它已死，由壳自己收场退出；
  界面**回了话**（正在落盘或正弹着"存不下去"的对话框）则不再计时——**拦住是界面故意的**，壳不替它做主。
  另外，界面没回话时用户再点一次关闭＝"我就是要关"，立刻退出；反复点不会把期限往后推。
- 上述判定收敛成一个可单测的状态机（四条归宿：未请求 / 等待回话 / 界面处理中 / 正在退出）。

### 测试

- Rust **82 个**全绿（含边界与稳定性的 5 例"故意弄错"、2 例规模；另 1 例病态规模按需运行），前端 **21 个**全绿。

---

## 0.7.1 — 2026-09-11

### 记录

- 登记两项后续计划（本次不实现）：**界面假死检测与自动复活**（界面心跳 + 看门狗 + 自动重载）、
  **命令耗时与界面卡顿度量**（诊断用）。

---

## 0.7.0 — 2026-09-11

### 关窗与崩溃恢复

- **关窗不放行**：关闭窗口不再直接退出——先让编辑器把当前这一章落盘，**存不下去就拦住**，
  弹出阻塞对话框，给三个出口：重试保存 / **导出到文件** / 仍然退出。
  界面没就绪时一律放行，免得程序出问题时窗口反而关不掉。
- **关窗快照**：正常退出前给当前这一章留一份版本快照；内容没变就不重复留，
  不会把「关窗」变成一个垃圾制造动作。
- **崩溃检测**：启动时登记本次会话，只有**正常退出**才标记为已正常退出。
  下次启动若发现标记仍停在「运行中」，就判定上次是被杀或崩溃，并在界面上说明——
  依据只有一个字段，不猜、不依赖文件时间戳。
- **回到原位**：会话里记着最后编辑的那一章（**打开编辑器时就记**，一个字没写就被杀同样有效），
  重开后直接回到那一章及其**最后落盘的版本**。
- **诚实边界**：进程被杀的那一刻，**内存里还没落盘的内容无法恢复**。能保证的是两件事：
  ① 已经落盘的一个字都不少（每次写入一个事务 + WAL）；② 关窗 / 失焦 / 切到后台前一定先落盘。
  可能丢的只有停笔防抖的那 150–300 毫秒（默认 250 毫秒，约几个字）。
- **逃生导出是原子写**：同目录临时文件 + 落盘 + rename，**绝不会留下半截文件**；
  文件名自动清掉不合法字符并限长；路径由核心给出，界面只负责显示。

### 测试

- Rust **68 个**全绿（核心 36 + 存储层 8 + 会话与崩溃恢复 7 + 边写边存 5 + 桌面壳 5 + 边界守卫 5 + 协议 2）；
  前端 **21 个**全绿（保存逻辑 8 + 关窗闸门 8 + 正文序列化 5）。
- 真机链路已实跑：应用启动即登记会话、心跳随读回校验推进；**强杀进程后标记仍停在未正常退出**，
  下次启动据此报告崩溃并回到原章节。

---

## 0.6.1 — 2026-09-11

### 修复与优化

- **修复开发模式无法启动**：开发与构建的前置命令是**在前端目录内**执行的，而配置里原先多带了一层目录前缀，
  于是包管理器在前端目录里又去找一层同名目录，报「找不到 package.json」。
  去掉多余前缀后，`cargo tauri dev` 能直接拉起开发服务器与应用（已实跑验证：开发服务器就绪 → 编译通过 → 应用进程启动）。

---

## 0.6.0 — 2026-09-11

### 边写边存

- **停笔才落盘**：连续打字不会产生任何写入，停笔约 250 毫秒才写一次（可在 150–300 毫秒间调整）。
  击键、撤销、光标全部留在编辑器内部，**不逐键跨越进程边界**。
- **写入串行化**：同一章同一时刻只有一个写入在飞；写回期间作者又改了字，改动会排队再存一次，
  不会被这次写吞掉。内容没变时核心直接返回，不写库也不记日志。
- **写后读回校验**（本轮最硬的一条）：每 2–5 秒核对一次"库里的正文还是不是我手上这份"。
  核对只比**内容指纹**，不把整章正文再搬一遍。
- **两道保险**：
  1. 长时间存不下去（超过 1.5 秒仍未落盘）判定为卡住 → 强制落盘；
  2. 发现库里的内容与手上这份不一致 → **先把手上这份留成快照，再把库改回这一版**，
     并在界面上保留"本次会话发生过抢救"的提示，让人能回头核对。
- **离开前落盘**：失焦、切到后台、组件卸载时立刻落盘，不等防抖计时。
- **界面反馈**：章名、字数、保存状态（待落盘 / 保存中 / 已保存 / 异常 / 已抢救）一目了然。
- **正文序列化只有一个出口**：库里存纯文本、段落之间以空行分隔——
  这样全文检索不被标记污染、字数口径干净，将来写磁盘镜像也直通。
- **首次运行引导**：打开就能写（没有作品时自动建一篇默认作品）；书架与章节切换随后接管这一职责。

### 工程与防回归

- 命令层按域拆分，并新增一条**机械检查**：声明的命令必须都注册、都必须有界面出口——
  漏注册等于界面调不到，注册了不实现等于启动就崩，两头都拦在测试里。
- 命令参数与返回值统一 **snake_case**，不给"两套写法"留出错空间。
- **前端测试首次落地**：使用运行时自带的测试运行器，**零新增依赖**，
  `npm test` 就能把防抖、排队、卡住检测、读回校验与抢救逻辑跑一遍（用假时钟，不用真的等）。

### 测试

- Rust **55 个**全绿（核心 32 + 存储层 8 + 边写边存 5 + 桌面壳 3 + 边界守卫 5 + 协议 2）；
  前端 **13 个**全绿（保存逻辑 8 + 正文序列化 5）。

---

## 0.5.0 — 2026-09-11

### 存储层

- **唯一读写入口**：作品、结构节点、正文、检索全部经由一个存储句柄；界面与外部模块都不直接写库。
  **每次改动都会留下变更记录**（带本机设备标识与单调序号），既是同步的底料，也是"改了什么"的凭据。
- **新建作品是一个事务**：作品与其起始节点一起建成，失败不留半个作品。
  长篇从一卷起步；**文章与短篇集的起始节点就是正文本身（零层级）**。
- **结构是可变深度的节点树**：卷 / 章 / 节 / 单篇 / 场景卡只是节点类型，代码不假设层级。
- **排序稳定可复现**：同级使用密集序号，移动后重排——无空洞、重复执行结果一致。
- **移动有护栏**：不能把节点移进自己的子孙（成环），也不能跨作品挂载，两类都会明确报错。
- **软删除**：删除连带整棵子树，但正文与历史都保留，回收站与"删错了"的撤销都靠它。
- **正文按需加载**：目录树接口**在类型上就拿不到正文**——懒加载不是靠自觉。
- **内容没变就不写**：以内容指纹判定，未变化时不写库也不记日志，为高频自动保存让路。
- **字数自动回算**：写入时算好字符数与字数并存到结构节点上，目录树不必扫正文就能显示每章字数。

### 全文检索

- **中文子串检索**：采用 trigram 分词，「任意三个字以上」的子串都能命中，不依赖分词质量；
  一到两个字的查询退回扫描路径——**慢一点，但结果是对的**（中文里两个字太常见，搜不到才是 bug）。
- **索引由数据库触发器同步**：标题与正文的写入、更新、删除都自动反映到索引，不靠应用代码记得更新。
- **命中高亮**：返回命中周围的片段，并标记命中是否落在标题上。
- 提供**重建索引**入口，用于导入或怀疑索引不一致时的兜底。

### 结构升级

- 新增一次结构迁移引入检索索引；**旧库打开时自动升级，并把存量正文回填进索引**——
  升级后老稿子立刻就能搜到（有自动化测试盯着这条）。

### 修复与优化

- 迁移幂等性测试改为「每个迁移只成功执行一次」，不再隐含"只有一个迁移"的假设。

### 测试

- 49 个全绿：核心 32 + 存储层 8 + 桌面壳 3 + 边界守卫 4 + 协议 2。
- 存储层验收覆盖：零层级单篇文章与四层长篇**都能建能写能搜**、逐层懒加载、
  密集排序、成环与跨作品拒绝、内容未变的零副作用、删除后从树与检索同时消失而正文仍在、
  标题改动即时可搜、旧库升级回填。

---

## 0.4.0 — 2026-09-11

### 单一真相源

- **数据权威收归桌面壳（Rust 侧）**：应用启动时由壳解析数据目录、打开并迁移数据库；失败即启动失败，
  **不带病运行**。目录来自系统 API + 固定文件名，不拼接任何外部输入。
- **界面没有文件系统入口**：界面侧不引入文件读写、取路径、文件对话框等任何相关模块，
  命令也**不接受路径参数**——界面能拿到的只有"被报告出来的结果"，改不了数据落在哪。
- **界面只经一条通道与核心对话**：所有命令集中登记在一处（`src/api/`），组件不再直接调用核心；
  这样命令粒度有人管，不会长成一锅粥的万能调用。
- **权限集逐项最小化**：桌面壳权限由"整套默认"改为逐项列举，并**去掉了路径解析能力**——
  界面拿不到真实路径，也就不存在"界面自己去找文件"这条路。
- **数据位置可见**：界面状态栏显示稿件所在位置（用户有权知道自己的稿子在哪，这只是只读报告）。

### 工程与防回归

- **边界改为机械检查**（4 条断言，随测试运行）：核心零界面依赖 / 界面不出现文件读写与取路径接口 /
  界面只经单一通道对话 / 权限集不含路径能力。断言里带"防假绿"自检——通道被删、目录扫空、
  权限集写空这类会让检查悄悄失效的情况，同样会红。
- 已用**植入违规**验证守卫确实会拦（临时加入一条文件读写引用，检查立即失败；移除后恢复通过）。
- 补登第三方声明：数据库驱动与测试用临时目录库（此前遗漏）。
- 清除第三方声明里一处指向内部项目的引用（公开读者顺着它找不到任何东西），并把一致性检查的
  词表补全，避免同类引用再次漏网。

### 测试

- 31 个全绿：核心 22 + 协议 2 + 数据句柄 3 + 边界守卫 4。

---

## 0.3.1 — 2026-09-11

### 修复与优化

- **统一公开内容口径**：README、CONTRIBUTING、第三方声明与全部源码注释改为**自包含**表述，
  清除了对本地绝对路径、内部项目名与私有文档的引用。
  这类引用在公开仓库里不只是信息泄露，还是**断链**——读者顺着点进去什么都没有。
- **构建生成目录**（Tauri 生成的 `app/gen/`）不再纳入版本控制。
- 增加一条提交前自检流程：公开内容改完后必须跑一次一致性检查，**0 命中才允许提交**。

---

## 0.3.0 — 2026-09-11

### 数据层

- **SQLite 数据层落地**：WAL 模式 + 外键约束 + 忙等待；驱动库使用 `bundled` 特性（自带 SQLite 源码编译），
  保证全文检索能力可用、版本可控，避免依赖系统库导致的静默降级。
- **数据迁移框架**：迁移列表**只增不改**；多语句在**显式事务内逐条执行**；中途失败**整体回滚**并记录失败原因，
  不留"半迁移"状态；**数据结构版本高于本引擎时明确拒绝打开**，而不是猜着读。
- **初始数据结构**：作品 / 结构节点 / 正文 / 版本快照 / 变更日志 / 碎片 / 设备 / 设置。
  - **结构节点支持任意层级**——"卷""章"只是节点类型，不是固定的表结构，因此单篇文章与超长篇共用一套模型。
  - **正文与结构分表**，目录树只读元数据、正文按需加载，为大体量作品做准备。
  - **变更日志只追加**，带设备标识与本机单调序号，为后续多端同步打底。
- **版本体系三源分离**：引擎版本 / 数据结构版本 / 导出格式版本。
  其中数据结构版本**自动派生**自迁移列表末位，不手写，避免版本号与实际结构漂移；并提供统一汇总入口便于比对。
- **启动期环境校验**：除版本检查外，还对全文检索能力做**实测探针**（真实建表验证，而非查编译选项）。
- **时间源可控**：测试构建下可替换时间源；**发布构建不包含该能力**，并有自动化测试证明这一点。
- **测试 22 个全绿。**

### 修复与优化

- 修正 SQLite 版本号的数值类型不匹配问题。

---

## 0.2.0 — 2026-09-11

### 工程骨架

- **Rust workspace** 三 crate 划分：核心引擎（零界面依赖）/ 本机协议定义 / 桌面外壳。
  核心不依赖任何界面框架，可被单元测试与命令行直接驱动。
- **桌面外壳**：Tauri 2 + Vue 3 + Vite；正文编辑器采用 TipTap 3。
- **界面布局**：三栏结构（目录 ｜ 正文编辑 ｜ 侧栏），侧栏为后续的引导问答功能预留位置。
- **许可与治理文件**：`LICENSE`（AGPL-3.0）、`CLA.md`、`CONTRIBUTING.md`（含生态许可说明）、
  `THIRD_PARTY_NOTICES.md`（含字体许可纪律：不内置商业授权字体）。
- 应用图标；`.gitignore`。

### 修复与优化

- 记录两个开发环境问题：WSL 下的临时路径对 Windows 侧工具不可见；Windows 的 `npm.cmd` 需经由 `cmd.exe` 调用。

---

## 0.1.0 — 2026-09-11

### 初始

- 项目立项，确定名称为**研墨**。
- 确立三条主线：
  1. **地基**——不丢稿、长篇流畅、中文字数与排版准确、能一键交稿；
  2. **引导问答**——由软件主动提问帮助作者理清思路（**只问不写，不生成正文**）；
  3. **创作留痕**——记录写作过程，可供作者自证。
- 许可策略：**AGPL-3.0 开源核心**，未来以独立进程方式提供可选增值模块；数据主权相关能力永久免费。
