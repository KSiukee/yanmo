# 安全与边界

这份文档回答四件事：研墨**保证什么**、**做不到什么**、**失败时怎么办**、以及**你怎么自己验证**。

写法上有一条自我要求：**能证伪的才写**。所以每条承诺都尽量给出"你怎么看见它 / 怎么验证它"，
而且大部分承诺在仓库里有随测试跑的机械检查盯着——不是口号。

---

## 一、零出网

研墨只处理本机数据，**不发起任何网络连接**：核心不提供网络接口，桌面程序与命令行入口都不依赖任何网络库。
你写下的字、导入导出的文件，都留在你自己的机器上。

这一点有**机械检查**盯着，不靠自觉：

- `app/tests/boundary.rs`：核心与命令行入口的依赖里不许有界面框架、也不许有网络库；
- `app/tests/release_boundary.rs`：直接扫出货源码与全部依赖清单，出现套接字、HTTP 客户端、
  或"调外部下载命令"这类口子就红——注释里的说明不算（那份检查会把注释剥掉再判）。

### 你可以自己验证（三步，任选其一或都做）

1. **看依赖树**：`cargo tree -p yanmo-cli` 与 `cargo tree -p yanmo-core`，里面没有任何网络库。
2. **跑守卫**：`cargo test --workspace`——零出网守卫就在里面，改坏了会红。
3. **断网实测**：把机器断网（或断掉网卡）后正常写作、备份、导出、编译投稿包；全程应当无异常。
   想更严格就用系统自带的防火墙或资源监视器观察 `yanmo.exe` 的连接列表——**应当始终为空**。

> 说明：本机协议口（给第三方壳与增值模块预留的那个）目前**只设计、未实现**，默认关闭；
> 它开启时也只绑回环地址。详见下面「接口清单与默认状态」。

---

## 二、我们保证什么

七层防线，从"每一笔字"到"整库搬家"：

| 防线 | 它挡住的事 | 你怎么看见它 |
| --- | --- | --- |
| **1. 写前先存一份快照** | 手滑、误删、回滚错了 | 每章有版本历史（编辑器章名那行「版本」），自动快照滚动保留、你手动留的那一份不动 |
| **2. 落盘后核对指纹** | "界面说存了、库里其实没存" | 停笔约 250 毫秒落盘一次；此后每 2–5 秒核对一次"库里的正文还是不是我手上这份"（只比指纹，不搬正文） |
| **3. 核不上就先留底再回写** | 两边不一致时把哪一份覆盖掉 | 一旦发现不一致或长时间存不进去：**先把手上这份留成快照，再把库改回这一版**——不赌运气 |
| **4. 关窗先过闸门** | 关窗一瞬间丢掉没落盘的内容 | 关窗会先落盘；**存不下去就拦住**，并给你三条路：重试 / 导出到文件 / 仍然退出 |
| **5. 多处备份 + 写回体检** | 单库损坏、误操作、机器出事 | 备份写到你自己挑的几处（每日首次启动与关窗会自动做一次），**写完读回体检**，账本记着每一份的来源与指纹 |
| **6. 回收站与软删** | 删错了想找回来 | 删章删书都进回收站，能整本或整段捞回；彻底删除是你显式再确认一次的动作 |
| **7. 界面起不来也有取稿路** | 显卡、驱动、界面崩溃导致打不开 | 随发行包附带的命令行程序：**双击即中文菜单**（读取 / 体检 / 导出），菜单自己只读不写；库坏了另有一条「从成稿导入」（默认干跑、`--yes` 才写，且只新建） |

另有三条与"不丢稿"同族、值得单列的性质：

- **单实例锁**：同一个数据目录同时只允许一个研墨在跑，避免两个实例互相覆盖；第二个实例会明确告诉你"已经在运行"。
- **数据位置你自己定**：首次启动会问一次稿子放哪，之后随时能在设置里换位置；**换位置不删旧目录**，你自己确认没问题了再删。
- **命令边界机械可见**：界面不许碰文件系统、不许拼路径（权限集逐项最小化），数据目录由桌面壳解析——
  写入者只有一个，不会出现两套数据各写一半。

---

## 三、我们做不到什么（坦白清单）

信任靠坦白的边界建立。以下这些，**研墨现在不保证**，请按它来安排你的工作习惯：

| 做不到 | 意味着什么 | 你该怎么办 |
| --- | --- | --- |
| **备份包不加密** | 备份文件是普通文件，谁拿到磁盘谁就能读 | 备份到移动盘或网盘请自行加密；需要强加密请用系统级方案（BitLocker / VeraCrypt 等） |
| **单库仍是单点** | 数据目录所在磁盘坏掉，活库一起没 | 至少保持"异盘一份"（备份目标选另一块盘或移动盘），并定期把成稿导出带走 |
| **最后约 250 毫秒的输入可能丢** | 落盘是停笔后触发的，不是逐键落盘 | 养成"停一下再关窗"的直觉；关窗闸门会兜住大多数情况，但**强制结束进程**是兜不住的 |
| **强杀进程时手上那一份会丢** | 任务管理器结束进程、断电、蓝屏 | 重启后会明确告诉你"上次不是正常退出"，并回到崩溃前最后一次落盘的内容 |
| **不替你判断正文对错** | 错别字、语病、逻辑冲突不在"不丢稿"范围内 | 用内置的排版清理与体检（它们**只报告不自动改**），或导出后另行处理 |
| **没有云同步、没有自动更新** | 换机器要自己搬；升级要自己下载 | 用备份包加成稿导出搬机器；升级前先备份，升级后别急着删旧版本安装包 |
| **Windows 上数据默认在用户目录** | 重装系统或换用户账户可能丢 | 首次启动就把数据目录换到你自己管理的盘位（设置里随时可换） |
| **安装包没有代码签名** | 首次运行会被 SmartScreen 拦成"未知发布者" | 见 README 的说明；介意的话可以先用命令行入口确认数据，再决定是否安装 |

---

## 四、失败模式与应对

| 现象 | 发生了什么 | 你该怎么办 |
| --- | --- | --- |
| 状态栏长时间显示"保存中 / 存不下去" | 磁盘满、目录只读、库被别的程序锁住 | 按提示处理（清空间或换权限）；**不要直接强制关窗**——先试「导出到文件」把手上这份拿出来 |
| 提示"库里的正文与手上这份不一致" | 有别的程序改了库，或上一次写没落干净 | 界面会先留快照再回写；去「版本」里对照一下，确认无误再继续写 |
| 启动说"上次不是正常退出" | 上次被强杀或断电 | 按提示回到崩溃前那一章的最后落盘版本；检查「版本」里有没有自动留下的快照 |
| 界面白屏或起不来 | 显卡、驱动、WebView2 环境问题 | 用命令行程序取稿（双击即菜单），先确保字在；再排查运行环境 |
| 库文件损坏打不开 | 磁盘故障、断电写坏、外部程序写坏 | 用「从备份恢复」换库（会先给你看清会丢几天、多少字，换库前原库留底）；命令行体检能告诉你坏在哪一层；**连库都打不开、只剩备份包时**，让研墨开一个新库再用命令行的「从成稿导入」把整本书收回来（不依赖数据库文件） |
| 时间显示不对或码字账本串天 | 系统时间被改过（或时区变化） | 账本按本地日期记，跨时区旅行会出现"某天特别多或特别少"；数据本身不受影响 |
| 提示"已经在运行" | 单实例锁（防止两个实例互相覆盖） | 找到正在跑的那个窗口；确认没有卡死的残留进程再重试 |

---

## 五、数据在哪、怎么带走

**在哪**：桌面程序顶栏最右侧那行小字一直显示数据目录与库文件名（点一下用文件管理器打开）；
设置面板的「稿子放在哪」里有一份明确的路径，「关于」里也能一键打开那个文件夹。

**怎么带走**（四种，互不依赖）：

1. **成稿导出**：把某一本导成投稿版 `.docx`、分章 `.txt` 或合并 `.txt`——给编辑、给自己留底都行；
2. **备份包**：带清单与指纹的一致性快照，可在另一台机器上用「从备份恢复」打开；
3. **版本快照**：单章的版本历史可以逐版对照，必要时导出；
4. **命令行导出**：界面起不来时的取稿路径；库彻底坏了还有「从成稿导入」把整本书收回新库
   （默认只看不写，见下面命令行那节）。

**换机器**的推荐顺序：先备份 → 再导出成稿 → 把备份包拷到新机器 → 用「从备份恢复」；
新机器上任一步失败，老机器上的原库**原样还在**。

---

## 六、接口清单与默认状态

| 接口 | 默认状态 | 说明 |
| --- | --- | --- |
| 桌面壳与核心之间的命令通道 | 开 | 界面唯一的取数口；权限集**逐项最小化**，不含路径与文件系统能力（有机械检查） |
| 命令行入口 `yanmo-cli` | 看稿与导出（会打开库、可能升级结构，不动稿件内容）+ 一条"默认只看不写"的导入 | 见下表的发行版命令；写库的开发档命令**在发布构建里根本不编译进去** |
| 自带验收能力（`--self-test-bench` / `--self-test-ui` / `--check` / `--mirror-sync`） | 随发行版提供；**不带这些启动参数时行为一字不变** | 只由启动参数进入、不开任何网络通道；数据目录由参数给（默认系统临时目录）。见下「自带验收」一节 |
| 本机协议口（`yanmo-proto`） | **关（未实现）** | 给第三方壳与增值模块预留，只设计不实现；将来开启也只绑回环加一次性 token |
| 出网能力 | **无** | 没有任何网络接口；有机械守卫（见第一节） |
| 自动化与故障复现能力 | **分两档** | ①**自带验收**（见下）：发行版里有，按启动参数进入，用于"任何人自己跑一遍"；②**开发档命令**（新建 / 写入 / 备份 / 注入这类）：只在开发构建里编进去，不随安装包分发 |

### 命令行入口（发行版只有这些）

| 命令 | 做什么 | 会改稿库吗 |
| --- | --- | --- |
| `verify` | 体检：库文件完整性 + 结构版本 | 会打开库（可能升级结构、登记本机、给老库回填字数标记）；**稿件内容一个字不动** |
| `works` | 列出书架上的书 | 同上：会打开库，稿件内容不动 |
| `nodes --work <id>` | 列出一本书的目录（只有元数据，不含正文） | 同上：会打开库，稿件内容不动 |
| `read --node <id>` | 读一章正文 | 同上：会打开库，稿件内容不动 |
| `export --work <id> --format txt\|json --out <目录>` | 把一本书导出成文件 | 同上：会打开库，稿件内容不动；另外只往你给的目录写文件 |
| `import --from <备份包目录 或 work.json> [--yes]` | 从成稿 JSON 把整本书**新建**回来 | 同上会打开库；不给 `--yes` 时不加任何东西，给了也只新建、不碰既有作品 |

**把这条边界看清楚**：上面这几条都不是"只读"——要读稿子就得先打开库，而打开会跑一次结构迁移与
本机登记（老库还会补一次字数标记）。它们从来改的不是你的稿子，但会动库文件本身。
**要体检或取证一份可能坏掉的库：先把库文件复制一份，再对副本跑 `verify`。**

**这一条 `import` 是发行版里唯一能给稿库加东西的命令**，所以它的限制写死在行为里，不是靠自觉：

- **默认干跑**：不给 `--yes` 时跑完全套（找成稿、验格式、与备份清单对账），**一个字节都不写库**；
- **只新建**：它只往库里**加**一本书，既有作品、正文、变更留痕一个字都不改（同名书会再建一本，不覆盖）；
- **要么整本都进，要么一个字都不进**：结构与正文在同一个事务里写完；
- **不伪造历史**：成稿里没有写作时间，导入的行**不计入「每日码字」账本**，留痕里只记"这一次导入"；
- **只读成稿 JSON**：不碰备份包里的数据库快照，也不需要能打开数据库。

它为什么必须待在发行版里：库彻底损坏时，作者手上能用的只有一个命令行入口加一份备份包——
把这条路只留给开发构建，等于在最需要它的时候把门关上。

其余会写库的命令（新建书与章、写入正文、登记会话等）只存在于开发构建：那部分代码在发布构建里
**根本不编译进二进制**，不是藏起来。开发构建是给自动化与故障复现用的（比如"写到一半把进程杀掉，
重启看稿子在不在"这类验证）。

### 自带验收（`--self-test-*` / `--check` / `--mirror-sync`）

程序里带了几条**自己验自己**的入口，用启动参数进入。四条共同的三条纪律：**数据目录由参数给**
（默认系统临时目录，不碰你正在用的那份稿库）、**不开任何网络通道**、**不带这些参数启动时
行为一字不变**。报告与结论一律写成文件（桌面程序在控制台里拿不到可用的标准输出）。

| 能力 | 怎么进 | 它做什么 | 你怎么验证 |
| --- | --- | --- | --- |
| 数据基准 | `--self-test-bench --dir <目录>` | **不开窗口**：造一份百万字级的库，量写入 / 检索 / 读章 / 重开 / 快照 / 体检 | 报告写在 `--report` 指定的文件里（默认在数据目录旁） |
| 界面冷启动 | `--self-test-ui --dir <同一目录>` | 开窗口打开同一份库，量"冷启动到界面可输入" | 与上一条合并成最终报告 |
| 库体检 | `--check <目录> [--out <文件>]` | 打开库、跑完整性检查，结论写成 JSON | 退出码 0 表示读得通；`--out` 里的 JSON 有 `integrity` / `works` / `chapters` / `words` |
| 磁盘 `.md` 镜像对账 | `--mirror-sync <目录> [--rounds N] [--out <文件>]` | **不开窗口**把"稿库 → 磁盘 `.md`"的对账跑 N 轮（默认 2），结论写成 JSON | 见下面两句 |

`--mirror-sync` 值得单独说清两件事：

- 它走的是**界面里同一条代码路径**（同一个对账函数）：同一份计划、同一份账、同一条
  "外面的改动一个字不碰"。它**不是**另写一份"给测试用的实现"，所以你验到的就是平时跑的那套。
- 报告里的 `rounds` 是每一轮"有没有真往盘上写"：一轮**该写**、紧接着的第二轮**什么都不该动**
  ——幂等有据可查，不是靠保证。退出码：`0` 账对上了 / `1` 有书没对上或出过错 /
  `2` 连库都打不开（**没跑起来**，与"跑完发现不对"分开说）。

> 与 `--check` 一样，这几条都会**打开库**（可能跑一次结构迁移、登记本机）。要体检或取证一份
> 可能坏掉的库，先把库文件复制一份再对副本跑。它们从来不动你的稿件内容。

命令行程序不猜数据目录：位置一律由 `--data` 明确给出，与桌面程序各用各的。
找不到库时它**绝不新建一个空库**——不会让你误以为稿子没了。导出的文件放在「文档」下的
`YanmoExport/RescueExport/<书名>/` 里，不会动你原本导出过的文件。

### 最省事的用法：双击

**不带任何参数双击它**（或直接运行），会打开一个中文菜单：

```
[1] 读取稿子（看看字还在不在）
[2] 检查数据库健康度
[3] 导出某一本 / 某一篇
[4] 导出全部
[5] 退出
```

数据目录它会自己按系统约定去找；找不到就请你粘贴路径（并告诉你去哪儿看：桌面程序顶栏那行小字）。

### 你可以自己验证命令行入口

```bash
# 1) 看它到底有哪些命令（发行版只列上面那几条）
yanmo-cli --help

# 2) 拿一条"会写库"的开发档命令试——发行版会明确拒绝，而不是默默执行
yanmo-cli --data <数据目录> write --node 1 --body x
# → {"ok":false,"code":"cli.usage","detail":"不认识的命令：write"}   （退出码 2）

# 3) `import` 不给 --yes 时只算不写：跑完对比一下库文件的修改时间与大小，一个字节都没动
yanmo-cli --data <数据目录> import --from <备份包目录>
# → {"ok":true,"command":"import","wrote":false,…}

# 4) 看依赖树：里面没有任何网络库
cargo tree -p yanmo-cli
```

命令行程序的输出只有**错误码 + 参数**（JSON 格式），不写额外的隐藏日志，也不保留任何额外状态——
它做的事和你从数据库里读到的完全一致。

---

## 七、其它边界

数据主权、许可（核心 AGPL-3.0-or-later）、模块权限边界这些，见 `README.md` 与 `CONTRIBUTING.md`；
它们与本文档讲的是同一件事的不同侧面：**你的稿子只属于你，任何程序能对它做什么都应当是可以核对、可以拒绝的。**

---

## 八、发现了安全问题，怎么告诉我们

**优先用 GitHub 的私密报告**：仓库页面上 **Security → Report a vulnerability**。
那条通道只有维护者看得到，不会被公开、也不会惊动别人——**报告在被修好之前不公开**。

不是安全问题、只是 bug 或疑问，走 [Issues](https://github.com/KSiukee/yanmo/issues) 就行。

**不管走哪条，都请别贴这些**：

- **稿子正文**（哪怕只是一段）——描述现象不需要它；
- **完整的数据库文件或备份包**；
- **你自己的文件路径**（截图里请打码；报"打不开"时说清操作步骤就够了）。

真要给复现素材，用一份**新键的空白书**重现同样的步骤，比给真实稿件安全得多。

**我们会怎么回**：这是个人项目，**不承诺响应时限**（也不想给你一个做不到的 SLA）；
但我们保证一句实话——能不能修、什么时候修，会明确告诉你，不会装作没看见。
如果你的稿子已经处在危险中（库坏了、数据可能丢），先看上面的「四、失败模式与应对」，
那几条自救路径不依赖我们在线。
