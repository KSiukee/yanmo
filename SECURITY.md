# 安全与边界

这份文档回答三件事：研墨**能做什么**、**默认怎么做**、以及**你怎么自己验证**。

## 零出网

研墨只处理本机数据，**不发起任何网络连接**：核心不提供网络接口，桌面程序与命令行入口都不依赖任何网络库。你写下的字、导入导出的文件，都留在你自己的机器上。

这一点有机械检查盯着，不靠自觉：`app/tests/boundary.rs` 会断言核心与命令行入口的依赖里**没有任何界面框架、也没有任何网络库**——引进来就红。

## 命令行入口

从 0.18.0 起，项目多了一个命令行程序 `yanmo-cli`。它存在的理由是具体的：
**界面起不来的时候，你得有一条不依赖界面的路把稿子取出来**——显卡崩溃、驱动失活、界面白屏、
启动失败，这些情况下写作界面用不了，但你的字还在。

### 最省事的用法：双击

**不带任何参数双击它**（或直接运行），会打开一个中文菜单：

```
[1] 读取稿子（看看字还在不在）
[2] 检查数据库健康度
[3] 导出某一本 / 某一篇
[4] 导出全部
[5] 退出
```

数据目录它会自己按系统约定去找；找不到就请你粘贴路径（并告诉你去哪儿看：桌面程序顶栏那行小字）。
**找不到库时它绝不新建一个空库**——不会让你误以为稿子没了。导出的文件放在"文档"下的
`YanmoExport/RescueExport/<书名>/` 里，不会动你原本导出过的文件。

### 想脚本化也可以按命令用（发行版只有这几条）

| 命令 | 做什么 |
| --- | --- |
| `verify` | 体检：库文件完整性 + 结构版本 |
| `works` | 列出书架上的书 |
| `nodes --work <id>` | 列出一本书的目录（只有元数据，不含正文） |
| `read --node <id>` | 读一章正文 |
| `export --work <id> --format txt\|json --out <目录>` | 把一本书导出成文件 |

**全部是只读或只写文件——发行版里没有任何能修改稿库的命令。**

会写库的命令（新建书与章、写入正文、登记会话等）只存在于**开发构建**：那部分代码在发布构建里**根本不编译进二进制**，不是藏起来。开发构建是给自动化与故障复现用的（比如"写到一半把进程杀掉，重启看稿子在不在"这类验证）。

命令行程序不猜数据目录：位置一律由 `--data` 明确给出，与桌面程序各用各的。

### 你可以自己验证

```bash
# 1) 看它到底有哪些命令（发行版只列上面那几条）
yanmo-cli --help

# 2) 拿一条"会写库"的命令试——发行版会明确拒绝，而不是默默执行
yanmo-cli --data <数据目录> write --node 1 --body x
# → {"ok":false,"code":"cli.usage","detail":"不认识的命令：write"}   （退出码 2）

# 3) 看依赖树：里面没有任何网络库
cargo tree -p yanmo-cli

# 4) 跑一遍边界守卫（核心与入口都不得依赖界面框架 / 网络库）
cargo test --workspace
```

命令行程序的输出只有**错误码 + 参数**（JSON 格式），不写额外的隐藏日志，也不保留任何额外状态——它做的事和你从数据库里读到的完全一致。

## 其它边界

数据主权、许可（核心 AGPL-3.0-or-later）、模块权限边界这些，见 `README.md` 与 `CONTRIBUTING.md`；它们与本文档讲的是同一件事的不同侧面：**你的稿子只属于你，任何程序能对它做什么都应当是可以核对、可以拒绝的。**
