# 发一个版本

这份文档写给"要出包的人"（现在的维护者，以及将来的你）。目标只有一个：
**发出去的每个文件，别人都能核对它是从哪份源码、用哪套工具链构建的。**

## 一条命令出包（本地）

```bat
tools\build-release.bat
```

它做六步，任一步失败就停、不出包：

1. 工具链自检（cargo / node / tauri CLI，缺了给人话提示）
2. **版本一致**：Cargo 工作区、应用配置、`CHANGELOG.md` 首条三处必须相同
3. 前端依赖与生产构建
4. **跑测试**（失败就不出包——宁可不发，也不发一个没测过的包）
5. 打包（NSIS 安装器）
6. 归集到 `dist/`：文件名带版本号，附 `.sha256` 与 **`构建指纹-<版本>.txt`**

本地反复出包时可用 `--no-bundle`（只出可执行文件）与 `--skip-tests`（**正式出包别用**）。

## 出包前先在本地预演一遍（推荐）

```bat
tools\rehearse-release.bat
```

它把**控制台代码页调成 CI 跑手那样（1252，西文）**，再跑一遍 CI 会跑的那两步（构建 + 生成发布说明）。

为什么需要这一步：CI 的 Windows 跑手是个**干净的、说西文的**机器，而本地往往是中文环境——
下面这些差异**本地完全看不见**，等打了 tag 才发现就白等一轮云端的十几分钟：

| 差异 | 本地 | CI 跑手 | 曾造成 |
| --- | --- | --- | --- |
| 控制台代码页 | 936（中文） | **1252（西文）** | 出包六步全绿，卡在生成发布说明（Python 打印中文抛 `UnicodeEncodeError`），Release 一步没走到 |
| 检出换行 | LF（`autocrlf=false`） | **CRLF**（`autocrlf=true`） | 一条查跨行选择器的守卫断言永远找不到那段 CSS，本地全绿、CI 红 |
| 仓库里的内部检查脚本 | 在 | **不在** | 测试会退到公开那两件（这是**设计如此**，不是故障） |

改了 `tools\` 下的脚本、改了 `.github\workflows`、或换了机器之后，出包前跑一遍它。

## 打 tag 自动出包（CI）

推一个 `v*` 形式的 tag 就会触发（工作流见 `.github/workflows/release.yml`）：

```bash
git tag v0.41.9
git push origin v0.41.9
```

CI 在 Windows runner 上做的事和人一样：装工具链 → 跑同一条构建命令 → 上传产物、校验和、构建指纹，
并用 `CHANGELOG.md` 的最新一节生成发布说明（缺"怎么核对"那段会自动补上，见 `tools/release_notes.py`）。

## 发布前的检查单

- [ ] **测试全绿**：`cargo test --workspace` 与 `cd app/frontend && npm test`
      （构建脚本会自己跑一遍，失败就不出包）
- [ ] **版本四处一致**：`Cargo.toml`（工作区）、`app/tauri.conf.json`、`CHANGELOG.md` 首条、`README.md` 里的「当前版本」
- [ ] **CHANGELOG 写清**"做了什么 + 验收证据"（不是只写日期）
- [ ] **桌面冒烟**：装 → 首启（问位置、建书）→ 写 → 存 → 关 → 重开（稿子在）→ 卸载（**稿子不被删**）
- [ ] **数据兼容**：这一版有没有改库结构/迁移；如果有，必须**只增不改**，并且用一份上一版的真实库跑一遍
      （升级后每章正文逐字节一致、结构与计数不变）
- [ ] **发布说明里必须有**：产物清单、每个产物的 SHA256、构建指纹、"未签名怎么继续"、
      "零出网怎么自验"（指向 `SECURITY.md`，不要另抄一遍）
- [ ] **三个产物都在**：一次构建出齐 `研墨-<版本>-setup.exe`（安装包）、`研墨-<版本>.exe`（便携版）、
      `研墨-<版本>-绿色版.zip`（绿色版）。绿色包里应当是**不带版本号的 `研墨/` 文件夹**
      （exe + `yanmo-portable.txt` + `LICENSE` + `一页怎么用.txt`）——文件夹名带版本号会让
      "解压新版"变成"换了一个新文件夹"，而稿子在旧文件夹的 `data\` 里，作者会以为稿子没了
- [ ] **许可证随包**：`app/tauri.conf.json` 的 `bundle.licenseFile` 指向仓库根的 `LICENSE`；
      绿色包内也要有一份（AGPL 分发二进制随包给许可证）
- [ ] **绿色版真机走一遍**：解压 → 双击 → 首启选「就用这里」→ 写一章 → 关；
      **确认 `yanmo-location.txt` 没有出现在文件夹里**（便携形态不该写它，
      写了就会在"文件夹改名 / U 盘换盘符"时变成打开空库或报错）→ 然后把**整个文件夹改名**再打开，
      稿子要还在（这一条是便携模式的命根子）

## 发布前的升级演练（拿上一版的真实库跑一遍）

**规矩：每个要发出去的版本，都用上一版写出来的真实库跑一遍常用动作。**

为什么不能只靠自动测试：那边用的是**合成样本**（按老结构造出来的库）。而真机上的东西是合成不出来的——
名字里的生僻字、深浅不一的目录、开过场景卡、删过章又从回收站捞回来、设置里改过命名规则……
"跨版本升级不丢稿"这件事，只有真样本能证明。

### 三步

**① 备一份样本**（只在副本上演练，**原库一个字都别碰**）

用上一版（或你现在装的这一版）写几章、存一次、做一次备份，然后把整个数据文件夹复制出来当样本。
⚠️ 连 `yanmo.db-wal` / `yanmo.db-shm` **一起复制**——只复制 `.db` 会留下一份不一致的库。

```bat
rem 数据文件夹的位置在软件顶栏那行小字里（点一下就能打开）
robocopy "<你的数据文件夹>" "<演练目录>\样本" /E
```

**② 用新版本打开副本，跑一遍常用动作**

```bash
# 命令行能把大半条链走完（发行版里就有这些命令）
yanmo-cli --data <副本目录> verify            # 打开即跑环境校验与迁移；要 integrity: ok
yanmo-cli --data <副本目录> works             # 书架：每本书的章数与字数
yanmo-cli --data <副本目录> nodes --work <id> # 目录树
yanmo-cli --data <副本目录> read --node <id>  # 读一章
yanmo-cli --data <副本目录> search --query <书里的一句话>   # 全文索引
yanmo-cli --data <副本目录> export --work <id> --format txt --out <导出目录>
```

**③ 对账：逐字比对"演练前"与"演练后"**

最省事的公开口径是**两边各导出一份成稿再 diff**：

```bash
yanmo-cli --data <副本的原样备份> export --work <id> --format txt --out before
yanmo-cli --data <副本>          export --work <id> --format txt --out after
diff -r before after        # 演练中没有改动的内容必须一字不差
```

**判据**（缺一条就不是"升级安全"）：

| 判据 | 看什么 |
| --- | --- |
| 打开没报错、库完好 | `verify` 给 `integrity: ok`，没有"结构过新/迁移失败" |
| **正文逐字不变** | `diff -r before after` 对没动过的书**零差异** |
| 规模只增不减 | `works` 的章数与字数与升级前一致（写了新章则 +N） |
| 索引跟得上 | `search` 找得到老章里的句子；新写的也能被搜到 |
| 快照与回收站还在 | 界面上「版本」里老快照列得出来；回收站里有东西的话还列得出来 |
| 还能接着写 | 新写一章能落盘，关窗重开后字还在 |
| 库结构版本按预期 | 迁移只前进，不倒退 |

### 真跑过一次的记录（2026-09-14）

样本：v0.43.5 写出来的真实库（2 本书 / 11 个节点 / 8 份快照 / 1 天账本）。
用它跑 0.44.0：`verify` 给 `integrity: ok`；**11 个原有节点逐字一致**（正文 SHA256、标题、
三口径字数、层级、顺序全对）；导出《祺》得 9 个文件；新写一章后索引 11 → 12 行、
书架章数 9 → 10；库结构 v7 → v7。

## 关于"可复现"

我们不宣称**逐字节可复现**：Windows 安装包里含时间戳等非确定性内容，做不到就是做不到。
能给的、也确实有用的是两件事，都写在 `构建指纹-<版本>.txt` 里：

1. **同一提交 + 同一工具链 → 功能等价的产物**；
2. **产物指纹**（SHA256）与"这一包是哪次提交、用哪套工具链构建的"的记录，方便任何人核对。

所以：改完代码先把提交落干净再出包——指纹里会标出"工作区有未提交改动"，
那种包不适合对外发。

## 发出去之后发现严重问题怎么办

**不要用同名文件悄悄替换**：早下载的人手里是旧文件、晚下载的是新文件，名字却一样，
哈希对不上时没人说得清谁是对的。正确做法是发一个新版本，并在说明里写清：

- 上一版有什么问题、影响谁（例如"某次操作可能丢一段正文"）；
- 这一版改了什么、怎么确认自己是否受影响；
- 如果你手上是旧版，稿子怎么先导出/备份出来。

## 相关文档

- 安全承诺、做不到的部分、零出网与命令行入口的自验步骤：[SECURITY.md](./SECURITY.md)
- 用起来（下载哪个、怎么装、首启看到什么、常见问题）：[README.md](./README.md)
- 改代码（前置条件、跑测试、分层约定）：[CONTRIBUTING.md](./CONTRIBUTING.md)
