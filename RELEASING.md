# 发一个版本

这份文档写给"要出包的人"（现在的维护者，以及将来的你）。目标只有一个：
**发出去的每个文件，别人都能核对它是从哪份源码、用哪套工具链构建的。**

## 一条命令出包（本地）

```bat
tools\build-release.bat
```

它做六步，任一步失败就停、不出包：

1. 工具链自检（cargo / node / tauri CLI，缺了给人话提示）
2. **版本一致**：Cargo 工作区、应用配置、`CHANGELOG.md` 首条三处必须相同
3. 前端依赖与生产构建
4. **跑测试**（失败就不出包——宁可不发，也不发一个没测过的包）
5. 打包（NSIS 安装器）
6. 归集到 `dist/`：文件名带版本号，附 `.sha256` 与 **`构建指纹-<版本>.txt`**

本地反复出包时可用 `--no-bundle`（只出可执行文件）与 `--skip-tests`（**正式出包别用**）。

## 打 tag 自动出包（CI）

推一个 `v*` 形式的 tag 就会触发（工作流见 `.github/workflows/release.yml`）：

```bash
git tag v0.41.9
git push origin v0.41.9
```

CI 在 Windows runner 上做的事和人一样：装工具链 → 跑同一条构建命令 → 上传产物、校验和、构建指纹，
并用 `CHANGELOG.md` 的最新一节生成发布说明（缺"怎么核对"那段会自动补上，见 `tools/release_notes.py`）。

## 发布前的检查单

- [ ] **测试全绿**：`cargo test --workspace` 与 `cd app/frontend && npm test`
      （构建脚本会自己跑一遍，失败就不出包）
- [ ] **版本四处一致**：`Cargo.toml`（工作区）、`app/tauri.conf.json`、`CHANGELOG.md` 首条、`README.md` 里的「当前版本」
- [ ] **CHANGELOG 写清**"做了什么 + 验收证据"（不是只写日期）
- [ ] **桌面冒烟**：装 → 首启（问位置、建书）→ 写 → 存 → 关 → 重开（稿子在）→ 卸载（**稿子不被删**）
- [ ] **数据兼容**：这一版有没有改库结构/迁移；如果有，必须**只增不改**，并且用一份上一版的真实库跑一遍
      （升级后每章正文逐字节一致、结构与计数不变）
- [ ] **发布说明里必须有**：产物清单、每个产物的 SHA256、构建指纹、"未签名怎么继续"、
      "零出网怎么自验"（指向 `SECURITY.md`，不要另抄一遍）

## 关于"可复现"

我们不宣称**逐字节可复现**：Windows 安装包里含时间戳等非确定性内容，做不到就是做不到。
能给的、也确实有用的是两件事，都写在 `构建指纹-<版本>.txt` 里：

1. **同一提交 + 同一工具链 → 功能等价的产物**；
2. **产物指纹**（SHA256）与"这一包是哪次提交、用哪套工具链构建的"的记录，方便任何人核对。

所以：改完代码先把提交落干净再出包——指纹里会标出"工作区有未提交改动"，
那种包不适合对外发。

## 发出去之后发现严重问题怎么办

**不要用同名文件悄悄替换**：早下载的人手里是旧文件、晚下载的是新文件，名字却一样，
哈希对不上时没人说得清谁是对的。正确做法是发一个新版本，并在说明里写清：

- 上一版有什么问题、影响谁（例如"某次操作可能丢一段正文"）；
- 这一版改了什么、怎么确认自己是否受影响；
- 如果你手上是旧版，稿子怎么先导出/备份出来。

## 相关文档

- 安全承诺、做不到的部分、零出网与命令行入口的自验步骤：[SECURITY.md](./SECURITY.md)
- 用起来（下载哪个、怎么装、首启看到什么、常见问题）：[README.md](./README.md)
- 改代码（前置条件、跑测试、分层约定）：[CONTRIBUTING.md](./CONTRIBUTING.md)
