# 参与研墨

## 生态许可说明（先读这个）

研墨核心采用 **AGPL-3.0-or-later**。很多人看到 AGPL 会紧张，所以把话说清楚：

| 你想做的事 | 可以吗 | 你的义务 |
|---|---|---|
| 自己用、随便改 | ✅ | **无** |
| 写主题 / 皮肤 / 字体 / 词库包并分享 | ✅ | **无** |
| 写独立前端壳（Android / macOS / Linux / Web），调用研墨公开协议 | ✅ | **无**（可闭源，甚至可收费） |
| fork 核心代码做自己的版本并分发 | ✅ | **整个版本必须以 AGPL 开源** |
| fork 核心 + 改个名字闭源卖 | ❌ | 侵权 |

**一句话**：除了最后一行，其他全是"可以"。AGPL 不禁止任何人使用、修改、分发——它只要求**分发时开源**。

### 为什么这样选

我们要防的是"**拿走源码、改改函数名、重打包当闭源软件甚至商业软件二次售卖**"。同时我们又必须让核心可被审计——因为研墨的头号承诺是「**你的稿子在你手里**」，一个闭源软件说这句话是没人能验证的。

### 为什么"独立壳"能不受约束

因为**进程边界就是许可边界**：

- 核心是「**可执行程序 + 公开协议**」，不是"别人必须链接的库"；
- 通过协议通信的独立程序**不构成衍生作品**，因此不受 AGPL 约束；
- 相反，**直接链接核心代码**（静态或动态）就构成衍生作品，必须整体 AGPL。

所以：**要性能就链接（须开源），要自由就走协议（无义务）。**

### 关于模块生态（第三方也能做，我们目前不抽成）

- 核心是 AGPL，所以**第三方可以做自己的壳，也可以做自己的模块**（闭源、收费都行），
  **我们不需要你分成**——**目前不对第三方模块抽成**。将来如果提供官方商店、收款、认证这类增值服务，
  会**事先把规则说清楚**：愿意用就用，不愿意用照样能自己分发。
- 两条硬边界（比抽成重要）：
  1. **不得使用「研墨」作为你的产品名或图标**——官方壳才是研墨，免得用户被误导；
  2. 模块要**标明来源与作者**，并声明它需要哪些数据权限。
- 一句邀请：如果你想要的某件事是**人人都需要**的基础能力，直接提 issue —— 它很可能应该进核心、
  **永远免费**，而不是做成模块。

### 你可以赞助某个核心能力（但我们不卖边界）

- 如果某个模块需要的能力（比如新增一类**只读数据**、或一个**通用钩子**）核心还没有，**可以提请求**；
  想让它更快实现，也可以**赞助**——我们收的是**开发劳动**，不是"平台租金"。
- 赞助能得到：**优先级 + 参与设计讨论**（一起想怎么做更合适）。
- 赞助**换不到**什么（写清楚，免得误会）：
  1. **不换数据与安全边界**——某个能力该不该开放，先按判据审；审不过，出多少钱都不做；
  2. **不换排他**——做出来的能力**所有模块一视同仁**，不会只给某一家用；
  3. **不换专有权利**——成果按 AGPL 并入核心、开源给所有人（想独占就自己 fork，许可允许，但你得自己维护分叉）；
  4. **不换运行时特权**——核心代码里不会因此多出任何授权检查。
- 流程公开：请求、结论（做 / 不做 / 为什么）、有没有人赞助，都写在公开处。
  **路线仍由项目自己定——赞助可以插队，不能决定路线。**
- 能力清单会**按版本**演进：模块声明"需要能力 vN"，核心版本不够时会明确告诉你"请更新研墨"，
  而不是含糊地不工作。

## 贡献

欢迎 PR。为了让项目保留将来双许可与商业化的自由，代码贡献需要签署 [CLA](./CLA.md)。

实际最有价值的贡献（这类项目通常缺的就是这些）：

- 敏感词库、起名词库
- 主题 / 皮肤 / 导出模板（这些是**纯数据文件**，不涉及代码许可）
- Bug 报告与复现步骤
- 文档与翻译

## 支持研墨

如果研墨帮到了你，可以请我喝杯茶：**[爱发电](https://afdian.com/a/siukee)**。

捐赠是自愿的，**没有回报，也不代表对持续更新的承诺**。项目真正的承诺只有一条：
**不让你被困住**——源码公开、数据可导出、格式开放，哪怕项目停止更新，你的稿子也不会被锁住。

## 开发须知

**必读铁律**：

1. **代码健康**：核心文件软上限 200 行、单职责、禁 utils 垃圾桶。行数是信号，**重复与耦合才是病**——别为凑行数硬拆。
2. **架构**：`yanmo-core` **零 UI 依赖**（可单测、可 CLI 驱动）；**击键级同步语义绝不跨进程**。
3. **许可**：引入任何第三方代码/素材前先确认许可；**不引入强传染性依赖**。

**代码分层**：

```
crates/yanmo-core/   引擎：数据权威 + 全部业务逻辑（零 UI 依赖）
crates/yanmo-proto/  本机协议定义（公开、稳定、易解析）
app/                 官方桌面壳（Tauri 2 + Vue 3 + TipTap 3）
```

**构建**（Windows）：需要 Rust 1.97+、Node 24+、Visual Studio Build Tools、WebView2。

```bash
cd app/frontend && npm install
cd ..            && cargo tauri dev
```

**提交前**：确认 `git status` 中没有构建产物与本地临时文件（详见 `.gitignore`）。
