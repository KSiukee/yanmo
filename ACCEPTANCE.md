# 研墨验收数据（自己跑得出来的那种）

> 竞品常见的是"只写文案不给证据"。这份文档反过来：**每条承诺都给出怎么验、实测多少、你怎么自己复核**。
> 数字由 `研墨.exe --self-test` 量出来（**0.28.0 起**），脚本在 [`tools/acceptance/`](tools/acceptance/README.md)——
> 不用装 Rust、不用装 Python，双击 `run-acceptance.bat` 就行；脚本会先核对版本，老版本会明确停下。

**我们不承诺具体数字**（机器不同，数字不同；承诺一个数字再拿它去要求别人是耍流氓），
但承诺三件事：**测量方法公开、数字可以复现、边界如实写出来**。

---

## 一、不丢稿

**承诺**：正常写作、崩溃、断电、换电脑，稿子不该丢。

### 怎么验

| 场景 | 怎么造 | 期望 |
| --- | --- | --- |
| 正常退出再打开 | 写完关窗，重开 | 字数只多不少 |
| **硬杀**（断电 / 任务管理器结束进程） | 写到一半直接杀进程 | 重开后最近一次落盘的内容还在 |
| 备份拿回来 | 从多个备份位置里挑一份恢复 | 恢复前先体检、说清会退回几天/少多少字；原库留底不删 |
| 坏库现场 | 拿一份损坏的库当恢复来源 | 拒绝恢复、**一个字节都不动**；坏库改名留底当证据 |
| 拔盘 | 备份盘拔掉再插回 | 列表里如实消失/出现，不假装成功 |
| 极端输入 | 23 种恶意检索词、越界 id、10 万字标题、跨作品挂载、恶意文件名 | 明确报错，**零崩溃**（没有 panic） |

上面每一条都有**自动化测试**盯着（核心侧 `crates/yanmo-core/tests/` 下的 `autosave / robustness /
restore / snapshots / trash / counts`，共 40+ 例），另有界面侧与壳侧各一组。
`tools/acceptance/drill-safekeeping.bat` 是你自己能跑的**真机演练**：沙盒里铺 50 章 → 打开写字 →
**硬杀** → 再打开，打印两次账（章数/字数），只该往上走。

### 关于"落地"这件事的边界

- 写完到落盘之间有一个很短的窗口（几十毫秒）；**断电正好卡在这个窗口**，最多丢最后几个字。
- 我们不做"绝不丢一个字"这种没法兑现的承诺。能兑现的是：**每次落盘都是原子的**
  （写临时文件 + rename），所以最坏情况是"少最后几个字"，不会出现"半个文件"或"整个库打不开"。

---

## 二、百万字不卡

**承诺**：长篇（百万字量级）不该卡——打开、翻章、检索、写字都要跟得上手。

### 实测（开发机，2026-09-13，v0.28.0）

机器：Intel64 Family 6 Model 191 Stepping 2（**16 逻辑核心**）· 31.8 GB · Windows
数据：**1000 章 × 3000 字 ≈ 300 万字**，库文件 21.2 MB
（这份报告由 `run-acceptance.bat` 一次跑完，原始文件 `acceptance-report-0.28.0-*.json/.md`）

| 步骤 | 耗时 | 说明 |
| --- | ---: | --- |
| 造数据·写入 1000 章（300 万字一次性铺完） | 913 毫秒 | 长篇最真实的那种压力 |
| 目录树·一次全拉（1001 个节点） | 58 毫秒 | 打开一本书 |
| 读一章正文（3000 字，冷读） | 57 毫秒 | 打开一章 |
| 写一章正文（改一个字存回去） | 0.54 毫秒 | 写字时的落盘 |
| 检索·四个字（走全文索引） | 14 毫秒 | 上限 50 条 |
| 检索·两个字（回退全扫） | 16 毫秒 | 上限 50 条 |
| 检索·一个字（回退全扫） | 14 毫秒 | 上限 50 条 |
| 库体检 `quick_check` | 101 毫秒 | 证明这份库是好的 |
| **一致性快照·把 300 万字备份一次** | 5.9 秒 | "不丢稿"的关键数字 |
| 关库再打开 | 82 毫秒 | 下次打开软件 |
| 整轮（含造数据） | 7.4 秒 | — |
| **界面冷启动（开窗 → 界面可用）** | **565 毫秒** | 单独进程量的真冷启动，含读回一章 |

> 界面那一步为什么单独开一个进程：同一个进程里先跑完数据基准，程序文件早进了系统缓存，
> 量出来的"冷启动"会偏乐观。报告里那三行 `-ui.log`（`shell.window_building` → `window.page_loaded`
> → `ui.ready`）就是它的时间线。

### 低配机（i7-4790 / GTX960 / 16 GB，Windows 10）

> 这一栏由用户在自己的老机器上跑 `run-acceptance.bat` 得到（**0.28.1**，2026-09-13）。
> 报的机器信息与上面那张表一样是脚本自己问出来的。

| 步骤 | 开发机（16 逻辑核 / 31.8 GB） | **低配机（8 逻辑核 / 15.9 GB）** | 倍数 |
| --- | ---: | ---: | ---: |
| 造数据·写入 1000 章（300 万字） | 913 毫秒 | 3.45 秒 | 3.8× |
| 目录树·一次全拉 | 58 毫秒 | 164 毫秒 | 2.8× |
| 读一章正文（3000 字，冷读） | 57 毫秒 | 164 毫秒 | 2.9× |
| 写一章正文（改一个字存回去） | 0.54 毫秒 | 0.76 毫秒 | 1.4× |
| 检索·四个字 | 14 毫秒 | 23 毫秒 | 1.6× |
| 检索·两个字 | 16 毫秒 | 26 毫秒 | 1.6× |
| 检索·一个字 | 14 毫秒 | 25 毫秒 | 1.8× |
| 库体检 `quick_check` | 101 毫秒 | 163 毫秒 | 1.6× |
| 一致性快照·备份一次 | 5.9 秒 | **2.3 秒** | **0.4×（低配机更快）** |
| 关库再打开 | 82 毫秒 | 209 毫秒 | 2.5× |
| **界面冷启动到可输入** | 565 毫秒 | **1.04 秒** | 1.8× |

### 这些数字怎么读

- **写字这件事一直是"无感"级**：写一章 0.76 毫秒、检索 25 毫秒——都在人察觉不到的范围内
  （一般把 100 毫秒以内当作"立即"）。所以"卡"不是长篇的问题，机器新旧都不卡在写字上。
- **"打开"类动作是几百毫秒的量级**：打开一章 164 毫秒、界面冷启动 1.04 秒。人对"打开"的容忍度大约在
  0.5–1.5 秒之间，所以 2014 年的机器上仍然是"正常打开"，不是"等它转圈"。
- **备份那一步反而低配机更快**（5.9 秒 → 2.3 秒）：说明这一步主要吃**磁盘顺序写**，不吃 CPU
  ——两台机器的数字差异在这里不能比 CPU 强弱，只能说明它跟你的盘有关。
- **3.8 倍那条（一次性铺 1000 章）**是最吃整机的一条：它是"一次性造一本书"的极端动作，
  日常写作里不会反复出现（第一章之后就是每章几十毫秒的事）。
- 一句话：**百万字量级在十年前的机器上依然可用**。但这些数字**不是承诺**，是实测——
  你自己的机器上跑一遍才算（同一份脚本，同一份数据规模，横向可比）。

---

## 三、CJK 算得对

**承诺**：中文字数不玩花样，三个口径各自说清怎么算，你能拿着文档核对。

### 三个口径

| 口径 | 怎么算 |
| --- | --- |
| **逐字** | 数所有可见字符：汉字、字母、数字、**标点也算**；空格与零宽字符不算 |
| **不含标点** | 只数"字"：汉字、字母、数字；标点、空格、零宽字符都不算 |
| **按词** | 中日韩文字**一个字算一个词**；连续的西文字母/数字算一个词（`hello` = 1，`don't` = 2） |

假名与谚文按**字**算（不整体算一个词）；零宽字符（如零宽空格、BOM）在任何口径下都不算。

### 对照表（这张表由测试锁住，口径变了测试会红）

| 正文 | 逐字 | 不含标点 | 按词 |
| --- | ---: | ---: | ---: |
| `你好，世界。` | 6 | 4 | 4 |
| `Hello, world!` | 12 | 10 | 2 |
| `第 1 章 开始` | 5 | 5 | 5 |
| `你好 world 世界` | 9 | 9 | 5 |
| `她说：「走吧。」` | 8 | 4 | 4 |

（换算依据：`Hello, world!` 里有 12 个可见字符，去标点剩 10 个字母，按词是 `Hello` + `world` 两个词。）

### 大数怎么说

- 字数超过一万时界面按"万字"显示一位小数（`display.words_wan`），原始数字仍然精确存在库里。
- 目录树、卷合计、书架看到的是**同一套口径**的合计（不是只加"按词"那一列），老库打开时会自动回填，
  不需要你把每一章点开一次。

---

## 已知边界（如实列，不藏）

1. **同步盘 / 移动盘**：不建议把稿子放在 OneDrive、坚果云、Dropbox 或 U 盘/移动硬盘上——
   同步盘会反复上传、偶尔回滚文件，SQLite 撞上可能坏库；移动盘写到一半被拔同理。
   选位置时软件会提示（但不拦；那是你的机器）。
2. **同一个数据目录只允许一个实例**：第二个研墨会被拦住并提示。两个进程同时写一个库是真会坏的。
3. **中文输入法**：Windows 输入法在 WebView2 的正文区有**上游未修缺陷**（首字丢失、全角标点要按两次），
   普通输入框不受影响。我们能控的"窗口激活/焦点时序"已经处理（v0.26.8 起真机复验通过）。
4. **文件名**：导出与备份文件名里的非法字符会被安全化（Windows 上不允许的字符不会进文件名）。
5. **单章极端体量**：单章 300 万字这种病态场景能读写，但界面渲染（TipTap）不做保证——
   正常写作按章切分不会有这个问题。

## 故障记录

这一节记录**发布之后**真实出现过的故障（有就写，没有就写"没有"）：

| 版本 | 日期 | 现象 | 影响 | 处置 |
| --- | --- | --- | --- | --- |
| — | — | 发布以来**没有收到丢稿报告** | — | 如果你遇到了，请附上数据目录里的 `yanmo.db`（或它的备份）与操作经过 |

> 报障时最有用的三样：**版本号**（标题栏 engine 那行）· **数据目录**（设置里能看到）· **当时在做什么**。
